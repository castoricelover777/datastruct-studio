{"section":"02-02","views":{"singly":{"02-02-singly-01":{"modes":{"detail":"// ============ 为什么要 typedef ============\n// C 语言里 struct LNode 必须连在一起写，typedef 之后：\n//     LNode     等价于  struct LNode\n//     LinkList  等价于  struct LNode *\n// 从此代码里只出现 LNode / LinkList，可读性大幅提升。\n// 注意 LinkList 是\"指针类型\"，所以 LinkList L 本身就是一个指针变量。\n\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n// 约定：函数返回 Status，OK 表示成功，ERROR 表示失败。\n#define OK 1\n// #define 是预处理指令，编译前做文本替换：代码里 OK 直接变成 1\n#define ERROR 0\n// 布尔值宏，教材习惯写法\n#define TRUE 1\n#define FALSE 0\n// 内存分配失败的专用错误码，取一个和 OK/ERROR 都不冲突的负数\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\n// 把 int 换成 float / char，整份代码不用改，这就是 typedef 的价值\ntypedef int Status;\ntypedef int ElemType;\n\n// 结点结构体：数据域 data + 指针域 next\n// ============ 内存里长什么样 ============\n//\n//   头结点(不存数据)        第 1 个结点          第 2 个结点\n//   ┌────┬──────┐      ┌────┬──────┐      ┌────┬──────┐\n//   │ -- │ next │─────▶│ 10 │ next │─────▶│ 20 │ NULL │\n//   └────┴──────┘      └────┴──────┘      └────┴──────┘\n//      L                 L->next            带数据的第一颗\n//\n// 头结点是\"哨兵\"：它不存有效数据，只为让插入/删除不用特判第一个位置。\ntypedef struct LNode {\n// 数据域，存具体元素\n    ElemType data;\n// 指针域，指向下一个结点；最后一个结点为 NULL\n// struct LNode *next 不能写成 LNode *next —— 此时 LNode 这个名字还没生效\n    struct LNode *next;\n// 一次 typedef 出两个名字：LNode（结点）和 LinkList（指向结点的指针）\n} LNode, *LinkList;","short":"\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n#define OK 1\n#define ERROR 0\n// 布尔值宏，教材习惯写法\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\ntypedef int Status;\ntypedef int ElemType;\n\n// 结点结构体：数据域 data + 指针域 next\ntypedef struct LNode {\n// 数据域，存具体元素\n    ElemType data;\n// 指针域，指向下一个结点；最后一个结点为 NULL\n    struct LNode *next;\n// 一次 typedef 出两个名字：LNode（结点）和 LinkList（指向结点的指针）\n} LNode, *LinkList;","none":"\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;"},"scaffold":"/* ====================================================================\n *  轮到你了：模块 01  头文件与 typedef\n *  作用：引入标准库，用 typedef 把 struct LNode 包装成好写又好读的类型名。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 只验证 typedef 是否可用：能声明变量、能访问成员就算过关 */\nint main(void)\n{\n    LinkList L = NULL;\n    LNode node;\n\n    node.data = 42;\n    node.next = NULL;\n\n    L = &node;\n\n    printf(\"结构体定义正确：L->data = %d，L->next = %s\\n\",\n           L->data, L->next == NULL ? \"NULL\" : \"非 NULL\");\n    printf(\"sizeof(LNode) = %d 字节\\n\", (int)sizeof(LNode));\n\n    return 0;\n}\n","expectedOutput":"结构体定义正确：L->data = 42，L->next = NULL\nsizeof(LNode) = 16 字节"},"02-02-singly-02":{"modes":{"detail":"// ============ 这个函数是后面所有插入操作的地基 ============\n// 任何\"插入\"最终都要先造出一个新结点，思路永远是：\n//   申请内存 → 填数据 → 指针域置 NULL → 把地址交出去\n// 内存申请必须检查失败！malloc 失败返回 NULL，直接解引用会崩溃。\n\n// 返回新结点的地址，所以返回类型是 LNode *\n// 参数 e 按值传递：函数内改 e 不影响调用者，因为我们只需要它的值\nLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 LNode *\n// sizeof(LNode) 通常等于 8（int 4 字节 + 指针 4 字节）或 16（64 位下指针 8 字节）\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n// printf 打印中文提示，让使用者知道是内存不够，而不是程序莫名其妙退出\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 新结点的 next 必须先置 NULL，否则它是野指针\n// 这是最容易漏的一步：漏了之后链表尾部就不\"封口\"，遍历会冲到非法内存\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}","short":"\n// 返回新结点的地址，所以返回类型是 LNode *\nLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 LNode *\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 新结点的 next 必须先置 NULL，否则它是野指针\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}","none":"\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ====================================================================\n *  轮到你了：模块 02  creatNode\n *  作用：向系统申请一块 LNode 大小的内存，填好数据域和指针域后返回它的地址。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 creatNode：数据填对了没有？next 是不是干净的 NULL？ */\nint main(void)\n{\n    LNode *p = creatNode(7);\n\n    printf(\"creatNode 造出的结点：data = %d，next = %s\\n\",\n           p->data, p->next == NULL ? \"NULL\" : \"非 NULL\");\n\n    free(p);\n    printf(\"结点已释放。\\n\");\n\n    return 0;\n}\n","expectedOutput":"creatNode 造出的结点：data = 7，next = NULL\n结点已释放。"},"02-02-singly-03":{"modes":{"detail":"// ============ 为什么需要\"头结点\" ============\n// 没有头结点时，在第一个位置插入/删除都要单独写一段代码；\n// 有了头结点，第一个位置和中间位置的操作逻辑完全一样，代码短一半。\n//\n//   初始化完成后：   L ──▶ ┌────┬──────┐\n//                          │ -- │ NULL │\n//                          └────┴──────┘\n\n// 参数是 LinkList *（二级指针），因为要修改调用者手里的 L 本身\n// 如果写成 LinkList L，函数内改了 L 也传不回去，调用者的 L 还是野指针\nStatus InitList(LinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (LinkList)malloc(sizeof(LNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}","short":"\n// 参数是 LinkList *（二级指针），因为要修改调用者手里的 L 本身\nStatus InitList(LinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (LinkList)malloc(sizeof(LNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}","none":"\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 03  InitList\n *  作用：建立只有头结点的空表：申请头结点、让它的 next 指向 NULL。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 InitList：初始化后应该是\"空表\"，而且 L 不能是野指针 */\nint main(void)\n{\n    LinkList L = NULL;\n\n    if (InitList(&L) != OK)\n    {\n        printf(\"初始化失败！\\n\");\n        return ERROR;\n    }\n\n    printf(\"初始化成功，L = %s\\n\", L != NULL ? \"有效头结点\" : \"NULL\");\n    printf(\"当前链表：\");\n    printList(L);\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"初始化成功，L = 有效头结点\n当前链表：(空表)"},"02-02-singly-04":{"modes":{"detail":"// ============ 指针推进过程 ============\n// 目标：在尾部追加 30\n//\n//  初始：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//          p\n//\n//  循环：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                              p        （p->next != NULL，继续走）\n//\n//  停止：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                                     p   （p->next == NULL，p 就是尾结点）\n//\n//  挂上：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n// 关键：p 从头结点起步，不是从 L->next 起步 —— 这样空表也能正确处理。\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\n// 返回值用 Status，让调用者知道追加成功与否\nStatus applist(LinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    LNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n// 循环条件是 p->next != NULL，看的永远是\"下一个\"，所以停下时 p 正是尾结点\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点，地址交给 s\n    s = creatNode(e);\n\n// 把新结点接到尾结点后面，链表就延长了一节\n// 只需要改一处指针；原来的尾结点 next 从 NULL 变成指向 s\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}","short":"\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus applist(LinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    LNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点，地址交给 s\n    s = creatNode(e);\n\n// 把新结点接到尾结点后面，链表就延长了一节\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}","none":"\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 04  applist\n *  作用：顺着 next 一路走到最后一个结点，把新结点挂到它后面。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 applist：连续追加后顺序是否保持，空表追加有没有问题 */\nint main(void)\n{\n    LinkList L = NULL;\n    int i;\n\n    InitList(&L);\n\n    printf(\"空表追加前：\");\n    printList(L);\n\n    for (i = 1; i <= 3; i++)\n    {\n        applist(L, i * 10);\n    }\n\n    printf(\"追加 10 20 30 后：\");\n    printList(L);\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"空表追加前：(空表)\n追加 10 20 30 后：10 -> 20 -> 30 -> NULL"},"02-02-singly-05":{"modes":{"detail":"// ============ 头插法 vs 尾插法 ============\n//\n//   applist（尾插）  ：从头一路走到尾，找到尾结点再挂上    —— 时间复杂度 O(n)\n//   HeadInsert（头插）：直接插在头结点后面，碰都不碰其他结点 —— 时间复杂度 O(1)\n//\n// 代价是头插会**颠倒顺序**：依次头插 10、20、30，得到的是 30 -> 20 -> 10。\n// 这不是缺陷，恰恰是头插法最常用的一个特性 —— \"头插法建表\"就是靠它做逆序的：\n//\n//     LinkList L;\n//     InitList(&L);\n//     for (i = 0; i < n; i++)\n//     {\n//         HeadInsert(L, a[i]);     /* 建出来的表正好是数组 a 的逆序 */\n//     }\n//\n// 再记住一个等价关系：HeadInsert(L, e) 和 ListInsert(L, 1, e) 干的是同一件事，\n// 前者只是把\"找第 0 个结点\"这一步省掉了 —— 因为头结点本身就是第 0 个。\n//\n// ============ 两步指针操作，顺序不能反 ============\n// 目标：把 5 头插进 10 -> 20 -> 30\n//\n//  插入前：   L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//\n//  第一步（新结点先接住原来的头一个）：s->next = L->next\n//            L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//                          ▲\n//            [5] ──────────┘\n//             s\n//\n//  第二步（头结点再指向新结点）：L->next = s\n//            L ─▶ [头] ─▶ [5] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//                          s\n//\n// 头结点 [头] 本身始终待在原地，头插法动的只是它的 next 指针。\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\n// 位置固定是第 1 位，所以不像 ListInsert 那样需要 i 这个参数\nStatus HeadInsert(LinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住原来头结点后面的那一串\n// 做完这一步，链表本身还是完整的，只是多了一个\"悬挂\"在外面的 s\n    s->next = L->next;\n// 第二步：头结点再指向新结点，s 正式成为第 1 个结点\n// 顺序同样不能反！先执行 L->next = s 的话，原来第一个结点的地址就丢了（内存泄漏）\n    L->next = s;\n\n// 插入成功\n    return OK;\n}","short":"\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus HeadInsert(LinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住原来头结点后面的那一串\n    s->next = L->next;\n// 第二步：头结点再指向新结点，s 正式成为第 1 个结点\n    L->next = s;\n\n// 插入成功\n    return OK;\n}","none":"\nStatus HeadInsert(LinkList L, ElemType e)\n{\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = L->next;\n    L->next = s;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 05  HeadInsert\n *  作用：把新结点直接挂到头结点后面，成为新的第一个结点，完全不需要遍历。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 HeadInsert：头插会颠倒顺序，而且插完链表必须仍然完整可遍历 */\nint main(void)\n{\n    LinkList L = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    printf(\"尾插 10 20 30 后：\");\n    printList(L);\n\n    HeadInsert(L, 5);\n    printf(\"头插 5 之后：     \");\n    printList(L);\n\n    HeadInsert(L, 1);\n    printf(\"再头插 1 之后：   \");\n    printList(L);\n    printf(\"（头插法建的链表天生逆序，「头插法建表」就是靠这个特性做倒序的）\\n\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"尾插 10 20 30 后：10 -> 20 -> 30 -> NULL\n头插 5 之后：     5 -> 10 -> 20 -> 30 -> NULL\n再头插 1 之后：   1 -> 5 -> 10 -> 20 -> 30 -> NULL\n（头插法建的链表天生逆序，「头插法建表」就是靠这个特性做倒序的）"},"02-02-singly-06":{"modes":{"detail":"// ============ 找第 3 个结点（i = 3）============\n//\n//   L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//          j=1     j=2     j=3\n//           p       p       p\n//\n//  循环结束时 j == i，p 正好停在第 i 个结点上。\n// 两个必须同时检查的失败情况：\n//   p == NULL  → i 比表长还大，数着数着掉出去了\n//   j > i      → i 小于 1（比如 i = 0），循环一次都没进\n\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\n// 这是 C 语言\"函数只能返回一个值\"的经典绕法：返回值给状态，数据走指针参数\nStatus GetElem_L(LinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n// 头结点不存数据，跳过它 —— 这是带头结点链表里最常见的起点写法\n    LNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n// p != NULL 写在前面，短路求值保证不会对空指针取 ->next\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}","short":"\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\nStatus GetElem_L(LinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n    LNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}","none":"\nStatus GetElem_L(LinkList L, int i, ElemType *e)\n{\n    LNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 06  GetElem_L\n *  作用：从头结点的下一个出发数到第 i 个结点，把它的数据域取出来。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 GetElem_L：正常位、边界位、越界位都要试 */\nint main(void)\n{\n    LinkList L = NULL;\n    ElemType e = -1;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n\n    printf(\"当前链表：\");\n    printList(L);\n\n    if (GetElem_L(L, 1, &e) == OK) printf(\"第 1 个元素 = %d\\n\", e);\n    if (GetElem_L(L, 3, &e) == OK) printf(\"第 3 个元素 = %d\\n\", e);\n    if (GetElem_L(L, 4, &e) == ERROR) printf(\"第 4 个元素：越界，返回 ERROR（符合预期）\\n\");\n    if (GetElem_L(L, 0, &e) == ERROR) printf(\"第 0 个元素：非法位置，返回 ERROR（符合预期）\\n\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"当前链表：10 -> 20 -> 30 -> NULL\n第 1 个元素 = 10\n第 3 个元素 = 30\n第 4 个元素：越界，返回 ERROR（符合预期）\n第 0 个元素：非法位置，返回 ERROR（符合预期）"},"02-02-singly-07":{"modes":{"detail":"// ============ 按值查找和按位查找的区别 ============\n// 按位查找：数位置，返回\"值\"\n// 按值查找：比值，返回\"结点地址\"\n//\n// 为什么返回地址而不是值？因为拿回地址后，调用者还能继续访问\n// p->next 等后继信息，也可以在它前面做插入删除。\n//\n// 找不到时返回 NULL —— C 语言里表示\"没有这个结点\"的标准做法。\n\n// 返回 LNode *，找不到就是 NULL\nLNode *LocateElem(LinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    LNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n// 一旦 p->data == e，循环立刻停下，p 就停在第一个匹配的结点上\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}","short":"\n// 返回 LNode *，找不到就是 NULL\nLNode *LocateElem(LinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    LNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}","none":"\nLNode *LocateElem(LinkList L, ElemType e)\n{\n    LNode *p = L->next;\n\n    while (p != NULL && p->data != e)\n    {\n        p = p->next;\n    }\n\n    return p;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 07  LocateElem\n *  作用：从头到尾逐个比较数据域，返回第一个值相等的结点的地址。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 LocateElem：找到了要能顺着 next 继续走，找不到必须给 NULL */\nint main(void)\n{\n    LinkList L = NULL;\n    LNode *p = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n\n    printf(\"当前链表：\");\n    printList(L);\n\n    p = LocateElem(L, 20);\n    if (p != NULL) printf(\"找到 20，它的后继是 %s\\n\", p->next != NULL ? \"有效结点\" : \"NULL\");\n\n    p = LocateElem(L, 10);\n    if (p != NULL) printf(\"找到 10，它就是第 1 个结点\\n\");\n\n    p = LocateElem(L, 99);\n    printf(\"查找 99 的结果：%s\\n\", p == NULL ? \"NULL（符合预期）\" : \"非 NULL（不符合预期）\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"当前链表：10 -> 20 -> 30 -> NULL\n找到 20，它的后继是 有效结点\n找到 10，它就是第 1 个结点\n查找 99 的结果：NULL（符合预期）"},"02-02-singly-08":{"modes":{"detail":"// ============ 插入的核心：两句指针赋值不能写反 ============\n// 目标：在第 2 个位置插入 15\n//\n//  插入前：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                       p\n//\n//  第一步（先接后面）：s->next = p->next\n//          [15] ─────▶ [20]\n//            s           ▲\n//                        └── 此时 p->next 还指着 [20]\n//\n//  第二步（再断前面）：p->next = s\n//          [10] ─▶ [15] ─▶ [20]\n//            p       s\n//\n// 如果两句写反，先执行 p->next = s，[20] 的地址就丢了（内存泄漏），\n// 而且 s->next 会指向 s 自己，链表出现环 —— 这是最经典的踩坑点。\n//\n// 还有一条铁律：先判断能不能插，再造结点。顺序颠倒会导致插入失败时\n// 白白多出一个没挂上链表的\"孤儿结点\"。\n\n// 参数 L 是头结点指针（不需要改 L 本身）；i 是位置，e 是数据\n// 位置 i 从 1 开始计数，表示\"插完以后它是第 i 个结点\"\nStatus ListInsert(LinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n// 这正是头结点的价值 —— 第 1 位和后面位置走同一段逻辑\n    LNode *p = L;\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n// 循环条件 j < i - 1：i = 1 时一次也不进循环，p 直接就是头结点\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n// 注意这里是先检查后造结点，避免产生孤儿结点\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住后面的那一串（顺序不能反！）\n    s->next = p->next;\n// 第二步：前驱再指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}","short":"\n// 参数 L 是头结点指针（不需要改 L 本身）；i 是位置，e 是数据\nStatus ListInsert(LinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n    LNode *p = L;\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住后面的那一串（顺序不能反！）\n    s->next = p->next;\n// 第二步：前驱再指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}","none":"\nStatus ListInsert(LinkList L, int i, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n    int j = 0;\n\n    while (p != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i - 1) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = p->next;\n    p->next = s;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 08  ListInsert\n *  作用：先找第 i-1 个结点 p，再造新结点 s，把 s 插到 p 的后面。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 ListInsert：中间插入、头部插入、越界插入三种情况 */\nint main(void)\n{\n    LinkList L = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n\n    printf(\"插入前：\");\n    printList(L);\n\n    ListInsert(L, 2, 15);\n    printf(\"在第 2 位插入 15：\");\n    printList(L);\n\n    ListInsert(L, 1, 5);\n    printf(\"在第 1 位插入 5 ：\");\n    printList(L);\n\n    printf(\"在第 99 位插入：%s\\n\", ListInsert(L, 99, 1) == OK ? \"成功（不符合预期）\" : \"失败（越界，符合预期）\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"插入前：10 -> 20 -> 30 -> NULL\n在第 2 位插入 15：10 -> 15 -> 20 -> 30 -> NULL\n在第 1 位插入 5 ：5 -> 10 -> 15 -> 20 -> 30 -> NULL\n在第 99 位插入：失败（越界，符合预期）"},"02-02-singly-09":{"modes":{"detail":"// ============ 删除的三步 ============\n// 目标：删除第 2 个结点（值为 15）\n//\n//  删除前：  L ─▶ [头] ─▶ [10] ─▶ [15] ─▶ [20] ─▶ NULL\n//                       p        q\n//\n//  摘链：    p->next = q->next    把 [20] 的地址交给 p\n//           L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                       p        q（已被绕开，但还占着内存）\n//\n//  释放：    free(q)             把结点还给系统，q 变野指针\n//\n// 顺序要点：一定要先把 q->data 存进 *e，再 free(q)；\n// 一旦 free 之后再去读 q->data 就是\"释放后使用\"，属于未定义行为。\n// 另外 free 之后建议把 q 置 NULL，防止后续误用。\n\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(LinkList L, int i, ElemType *e)\n{\n// p 从头结点起步，最终停在第 i-1 个结点\n    LNode *p = L;\n// q 是被摘下来的那个结点\n    LNode *q = NULL;\n// j 记录 p 的位置，头结点记为 0\n    int j = 0;\n\n// 走 i-1 步定位前驱\n// 这里用 p->next != NULL 作为条件：确保 p 后面确实有一个结点可以摘\n    while (p->next != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p->next 为空说明第 i 个结点不存在\n    if (p->next == NULL || j > i - 1) return ERROR;\n\n// 用 q 记住待删结点\n    q = p->next;\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = q->data;\n// 摘链：前驱直接跨过 q，指向 q 的后继\n    p->next = q->next;\n// 释放 q 占用的内存，防止内存泄漏\n    free(q);\n\n// 删除成功\n    return OK;\n}","short":"\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(LinkList L, int i, ElemType *e)\n{\n// p 从头结点起步，最终停在第 i-1 个结点\n    LNode *p = L;\n// q 是被摘下来的那个结点\n    LNode *q = NULL;\n// j 记录 p 的位置，头结点记为 0\n    int j = 0;\n\n// 走 i-1 步定位前驱\n    while (p->next != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p->next 为空说明第 i 个结点不存在\n    if (p->next == NULL || j > i - 1) return ERROR;\n\n// 用 q 记住待删结点\n    q = p->next;\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = q->data;\n// 摘链：前驱直接跨过 q，指向 q 的后继\n    p->next = q->next;\n// 释放 q 占用的内存，防止内存泄漏\n    free(q);\n\n// 删除成功\n    return OK;\n}","none":"\nStatus ListDelete(LinkList L, int i, ElemType *e)\n{\n    LNode *p = L;\n    LNode *q = NULL;\n    int j = 0;\n\n    while (p->next != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p->next == NULL || j > i - 1) return ERROR;\n\n    q = p->next;\n    *e = q->data;\n    p->next = q->next;\n    free(q);\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 09  ListDelete\n *  作用：先找第 i-1 个结点 p，摘下 p 后面的结点 q，取出数据后释放 q。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 ListDelete：删中间、删第一个、越界删除 */\nint main(void)\n{\n    LinkList L = NULL;\n    ElemType e = -1;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n\n    printf(\"删除前：\");\n    printList(L);\n\n    if (ListDelete(L, 2, &e) == OK) printf(\"删除第 2 个结点，值是 %d\\n\", e);\n    printf(\"删除后：\");\n    printList(L);\n\n    if (ListDelete(L, 1, &e) == OK) printf(\"删除第 1 个结点，值是 %d\\n\", e);\n    printf(\"再删除后：\");\n    printList(L);\n\n    printf(\"删除第 9 个结点：%s\\n\", ListDelete(L, 9, &e) == OK ? \"成功（不符合预期）\" : \"失败（越界，符合预期）\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"删除前：10 -> 20 -> 30 -> NULL\n删除第 2 个结点，值是 20\n删除后：10 -> 30 -> NULL\n删除第 1 个结点，值是 10\n再删除后：30 -> NULL\n删除第 9 个结点：失败（越界，符合预期）"},"02-02-singly-10":{"modes":{"detail":"// ============ 遍历的固定套路 ============\n//\n//   p = L->next;              ← 跳过不存数据的头结点\n//   while (p != NULL) {       ← 走到 NULL 说明到尾了\n//       ... 处理 p->data ...\n//       p = p->next;          ← 千万别忘了这一句，否则死循环\n//   }\n//\n// 输出格式 10 -> 20 -> 30 -> NULL，最后那个 NULL 是\"链尾\"的可视化，\n// 方便一眼看出链表在哪里结束。\n\n// 只读操作，不需要修改链表，参数直接传 LinkList（一级指针）\nvoid printList(LinkList L)\n{\n// 从第一个真实结点开始\n    LNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，\"->\" 是链表的视觉箭头\n        printf(\"%d -> \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}","short":"\n// 只读操作，不需要修改链表，参数直接传 LinkList（一级指针）\nvoid printList(LinkList L)\n{\n// 从第一个真实结点开始\n    LNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，\"->\" 是链表的视觉箭头\n        printf(\"%d -> \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}","none":"\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 10  printList\n *  作用：从头到尾走一遍，把每个结点的数据打出来，最后打一个 NULL 收尾。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 printList：空表要有明确提示，非空要能完整走完 */\nint main(void)\n{\n    LinkList L = NULL;\n\n    InitList(&L);\n\n    printf(\"空表输出：\");\n    printList(L);\n\n    applist(L, 10);\n    applist(L, 15);\n    applist(L, 20);\n    printf(\"三元素输出：\");\n    printList(L);\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"空表输出：(空表)\n三元素输出：10 -> 15 -> 20 -> NULL"},"02-02-singly-11":{"modes":{"detail":"// ============ 为什么必须先保存 next ============\n//\n//  错误写法：                正确写法：\n//    free(p);                  q = p;\n//    p = p->next;   ← 危险!    p = p->next;\n//                             free(q);\n//\n// free(p) 之后那块内存已经还给系统，p->next 读到的是垃圾值，\n// 用这个垃圾值当地址去访问就是\"释放后使用\"（use after free）。\n// 所以要先记住后继，再释放当前。\n\n// 参数是 LinkList *，因为最后要把调用者的 L 改成 NULL\n// 如果传一级指针，函数内把 L 置 NULL 传不回去，调用者手里就是野指针\nvoid freeList(LinkList *L)\n{\n// 从第一个真实结点开始清理\n    LNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    LNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n// 这三步顺序固定：存 → 走 → 放，缺一不可\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}","short":"\n// 参数是 LinkList *，因为最后要把调用者的 L 改成 NULL\nvoid freeList(LinkList *L)\n{\n// 从第一个真实结点开始清理\n    LNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    LNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}","none":"\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ====================================================================\n *  轮到你了：模块 11  freeList\n *  作用：逐个释放每个结点，最后把头结点也释放掉，并把指针置 NULL。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 freeList：释放完 L 必须变成 NULL，否则是野指针 */\nint main(void)\n{\n    LinkList L = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n\n    printf(\"释放前 L = %s\\n\", L != NULL ? \"非 NULL\" : \"NULL\");\n    freeList(&L);\n    printf(\"释放后 L = %s\\n\", L == NULL ? \"NULL（符合预期）\" : \"非 NULL（不符合预期）\");\n\n    return 0;\n}\n","expectedOutput":"释放前 L = 非 NULL\n释放后 L = NULL（符合预期）"},"02-02-singly-12":{"modes":{"detail":"// ============ 测试程序的编排思路 ============\n// 千万不要一次性把所有操作写完再运行，那样出错无从下手。\n// 正确做法是\"一步一打印\"：每做完一个操作就 printList，\n// 让输出自己告诉你哪一步开始不对。\n//\n// 另外一定要测三种边界：\n//   1. 空表      （初始化后立刻打印）\n//   2. 越界位置  （插入/查找/删除一个不存在的位置，应返回 ERROR）\n//   3. 空表释放  （防止 freeList 对空表崩溃）\n\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    LinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    LNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望输出：10 -> 20 -> 30 -> NULL\n    printf(\"追加后：\");\n    printList(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望输出：5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"头插后：\");\n    printList(L);\n\n// 再头插 3，亲眼看一次\"头插会颠倒顺序\"\n    HeadInsert(L, 3);\n// 期望输出：3 -> 5 -> 10 -> 20 -> 30 -> NULL\n// 头插法建出来的表天生是逆序的，所以\"头插法建表\"常被拿来倒序一个数组\n    printf(\"再头插 3 后：\");\n    printList(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 3 和 5 之间\n    ListInsert(L, 2, 15);\n// 期望输出：3 -> 15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"插入后：\");\n    printList(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n// 此时链表是 3 -> 15 -> 5 -> 10 -> 20 -> 30，第 3 个是 5\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：5\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就打印它的后继信息\n    if (p != NULL)\n    {\n// p->next 非空则说明 20 不是尾结点\n        printf(\"找到值为 20 的结点，它的后继是 %s\\n\", p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位删除 ==========\n    printf(\"\\n========== 7. 删除第 1 个结点 ==========\\n\");\n// 删除第 1 个结点，被删的值通过 e 带出来\n    if (ListDelete(L, 1, &e) == OK)\n    {\n// 期望输出：被删除的是 3\n        printf(\"已删除第 1 个结点，它的值是 %d\\n\", e);\n    }\n// 期望输出：15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"删除后：\");\n    printList(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：释放整表 ==========\n    printf(\"\\n========== 8. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}","short":"\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    LinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    LNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望输出：10 -> 20 -> 30 -> NULL\n    printf(\"追加后：\");\n    printList(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望输出：5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"头插后：\");\n    printList(L);\n\n// 再头插 3，亲眼看一次\"头插会颠倒顺序\"\n    HeadInsert(L, 3);\n// 期望输出：3 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"再头插 3 后：\");\n    printList(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 3 和 5 之间\n    ListInsert(L, 2, 15);\n// 期望输出：3 -> 15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"插入后：\");\n    printList(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：5\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就打印它的后继信息\n    if (p != NULL)\n    {\n// p->next 非空则说明 20 不是尾结点\n        printf(\"找到值为 20 的结点，它的后继是 %s\\n\", p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位删除 ==========\n    printf(\"\\n========== 7. 删除第 1 个结点 ==========\\n\");\n// 删除第 1 个结点，被删的值通过 e 带出来\n    if (ListDelete(L, 1, &e) == OK)\n    {\n// 期望输出：被删除的是 3\n        printf(\"已删除第 1 个结点，它的值是 %d\\n\", e);\n    }\n// 期望输出：15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"删除后：\");\n    printList(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：释放整表 ==========\n    printf(\"\\n========== 8. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}","none":"\nint main(void)\n{\n    LinkList L = NULL;\n    LNode *p = NULL;\n    ElemType e = 0;\n\n    printf(\"========== 1. 初始化 ==========\\n\");\n    if (InitList(&L) != OK)\n    {\n        printf(\"初始化失败！\\n\");\n        return ERROR;\n    }\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    printf(\"追加后：\");\n    printList(L);\n\n    printf(\"\\n========== 3. 头插法插入 ==========\\n\");\n    HeadInsert(L, 5);\n    printf(\"头插后：\");\n    printList(L);\n\n    HeadInsert(L, 3);\n    printf(\"再头插 3 后：\");\n    printList(L);\n\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n    ListInsert(L, 2, 15);\n    printf(\"插入后：\");\n    printList(L);\n\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n    p = LocateElem(L, 20);\n    if (p != NULL)\n    {\n        printf(\"找到值为 20 的结点，它的后继是 %s\\n\", p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n    if (LocateElem(L, 99) == NULL)\n    {\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 7. 删除第 1 个结点 ==========\\n\");\n    if (ListDelete(L, 1, &e) == OK)\n    {\n        printf(\"已删除第 1 个结点，它的值是 %d\\n\", e);\n    }\n    printf(\"删除后：\");\n    printList(L);\n\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 8. 释放内存 ==========\\n\");\n    freeList(&L);\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n    return 0;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 05 HeadInsert ======== */\n\nStatus HeadInsert(LinkList L, ElemType e)\n{\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = L->next;\n    L->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 06 GetElem_L ======== */\n\nStatus GetElem_L(LinkList L, int i, ElemType *e)\n{\n    LNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 07 LocateElem ======== */\n\nLNode *LocateElem(LinkList L, ElemType e)\n{\n    LNode *p = L->next;\n\n    while (p != NULL && p->data != e)\n    {\n        p = p->next;\n    }\n\n    return p;\n}\n\n/* ======== [已给出] 模块 08 ListInsert ======== */\n\nStatus ListInsert(LinkList L, int i, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n    int j = 0;\n\n    while (p != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i - 1) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = p->next;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 09 ListDelete ======== */\n\nStatus ListDelete(LinkList L, int i, ElemType *e)\n{\n    LNode *p = L;\n    LNode *q = NULL;\n    int j = 0;\n\n    while (p->next != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p->next == NULL || j > i - 1) return ERROR;\n\n    q = p->next;\n    *e = q->data;\n    p->next = q->next;\n    free(q);\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 printList ======== */\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 11 freeList ======== */\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 12  main\n *  作用：把 9 个基本操作串起来跑一遍，用输出验证每一步的结果是否符合预期。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n","expectedOutput":"========== 1. 初始化 ==========\n初始化完成，当前链表：(空表)\n\n========== 2. 尾部追加 10 20 30 ==========\n追加后：10 -> 20 -> 30 -> NULL\n\n========== 3. 头插法插入 ==========\n头插后：5 -> 10 -> 20 -> 30 -> NULL\n再头插 3 后：3 -> 5 -> 10 -> 20 -> 30 -> NULL\n\n========== 4. 在第 2 位插入 15 ==========\n插入后：3 -> 15 -> 5 -> 10 -> 20 -> 30 -> NULL\n在第 99 位插入 5 ：失败（越界，符合预期）\n\n========== 5. 按位查找 ==========\n第 3 个元素的值是 5\n查第 100 个元素失败（越界，符合预期）\n\n========== 6. 按值查找 ==========\n找到值为 20 的结点，它的后继是 一个有效结点\n查找值 99 返回 NULL（符合预期）\n\n========== 7. 删除第 1 个结点 ==========\n已删除第 1 个结点，它的值是 3\n删除后：15 -> 5 -> 10 -> 20 -> 30 -> NULL\n删除第 10 个结点：失败（越界，符合预期）\n\n========== 8. 释放内存 ==========\n释放完成，L = NULL"},"02-02-singly-13":{"modes":{"detail":"// ============ 为什么要 typedef ============\n// C 语言里 struct LNode 必须连在一起写，typedef 之后：\n//     LNode     等价于  struct LNode\n//     LinkList  等价于  struct LNode *\n// 从此代码里只出现 LNode / LinkList，可读性大幅提升。\n// 注意 LinkList 是\"指针类型\"，所以 LinkList L 本身就是一个指针变量。\n\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n// 约定：函数返回 Status，OK 表示成功，ERROR 表示失败。\n#define OK 1\n// #define 是预处理指令，编译前做文本替换：代码里 OK 直接变成 1\n#define ERROR 0\n// 布尔值宏，教材习惯写法\n#define TRUE 1\n#define FALSE 0\n// 内存分配失败的专用错误码，取一个和 OK/ERROR 都不冲突的负数\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\n// 把 int 换成 float / char，整份代码不用改，这就是 typedef 的价值\ntypedef int Status;\ntypedef int ElemType;\n\n// 结点结构体：数据域 data + 指针域 next\n// ============ 内存里长什么样 ============\n//\n//   头结点(不存数据)        第 1 个结点          第 2 个结点\n//   ┌────┬──────┐      ┌────┬──────┐      ┌────┬──────┐\n//   │ -- │ next │─────▶│ 10 │ next │─────▶│ 20 │ NULL │\n//   └────┴──────┘      └────┴──────┘      └────┴──────┘\n//      L                 L->next            带数据的第一颗\n//\n// 头结点是\"哨兵\"：它不存有效数据，只为让插入/删除不用特判第一个位置。\ntypedef struct LNode {\n// 数据域，存具体元素\n    ElemType data;\n// 指针域，指向下一个结点；最后一个结点为 NULL\n// struct LNode *next 不能写成 LNode *next —— 此时 LNode 这个名字还没生效\n    struct LNode *next;\n// 一次 typedef 出两个名字：LNode（结点）和 LinkList（指向结点的指针）\n} LNode, *LinkList;\n\n// ============ 这个函数是后面所有插入操作的地基 ============\n// 任何\"插入\"最终都要先造出一个新结点，思路永远是：\n//   申请内存 → 填数据 → 指针域置 NULL → 把地址交出去\n// 内存申请必须检查失败！malloc 失败返回 NULL，直接解引用会崩溃。\n\n// 返回新结点的地址，所以返回类型是 LNode *\n// 参数 e 按值传递：函数内改 e 不影响调用者，因为我们只需要它的值\nLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 LNode *\n// sizeof(LNode) 通常等于 8（int 4 字节 + 指针 4 字节）或 16（64 位下指针 8 字节）\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n// printf 打印中文提示，让使用者知道是内存不够，而不是程序莫名其妙退出\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 新结点的 next 必须先置 NULL，否则它是野指针\n// 这是最容易漏的一步：漏了之后链表尾部就不\"封口\"，遍历会冲到非法内存\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}\n\n// ============ 为什么需要\"头结点\" ============\n// 没有头结点时，在第一个位置插入/删除都要单独写一段代码；\n// 有了头结点，第一个位置和中间位置的操作逻辑完全一样，代码短一半。\n//\n//   初始化完成后：   L ──▶ ┌────┬──────┐\n//                          │ -- │ NULL │\n//                          └────┴──────┘\n\n// 参数是 LinkList *（二级指针），因为要修改调用者手里的 L 本身\n// 如果写成 LinkList L，函数内改了 L 也传不回去，调用者的 L 还是野指针\nStatus InitList(LinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (LinkList)malloc(sizeof(LNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}\n\n// ============ 指针推进过程 ============\n// 目标：在尾部追加 30\n//\n//  初始：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//          p\n//\n//  循环：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                              p        （p->next != NULL，继续走）\n//\n//  停止：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                                     p   （p->next == NULL，p 就是尾结点）\n//\n//  挂上：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n// 关键：p 从头结点起步，不是从 L->next 起步 —— 这样空表也能正确处理。\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\n// 返回值用 Status，让调用者知道追加成功与否\nStatus applist(LinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    LNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n// 循环条件是 p->next != NULL，看的永远是\"下一个\"，所以停下时 p 正是尾结点\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点，地址交给 s\n    s = creatNode(e);\n\n// 把新结点接到尾结点后面，链表就延长了一节\n// 只需要改一处指针；原来的尾结点 next 从 NULL 变成指向 s\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}\n\n// ============ 头插法 vs 尾插法 ============\n//\n//   applist（尾插）  ：从头一路走到尾，找到尾结点再挂上    —— 时间复杂度 O(n)\n//   HeadInsert（头插）：直接插在头结点后面，碰都不碰其他结点 —— 时间复杂度 O(1)\n//\n// 代价是头插会**颠倒顺序**：依次头插 10、20、30，得到的是 30 -> 20 -> 10。\n// 这不是缺陷，恰恰是头插法最常用的一个特性 —— \"头插法建表\"就是靠它做逆序的：\n//\n//     LinkList L;\n//     InitList(&L);\n//     for (i = 0; i < n; i++)\n//     {\n//         HeadInsert(L, a[i]);     /* 建出来的表正好是数组 a 的逆序 */\n//     }\n//\n// 再记住一个等价关系：HeadInsert(L, e) 和 ListInsert(L, 1, e) 干的是同一件事，\n// 前者只是把\"找第 0 个结点\"这一步省掉了 —— 因为头结点本身就是第 0 个。\n//\n// ============ 两步指针操作，顺序不能反 ============\n// 目标：把 5 头插进 10 -> 20 -> 30\n//\n//  插入前：   L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//\n//  第一步（新结点先接住原来的头一个）：s->next = L->next\n//            L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//                          ▲\n//            [5] ──────────┘\n//             s\n//\n//  第二步（头结点再指向新结点）：L->next = s\n//            L ─▶ [头] ─▶ [5] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//                          s\n//\n// 头结点 [头] 本身始终待在原地，头插法动的只是它的 next 指针。\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\n// 位置固定是第 1 位，所以不像 ListInsert 那样需要 i 这个参数\nStatus HeadInsert(LinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住原来头结点后面的那一串\n// 做完这一步，链表本身还是完整的，只是多了一个\"悬挂\"在外面的 s\n    s->next = L->next;\n// 第二步：头结点再指向新结点，s 正式成为第 1 个结点\n// 顺序同样不能反！先执行 L->next = s 的话，原来第一个结点的地址就丢了（内存泄漏）\n    L->next = s;\n\n// 插入成功\n    return OK;\n}\n\n// ============ 找第 3 个结点（i = 3）============\n//\n//   L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ [30] ─▶ NULL\n//          j=1     j=2     j=3\n//           p       p       p\n//\n//  循环结束时 j == i，p 正好停在第 i 个结点上。\n// 两个必须同时检查的失败情况：\n//   p == NULL  → i 比表长还大，数着数着掉出去了\n//   j > i      → i 小于 1（比如 i = 0），循环一次都没进\n\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\n// 这是 C 语言\"函数只能返回一个值\"的经典绕法：返回值给状态，数据走指针参数\nStatus GetElem_L(LinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n// 头结点不存数据，跳过它 —— 这是带头结点链表里最常见的起点写法\n    LNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n// p != NULL 写在前面，短路求值保证不会对空指针取 ->next\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}\n\n// ============ 按值查找和按位查找的区别 ============\n// 按位查找：数位置，返回\"值\"\n// 按值查找：比值，返回\"结点地址\"\n//\n// 为什么返回地址而不是值？因为拿回地址后，调用者还能继续访问\n// p->next 等后继信息，也可以在它前面做插入删除。\n//\n// 找不到时返回 NULL —— C 语言里表示\"没有这个结点\"的标准做法。\n\n// 返回 LNode *，找不到就是 NULL\nLNode *LocateElem(LinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    LNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n// 一旦 p->data == e，循环立刻停下，p 就停在第一个匹配的结点上\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}\n\n// ============ 插入的核心：两句指针赋值不能写反 ============\n// 目标：在第 2 个位置插入 15\n//\n//  插入前：  L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                       p\n//\n//  第一步（先接后面）：s->next = p->next\n//          [15] ─────▶ [20]\n//            s           ▲\n//                        └── 此时 p->next 还指着 [20]\n//\n//  第二步（再断前面）：p->next = s\n//          [10] ─▶ [15] ─▶ [20]\n//            p       s\n//\n// 如果两句写反，先执行 p->next = s，[20] 的地址就丢了（内存泄漏），\n// 而且 s->next 会指向 s 自己，链表出现环 —— 这是最经典的踩坑点。\n//\n// 还有一条铁律：先判断能不能插，再造结点。顺序颠倒会导致插入失败时\n// 白白多出一个没挂上链表的\"孤儿结点\"。\n\n// 参数 L 是头结点指针（不需要改 L 本身）；i 是位置，e 是数据\n// 位置 i 从 1 开始计数，表示\"插完以后它是第 i 个结点\"\nStatus ListInsert(LinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n// 这正是头结点的价值 —— 第 1 位和后面位置走同一段逻辑\n    LNode *p = L;\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n// 循环条件 j < i - 1：i = 1 时一次也不进循环，p 直接就是头结点\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n// 注意这里是先检查后造结点，避免产生孤儿结点\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住后面的那一串（顺序不能反！）\n    s->next = p->next;\n// 第二步：前驱再指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}\n\n// ============ 删除的三步 ============\n// 目标：删除第 2 个结点（值为 15）\n//\n//  删除前：  L ─▶ [头] ─▶ [10] ─▶ [15] ─▶ [20] ─▶ NULL\n//                       p        q\n//\n//  摘链：    p->next = q->next    把 [20] 的地址交给 p\n//           L ─▶ [头] ─▶ [10] ─▶ [20] ─▶ NULL\n//                       p        q（已被绕开，但还占着内存）\n//\n//  释放：    free(q)             把结点还给系统，q 变野指针\n//\n// 顺序要点：一定要先把 q->data 存进 *e，再 free(q)；\n// 一旦 free 之后再去读 q->data 就是\"释放后使用\"，属于未定义行为。\n// 另外 free 之后建议把 q 置 NULL，防止后续误用。\n\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(LinkList L, int i, ElemType *e)\n{\n// p 从头结点起步，最终停在第 i-1 个结点\n    LNode *p = L;\n// q 是被摘下来的那个结点\n    LNode *q = NULL;\n// j 记录 p 的位置，头结点记为 0\n    int j = 0;\n\n// 走 i-1 步定位前驱\n// 这里用 p->next != NULL 作为条件：确保 p 后面确实有一个结点可以摘\n    while (p->next != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p->next 为空说明第 i 个结点不存在\n    if (p->next == NULL || j > i - 1) return ERROR;\n\n// 用 q 记住待删结点\n    q = p->next;\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = q->data;\n// 摘链：前驱直接跨过 q，指向 q 的后继\n    p->next = q->next;\n// 释放 q 占用的内存，防止内存泄漏\n    free(q);\n\n// 删除成功\n    return OK;\n}\n\n// ============ 遍历的固定套路 ============\n//\n//   p = L->next;              ← 跳过不存数据的头结点\n//   while (p != NULL) {       ← 走到 NULL 说明到尾了\n//       ... 处理 p->data ...\n//       p = p->next;          ← 千万别忘了这一句，否则死循环\n//   }\n//\n// 输出格式 10 -> 20 -> 30 -> NULL，最后那个 NULL 是\"链尾\"的可视化，\n// 方便一眼看出链表在哪里结束。\n\n// 只读操作，不需要修改链表，参数直接传 LinkList（一级指针）\nvoid printList(LinkList L)\n{\n// 从第一个真实结点开始\n    LNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，\"->\" 是链表的视觉箭头\n        printf(\"%d -> \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}\n\n// ============ 为什么必须先保存 next ============\n//\n//  错误写法：                正确写法：\n//    free(p);                  q = p;\n//    p = p->next;   ← 危险!    p = p->next;\n//                             free(q);\n//\n// free(p) 之后那块内存已经还给系统，p->next 读到的是垃圾值，\n// 用这个垃圾值当地址去访问就是\"释放后使用\"（use after free）。\n// 所以要先记住后继，再释放当前。\n\n// 参数是 LinkList *，因为最后要把调用者的 L 改成 NULL\n// 如果传一级指针，函数内把 L 置 NULL 传不回去，调用者手里就是野指针\nvoid freeList(LinkList *L)\n{\n// 从第一个真实结点开始清理\n    LNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    LNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n// 这三步顺序固定：存 → 走 → 放，缺一不可\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}\n\n// ============ 测试程序的编排思路 ============\n// 千万不要一次性把所有操作写完再运行，那样出错无从下手。\n// 正确做法是\"一步一打印\"：每做完一个操作就 printList，\n// 让输出自己告诉你哪一步开始不对。\n//\n// 另外一定要测三种边界：\n//   1. 空表      （初始化后立刻打印）\n//   2. 越界位置  （插入/查找/删除一个不存在的位置，应返回 ERROR）\n//   3. 空表释放  （防止 freeList 对空表崩溃）\n\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    LinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    LNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望输出：10 -> 20 -> 30 -> NULL\n    printf(\"追加后：\");\n    printList(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望输出：5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"头插后：\");\n    printList(L);\n\n// 再头插 3，亲眼看一次\"头插会颠倒顺序\"\n    HeadInsert(L, 3);\n// 期望输出：3 -> 5 -> 10 -> 20 -> 30 -> NULL\n// 头插法建出来的表天生是逆序的，所以\"头插法建表\"常被拿来倒序一个数组\n    printf(\"再头插 3 后：\");\n    printList(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 3 和 5 之间\n    ListInsert(L, 2, 15);\n// 期望输出：3 -> 15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"插入后：\");\n    printList(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n// 此时链表是 3 -> 15 -> 5 -> 10 -> 20 -> 30，第 3 个是 5\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：5\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就打印它的后继信息\n    if (p != NULL)\n    {\n// p->next 非空则说明 20 不是尾结点\n        printf(\"找到值为 20 的结点，它的后继是 %s\\n\", p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位删除 ==========\n    printf(\"\\n========== 7. 删除第 1 个结点 ==========\\n\");\n// 删除第 1 个结点，被删的值通过 e 带出来\n    if (ListDelete(L, 1, &e) == OK)\n    {\n// 期望输出：被删除的是 3\n        printf(\"已删除第 1 个结点，它的值是 %d\\n\", e);\n    }\n// 期望输出：15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"删除后：\");\n    printList(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：释放整表 ==========\n    printf(\"\\n========== 8. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}\n","short":"\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n#define OK 1\n#define ERROR 0\n// 布尔值宏，教材习惯写法\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\ntypedef int Status;\ntypedef int ElemType;\n\n// 结点结构体：数据域 data + 指针域 next\ntypedef struct LNode {\n// 数据域，存具体元素\n    ElemType data;\n// 指针域，指向下一个结点；最后一个结点为 NULL\n    struct LNode *next;\n// 一次 typedef 出两个名字：LNode（结点）和 LinkList（指向结点的指针）\n} LNode, *LinkList;\n\n\n// 返回新结点的地址，所以返回类型是 LNode *\nLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 LNode *\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 新结点的 next 必须先置 NULL，否则它是野指针\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}\n\n\n// 参数是 LinkList *（二级指针），因为要修改调用者手里的 L 本身\nStatus InitList(LinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (LinkList)malloc(sizeof(LNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}\n\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus applist(LinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    LNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点，地址交给 s\n    s = creatNode(e);\n\n// 把新结点接到尾结点后面，链表就延长了一节\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}\n\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus HeadInsert(LinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住原来头结点后面的那一串\n    s->next = L->next;\n// 第二步：头结点再指向新结点，s 正式成为第 1 个结点\n    L->next = s;\n\n// 插入成功\n    return OK;\n}\n\n\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\nStatus GetElem_L(LinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n    LNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}\n\n\n// 返回 LNode *，找不到就是 NULL\nLNode *LocateElem(LinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    LNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}\n\n\n// 参数 L 是头结点指针（不需要改 L 本身）；i 是位置，e 是数据\nStatus ListInsert(LinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n    LNode *p = L;\n// s 是等待挂链的新结点\n    LNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点先接住后面的那一串（顺序不能反！）\n    s->next = p->next;\n// 第二步：前驱再指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}\n\n\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(LinkList L, int i, ElemType *e)\n{\n// p 从头结点起步，最终停在第 i-1 个结点\n    LNode *p = L;\n// q 是被摘下来的那个结点\n    LNode *q = NULL;\n// j 记录 p 的位置，头结点记为 0\n    int j = 0;\n\n// 走 i-1 步定位前驱\n    while (p->next != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p->next 为空说明第 i 个结点不存在\n    if (p->next == NULL || j > i - 1) return ERROR;\n\n// 用 q 记住待删结点\n    q = p->next;\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = q->data;\n// 摘链：前驱直接跨过 q，指向 q 的后继\n    p->next = q->next;\n// 释放 q 占用的内存，防止内存泄漏\n    free(q);\n\n// 删除成功\n    return OK;\n}\n\n\n// 只读操作，不需要修改链表，参数直接传 LinkList（一级指针）\nvoid printList(LinkList L)\n{\n// 从第一个真实结点开始\n    LNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，\"->\" 是链表的视觉箭头\n        printf(\"%d -> \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}\n\n\n// 参数是 LinkList *，因为最后要把调用者的 L 改成 NULL\nvoid freeList(LinkList *L)\n{\n// 从第一个真实结点开始清理\n    LNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    LNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}\n\n\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    LinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    LNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望输出：10 -> 20 -> 30 -> NULL\n    printf(\"追加后：\");\n    printList(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望输出：5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"头插后：\");\n    printList(L);\n\n// 再头插 3，亲眼看一次\"头插会颠倒顺序\"\n    HeadInsert(L, 3);\n// 期望输出：3 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"再头插 3 后：\");\n    printList(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 3 和 5 之间\n    ListInsert(L, 2, 15);\n// 期望输出：3 -> 15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"插入后：\");\n    printList(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：5\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就打印它的后继信息\n    if (p != NULL)\n    {\n// p->next 非空则说明 20 不是尾结点\n        printf(\"找到值为 20 的结点，它的后继是 %s\\n\", p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位删除 ==========\n    printf(\"\\n========== 7. 删除第 1 个结点 ==========\\n\");\n// 删除第 1 个结点，被删的值通过 e 带出来\n    if (ListDelete(L, 1, &e) == OK)\n    {\n// 期望输出：被删除的是 3\n        printf(\"已删除第 1 个结点，它的值是 %d\\n\", e);\n    }\n// 期望输出：15 -> 5 -> 10 -> 20 -> 30 -> NULL\n    printf(\"删除后：\");\n    printList(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：释放整表 ==========\n    printf(\"\\n========== 8. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}\n","none":"\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct LNode {\n    ElemType data;\n    struct LNode *next;\n} LNode, *LinkList;\n\n\nLNode *creatNode(ElemType e)\n{\n    LNode *p = (LNode *)malloc(sizeof(LNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->next = NULL;\n\n    return p;\n}\n\n\nStatus InitList(LinkList *L)\n{\n    *L = (LinkList)malloc(sizeof(LNode));\n    if (*L == NULL) return ERROR;\n    (*L)->next = NULL;\n    return OK;\n}\n\n\nStatus applist(LinkList L, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    p->next = s;\n\n    return OK;\n}\n\n\nStatus HeadInsert(LinkList L, ElemType e)\n{\n    LNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = L->next;\n    L->next = s;\n\n    return OK;\n}\n\n\nStatus GetElem_L(LinkList L, int i, ElemType *e)\n{\n    LNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    return OK;\n}\n\n\nLNode *LocateElem(LinkList L, ElemType e)\n{\n    LNode *p = L->next;\n\n    while (p != NULL && p->data != e)\n    {\n        p = p->next;\n    }\n\n    return p;\n}\n\n\nStatus ListInsert(LinkList L, int i, ElemType e)\n{\n    LNode *p = L;\n    LNode *s = NULL;\n    int j = 0;\n\n    while (p != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i - 1) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = p->next;\n    p->next = s;\n\n    return OK;\n}\n\n\nStatus ListDelete(LinkList L, int i, ElemType *e)\n{\n    LNode *p = L;\n    LNode *q = NULL;\n    int j = 0;\n\n    while (p->next != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p->next == NULL || j > i - 1) return ERROR;\n\n    q = p->next;\n    *e = q->data;\n    p->next = q->next;\n    free(q);\n\n    return OK;\n}\n\n\nvoid printList(LinkList L)\n{\n    LNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d -> \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n\nvoid freeList(LinkList *L)\n{\n    LNode *p = (*L)->next;\n    LNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n\nint main(void)\n{\n    LinkList L = NULL;\n    LNode *p = NULL;\n    ElemType e = 0;\n\n    printf(\"========== 1. 初始化 ==========\\n\");\n    if (InitList(&L) != OK)\n    {\n        printf(\"初始化失败！\\n\");\n        return ERROR;\n    }\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    printf(\"追加后：\");\n    printList(L);\n\n    printf(\"\\n========== 3. 头插法插入 ==========\\n\");\n    HeadInsert(L, 5);\n    printf(\"头插后：\");\n    printList(L);\n\n    HeadInsert(L, 3);\n    printf(\"再头插 3 后：\");\n    printList(L);\n\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n    ListInsert(L, 2, 15);\n    printf(\"插入后：\");\n    printList(L);\n\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n    p = LocateElem(L, 20);\n    if (p != NULL)\n    {\n        printf(\"找到值为 20 的结点，它的后继是 %s\\n\", p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n    if (LocateElem(L, 99) == NULL)\n    {\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 7. 删除第 1 个结点 ==========\\n\");\n    if (ListDelete(L, 1, &e) == OK)\n    {\n        printf(\"已删除第 1 个结点，它的值是 %d\\n\", e);\n    }\n    printf(\"删除后：\");\n    printList(L);\n\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 8. 释放内存 ==========\\n\");\n    freeList(&L);\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n    return 0;\n}\n"},"scaffold":null,"expectedOutput":"========== 1. 初始化 ==========\n初始化完成，当前链表：(空表)\n\n========== 2. 尾部追加 10 20 30 ==========\n追加后：10 -> 20 -> 30 -> NULL\n\n========== 3. 头插法插入 ==========\n头插后：5 -> 10 -> 20 -> 30 -> NULL\n再头插 3 后：3 -> 5 -> 10 -> 20 -> 30 -> NULL\n\n========== 4. 在第 2 位插入 15 ==========\n插入后：3 -> 15 -> 5 -> 10 -> 20 -> 30 -> NULL\n在第 99 位插入 5 ：失败（越界，符合预期）\n\n========== 5. 按位查找 ==========\n第 3 个元素的值是 5\n查第 100 个元素失败（越界，符合预期）\n\n========== 6. 按值查找 ==========\n找到值为 20 的结点，它的后继是 一个有效结点\n查找值 99 返回 NULL（符合预期）\n\n========== 7. 删除第 1 个结点 ==========\n已删除第 1 个结点，它的值是 3\n删除后：15 -> 5 -> 10 -> 20 -> 30 -> NULL\n删除第 10 个结点：失败（越界，符合预期）\n\n========== 8. 释放内存 ==========\n释放完成，L = NULL"}},"doubly":{"02-02-doubly-01":{"modes":{"detail":"// ============ 相比单链表，结构体里多了什么 ============\n//\n//   单链表：  typedef struct LNode  { ElemType data; struct LNode  *next;  } LNode,  *LinkList;\n//   双向链表：typedef struct DuLNode { ElemType data; struct DuLNode *prior;\n//                                                     struct DuLNode *next;  } DuLNode, *DuLinkList;\n//\n// 就多了一个 prior。命名上习惯用 Du 前缀（double）把两者区分开 ——\n// 一份代码里同时出现单链表和双向链表时，这个前缀能救命。\n//\n// ============ 内存里长什么样 ============\n//\n//   [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//    L\n//\n// 屏幕上每个 ⇄ 在内存里其实是**两根方向相反**的指针。\n// 把每个结点的两个指针摊开列出来：\n//\n//   [头]：prior = NULL、next = [10]\n//   [10]：prior = [头]、next = [20]\n//   [20]：prior = [10]、next = [30]\n//   [30]：prior = [20]、next = NULL\n//\n// 只有头结点的 prior 是 NULL（它前面没有结点了），\n// 只有尾结点的 next 是 NULL（它后面没有结点了）。\n// 这两条性质后面写插入、删除时会反复用到 —— 尤其是\"要不要判空\"。\n\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n// 约定：函数返回 Status，OK 表示成功，ERROR 表示失败\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n// 内存分配失败的专用错误码，取一个和 OK/ERROR 都不冲突的负数\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\ntypedef int Status;\ntypedef int ElemType;\n\n// 双向结点：数据域 + 前驱指针 + 后继指针\n// 两个指针的分工：\n//   prior  指向前一个结点，头结点和\"第一个数据结点\"的 prior 关系要注意\n//   next   指向后一个结点，最后一个结点的 next 是 NULL\n//\n// 有一条贯穿全书的等式，插入删除时全靠它自检：\n//\n//     p->next->prior == p        以及        p->prior->next == p\n//\n// 也就是\"我指向谁，谁就得指回我\"。写双向链表的代码时，每改一次指针\n// 都拿这句话对一遍，基本不会错。\ntypedef struct DuLNode {\n// 数据域，存具体元素\n    ElemType data;\n// 前驱指针：指向上一个结点（这是单链表没有的）\n    struct DuLNode *prior;\n// 后继指针：指向下一个结点，尾结点为 NULL\n    struct DuLNode *next;\n// 一次 typedef 出两个名字：DuLNode（结点）和 DuLinkList（指向结点的指针）\n} DuLNode, *DuLinkList;","short":"\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\ntypedef int Status;\ntypedef int ElemType;\n\n// 双向结点：数据域 + 前驱指针 + 后继指针\ntypedef struct DuLNode {\n// 数据域，存具体元素\n    ElemType data;\n// 前驱指针：指向上一个结点（这是单链表没有的）\n    struct DuLNode *prior;\n// 后继指针：指向下一个结点，尾结点为 NULL\n    struct DuLNode *next;\n// 一次 typedef 出两个名字：DuLNode（结点）和 DuLinkList（指向结点的指针）\n} DuLNode, *DuLinkList;","none":"\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;"},"scaffold":"/* ====================================================================\n *  轮到你了：模块 01  头文件与 typedef\n *  作用：引入标准库，把双向结点 struct DuLNode 包装成 DuLNode / DuLinkList。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 只验证 typedef 是否可用：能声明变量、能访问两个指针就算过关 */\nint main(void)\n{\n    DuLinkList L = NULL;\n    DuLNode node;\n\n    node.data = 42;\n    node.prior = NULL;\n    node.next = NULL;\n\n    L = &node;\n\n    printf(\"结构体定义正确：L->data = %d，prior = %s，next = %s\\n\",\n           L->data,\n           L->prior == NULL ? \"NULL\" : \"非 NULL\",\n           L->next == NULL ? \"NULL\" : \"非 NULL\");\n    printf(\"sizeof(DuLNode) = %d 字节（比单链表结点多一个指针）\\n\", (int)sizeof(DuLNode));\n\n    return 0;\n}\n","expectedOutput":"结构体定义正确：L->data = 42，prior = NULL，next = NULL\nsizeof(DuLNode) = 24 字节（比单链表结点多一个指针）"},"02-02-doubly-02":{"modes":{"detail":"// ============ 比单链表多置一个指针 ============\n// 单链表的新结点只需要 p->next = NULL；\n// 双向链表必须**两个方向都置 NULL**，否则 prior 就是野指针，\n// 后面一旦顺着 prior 往回走（比如反向遍历）就会访问到非法内存。\n//\n// 这个函数只负责\"造出一个孤立的结点\"，它还没有被挂到任何链表上，\n// 所以两个指针都应该是干净的 NULL。\n\n// 返回新结点的地址，所以返回类型是 DuLNode *\nDuLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 DuLNode *\n// 双向结点比单链表结点大一个指针：32 位下 12 字节，64 位下 24 字节\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n// 打印中文提示，让使用者知道是内存不够，而不是程序莫名其妙退出\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 前驱置 NULL（单链表没有这一步）\n    p->prior = NULL;\n// 后继置 NULL\n// 两个方向都干净了，结点才算\"造好但还没上链\"\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}","short":"\n// 返回新结点的地址，所以返回类型是 DuLNode *\nDuLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 DuLNode *\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 前驱置 NULL（单链表没有这一步）\n    p->prior = NULL;\n// 后继置 NULL\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}","none":"\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ====================================================================\n *  轮到你了：模块 02  creatNode\n *  作用：申请一块 DuLNode 大小的内存，填好数据域，并把两个指针都置 NULL。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 creatNode：两个指针是不是都干净地置成了 NULL */\nint main(void)\n{\n    DuLNode *p = creatNode(7);\n\n    printf(\"creatNode 造出的结点：data = %d，prior = %s，next = %s\\n\",\n           p->data,\n           p->prior == NULL ? \"NULL\" : \"非 NULL\",\n           p->next == NULL ? \"NULL\" : \"非 NULL\");\n    printf(\"（两个指针都要是 NULL，漏了 prior 后面反向走就会踩野指针）\\n\");\n\n    free(p);\n    printf(\"结点已释放。\\n\");\n\n    return 0;\n}\n","expectedOutput":"creatNode 造出的结点：data = 7，prior = NULL，next = NULL\n（两个指针都要是 NULL，漏了 prior 后面反向走就会踩野指针）\n结点已释放。"},"02-02-doubly-03":{"modes":{"detail":"// ============ 头结点在双向链表里的特殊性 ============\n// 头结点是\"哨兵\"：不存有效数据，只为让插入/删除不用特判第一个位置。\n// 在双向链表里它还有两条铁律：\n//\n//   1. 头结点的 prior 永远是 NULL —— 它前面没有结点了；\n//   2. 头结点的 next 指向第一个数据结点（空表时是 NULL）。\n//\n// 空表的样子：\n//\n//        ┌────┬──────┬──────┐\n//        │NULL│  --  │ NULL │\n//        └────┴──────┴──────┘\n//          L\n//\n// 因为头结点的 prior 是 NULL，\"第一个数据结点的 prior 指向头结点\"这条关系\n// 才和中间结点完全一致 —— 这就是后面删除时 p->prior 不用判空的原因。\n\n// 参数是 DuLinkList *（二级指针），因为要修改调用者手里的 L 本身\nStatus InitList(DuLinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 头结点的 prior 恒为 NULL，写出来是为了明确这条不变式\n    (*L)->prior = NULL;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}","short":"\n// 参数是 DuLinkList *（二级指针），因为要修改调用者手里的 L 本身\nStatus InitList(DuLinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 头结点的 prior 恒为 NULL，写出来是为了明确这条不变式\n    (*L)->prior = NULL;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}","none":"\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 12 printListReverse ======== */\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 03  InitList\n *  作用：建立只有头结点的空双向链表：申请头结点，它的两个指针都是 NULL。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 InitList：初始化后是空表，而且头结点的 prior 必须是 NULL */\nint main(void)\n{\n    DuLinkList L = NULL;\n\n    if (InitList(&L) != OK)\n    {\n        printf(\"初始化失败！\\n\");\n        return ERROR;\n    }\n\n    printf(\"初始化成功，L = %s\\n\", L != NULL ? \"有效头结点\" : \"NULL\");\n    printf(\"头结点的 prior = %s（应为 NULL）\\n\", L->prior == NULL ? \"NULL\" : \"非 NULL\");\n    printf(\"当前链表（正向）：\");\n    printList(L);\n    printf(\"当前链表（反向）：\");\n    printListReverse(L);\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"初始化成功，L = 有效头结点\n头结点的 prior = NULL（应为 NULL）\n当前链表（正向）：(空表)\n当前链表（反向）：(空表)"},"02-02-doubly-04":{"modes":{"detail":"// ============ 双向链表比单链表多改一根指针 ============\n// 单链表尾插只有一步：p->next = s;\n// 双向链表要做两步，而且两步都要做：\n//\n//     s->prior = p;      ← 新结点回指旧的尾结点\n//     p->next  = s;      ← 旧尾结点指向新结点\n//\n// 少了第一句，整条链表从后往前就断了：正向遍历看不出来，一旦反向遍历\n// （或者删除尾结点时用 p->prior）就会踩到野指针。\n//\n// ============ 指针推进过程 ============\n// 目标：在尾部追加 30\n//\n//  初始：  L ─▶ [头] ⇄ [10] ⇄ [20]\n//          p\n//\n//  循环：  L ─▶ [头] ⇄ [10] ⇄ [20]\n//                              p        （p->next == NULL，p 就是尾结点）\n//\n//  挂链：  L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n// 注意 [20] 和 [30] 之间是**双向**的箭头，两根指针缺一不可。\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus applist(DuLinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    DuLNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n// 循环条件是 p->next != NULL，看的永远是\"下一个\"，所以停下时 p 正是尾结点\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点（此时它的 prior 和 next 都是 NULL）\n    s = creatNode(e);\n\n// 第一步：新结点的 prior 回指旧的尾结点\n// 这一句是双向链表特有的，单链表尾插没有\n    s->prior = p;\n// 第二步：旧尾结点的 next 指向新结点\n// 两句都做完，[p] ⇄ [s] 这条双向关系才成立\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}","short":"\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus applist(DuLinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    DuLNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点（此时它的 prior 和 next 都是 NULL）\n    s = creatNode(e);\n\n// 第一步：新结点的 prior 回指旧的尾结点\n    s->prior = p;\n// 第二步：旧尾结点的 next 指向新结点\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}","none":"\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 12 printListReverse ======== */\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 04  applist\n *  作用：顺着 next 走到最后一个结点，把新结点接到它后面，并补上 prior 方向。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 applist：尾插之后正反两个方向都必须是对的 */\nint main(void)\n{\n    DuLinkList L = NULL;\n    int i;\n\n    InitList(&L);\n\n    printf(\"空表正向：\");\n    printList(L);\n\n    for (i = 1; i <= 3; i++)\n    {\n        applist(L, i * 10);\n    }\n\n    printf(\"尾插 10 20 30 后（正向）：\");\n    printList(L);\n    printf(\"尾插 10 20 30 后（反向）：\");\n    printListReverse(L);\n    printf(\"（反向能打对，说明尾插时 s->prior 那一句没漏）\\n\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"空表正向：(空表)\n尾插 10 20 30 后（正向）：10 ⇄ 20 ⇄ 30 ⇄ NULL\n尾插 10 20 30 后（反向）：30 ⇄ 20 ⇄ 10 ⇄ NULL\n（反向能打对，说明尾插时 s->prior 那一句没漏）"},"02-02-doubly-05":{"modes":{"detail":"// ============ 头插法的特点 ============\n//\n//   applist（尾插）  ：从头一路走到尾             —— 时间复杂度 O(n)\n//   HeadInsert（头插）：直接插在头结点后面        —— 时间复杂度 O(1)\n//\n// 代价是头插会**颠倒顺序**：依次头插 10、20、30，得到的是 30 ⇄ 20 ⇄ 10。\n// 这不是缺陷 —— \"头插法建表\"正是靠它做逆序的。\n//\n// 再记住一个等价关系：HeadInsert(L, e) 和 ListInsert(L, 1, e) 干的是同一件事。\n//\n// ============ 双向链表的头插为什么必须判空表 ============\n// 单链表头插只有两句，不用管表是不是空的：\n//\n//     s->next = L->next;\n//     L->next = s;\n//\n// 双向链表要处理的指针多一根，而且有一句**在空表时是非法操作**：\n//\n//     s->next = L->next;\n//     if (s->next != NULL)  s->next->prior = s;    ← 必须判空！\n//     s->prior = L;\n//     L->next = s;\n//\n// 空表时 L->next 是 NULL，如果不判空就去写 s->next->prior，\n// 等于对空指针取成员 —— 程序当场崩溃。这是双向链表头插最经典的坑。\n//\n// ============ 插入前后 ============\n// 把 5 头插进 10 ⇄ 20\n//\n//  插入前：  L ─▶ [头] ⇄ [10] ⇄ [20]\n//\n//  四步做完：\n//           L ─▶ [头] ⇄ [5] ⇄ [10] ⇄ [20]\n//\n// 其中 [5] 与 [头]、[5] 与 [10] 之间都要是双向的。\n\n// 参数 L 是头结点指针，不需要改 L 本身\n// 位置固定在第 1 位，所以不需要 i 这个参数\nStatus HeadInsert(DuLinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点的 next 接住原来头结点后面的那一串\n    s->next = L->next;\n// 第二步：如果后面确实有结点，让它的 prior 改指新结点\n// 这一步必须判空：空表时 L->next 是 NULL，s->next->prior 就是空指针取成员\n    if (s->next != NULL)\n    {\n        s->next->prior = s;\n    }\n// 第三步：新结点的 prior 指向头结点\n// 头结点一定存在，所以这一句不需要判空\n    s->prior = L;\n// 第四步：头结点指向新结点，s 正式成为第 1 个结点\n    L->next = s;\n\n// 插入成功\n    return OK;\n}","short":"\n// 参数 L 是头结点指针，不需要改 L 本身\nStatus HeadInsert(DuLinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点的 next 接住原来头结点后面的那一串\n    s->next = L->next;\n// 第二步：如果后面确实有结点，让它的 prior 改指新结点\n    if (s->next != NULL)\n    {\n        s->next->prior = s;\n    }\n// 第三步：新结点的 prior 指向头结点\n    s->prior = L;\n// 第四步：头结点指向新结点，s 正式成为第 1 个结点\n    L->next = s;\n\n// 插入成功\n    return OK;\n}","none":"\nStatus HeadInsert(DuLinkList L, ElemType e)\n{\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = L->next;\n    if (s->next != NULL)\n    {\n        s->next->prior = s;\n    }\n    s->prior = L;\n    L->next = s;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 12 printListReverse ======== */\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 05  HeadInsert\n *  作用：把新结点挂到头结点后面成为第一个结点；双向链表要特判空表。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 HeadInsert：先拿空表开刀 —— 这是双向头插最容易崩的路径 */\nint main(void)\n{\n    DuLinkList L = NULL;\n\n    InitList(&L);\n\n    HeadInsert(L, 5);\n    printf(\"空表头插 5 后（正向）：\");\n    printList(L);\n    printf(\"空表头插 5 后（反向）：\");\n    printListReverse(L);\n    printf(\"（空表时 L->next 是 NULL，不判空就去写 s->next->prior 会直接崩）\\n\");\n\n    HeadInsert(L, 1);\n    printf(\"再头插 1 后（正向）：\");\n    printList(L);\n    printf(\"再头插 1 后（反向）：\");\n    printListReverse(L);\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"空表头插 5 后（正向）：5 ⇄ NULL\n空表头插 5 后（反向）：5 ⇄ NULL\n（空表时 L->next 是 NULL，不判空就去写 s->next->prior 会直接崩）\n再头插 1 后（正向）：1 ⇄ 5 ⇄ NULL\n再头插 1 后（反向）：5 ⇄ 1 ⇄ NULL"},"02-02-doubly-06":{"modes":{"detail":"// ============ 这段代码和单链表完全一样 ============\n// 按位查找只需要沿 next 往后走，\"双向\"在这里帮不上忙\n// —— 除非你想从尾部倒着数，但那需要先知道表长，通常不划算。\n// （进阶：有的实现会额外记录 length，那样 i 大于一半时可以从尾结点\n//   顺着 prior 往前找，代价是每次插入删除都要维护 length。本教材从简。）\n//\n// ============ 找第 3 个结点（i = 3）============\n//\n//   L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//          j=1     j=2     j=3\n//           p       p       p\n//\n// 两个必须同时检查的失败情况：\n//   p == NULL  → i 比表长还大，数着数着掉出去了\n//   j > i      → i 小于 1（比如 i = 0），循环一次都没进\n\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\nStatus GetElem_L(DuLinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n// p != NULL 写在前面，短路求值保证不会对空指针取 ->next\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}","short":"\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\nStatus GetElem_L(DuLinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}","none":"\nStatus GetElem_L(DuLinkList L, int i, ElemType *e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 06  GetElem_L\n *  作用：从头结点的下一个出发数到第 i 个结点，把它的数据域取出来。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 GetElem_L：正常位、边界位、越界位都要试 */\nint main(void)\n{\n    DuLinkList L = NULL;\n    ElemType e = -1;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n\n    printf(\"当前链表：\");\n    printList(L);\n\n    if (GetElem_L(L, 1, &e) == OK) printf(\"第 1 个元素 = %d\\n\", e);\n    if (GetElem_L(L, 3, &e) == OK) printf(\"第 3 个元素 = %d\\n\", e);\n    if (GetElem_L(L, 4, &e) == ERROR) printf(\"第 4 个元素：越界，返回 ERROR（符合预期）\\n\");\n    if (GetElem_L(L, 0, &e) == ERROR) printf(\"第 0 个元素：非法位置，返回 ERROR（符合预期）\\n\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"当前链表：10 ⇄ 20 ⇄ 30 ⇄ NULL\n第 1 个元素 = 10\n第 3 个元素 = 30\n第 4 个元素：越界，返回 ERROR（符合预期）\n第 0 个元素：非法位置，返回 ERROR（符合预期）"},"02-02-doubly-07":{"modes":{"detail":"// ============ 按值查找返回的是\"结点\"不是\"值\" ============\n// 拿回结点地址后，调用者还能继续访问它的 prior / next，\n// 也能在它前后做插入删除 —— 这比只拿回一个数据值有用得多。\n//\n// 找不到时返回 NULL，这是 C 语言表示\"没有这个结点\"的标准做法。\n//\n// 双向链表在这里同样没有优势（按值查找本身就是逐个比对），\n// 除非链表是按别的规则组织过的。\n\n// 返回 DuLNode *，找不到就是 NULL\nDuLNode *LocateElem(DuLinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    DuLNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n// 一旦 p->data == e，循环立刻停下，p 就停在第一个匹配的结点上\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}","short":"\n// 返回 DuLNode *，找不到就是 NULL\nDuLNode *LocateElem(DuLinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    DuLNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}","none":"\nDuLNode *LocateElem(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L->next;\n\n    while (p != NULL && p->data != e)\n    {\n        p = p->next;\n    }\n\n    return p;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 07  LocateElem\n *  作用：从头到尾逐个比较数据域，返回第一个值相等的结点的地址。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 LocateElem：拿到结点后，前驱和后继都要能说对 */\nint main(void)\n{\n    DuLinkList L = NULL;\n    DuLNode *p = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n\n    printf(\"当前链表：\");\n    printList(L);\n\n    p = LocateElem(L, 20);\n    if (p != NULL)\n    {\n        printf(\"找到 20：前驱是 %d，后继是 %s\\n\",\n               p->prior->data,\n               p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n\n    p = LocateElem(L, 10);\n    if (p != NULL)\n    {\n        printf(\"找到 10：它的前驱是头结点（data 是 %d，属于垃圾值，不该读）\\n\", p->prior->data);\n    }\n\n    p = LocateElem(L, 99);\n    printf(\"查找 99 的结果：%s\\n\", p == NULL ? \"NULL（符合预期）\" : \"非 NULL（不符合预期）\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"当前链表：10 ⇄ 20 ⇄ 30 ⇄ NULL\n找到 20：前驱是 10，后继是 一个有效结点\n找到 10：它的前驱是头结点（data 是 -202547232，属于垃圾值，不该读）\n查找 99 的结果：NULL（符合预期）"},"02-02-doubly-08":{"modes":{"detail":"// ============ 改和查是一对 ============\n// 把 ModifyElem 和 GetElem_L 摆在一起看，会发现它们**只差最后一句**：\n//\n//     GetElem_L ：  *e = p->data;     把结点里的值读出来\n//     ModifyElem ：  p->data = e;     把新值写进结点\n//\n// 前面\"走 i-1 步定位\"的循环、以及越界检查，两者一模一样。\n// 这也说明一件事：**定位是链表操作里最通用的部分**，\n// 插入、删除、查找、修改都要先定位，区别只在定位到第几个结点、\n// 以及定位之后动几根指针。\n//\n// ============ 修改只动数据域，不动指针 ============\n// 这一点很重要：改一个结点的值不会改变链表的形状，\n// 所以不需要碰 prior / next，也就不会有双向链表那些\"漏改一根指针\"的风险。\n//\n// 代价是修改**不能改变链表的顺序**。如果你要的是\"把某个值挪到前面\"，\n// 那得先删除再插入，属于删+插的组合操作。\n\n// 要改的目标值通过 e 传进来（按值传递就够了，不需要指针）\nStatus ModifyElem(DuLinkList L, int i, ElemType e)\n{\n// 从第 1 个\"带数据的结点\"开始\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步\n    int j = 1;\n\n// 定位到第 i 个结点（与 GetElem_L 的循环完全相同）\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：第 i 个结点不存在就报错\n    if (p == NULL || j > i) return ERROR;\n\n// 把新值写进数据域 —— 只改数据，不碰任何指针\n    p->data = e;\n\n// 修改成功\n    return OK;\n}","short":"\n// 要改的目标值通过 e 传进来（按值传递就够了，不需要指针）\nStatus ModifyElem(DuLinkList L, int i, ElemType e)\n{\n// 从第 1 个\"带数据的结点\"开始\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步\n    int j = 1;\n\n// 定位到第 i 个结点（与 GetElem_L 的循环完全相同）\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：第 i 个结点不存在就报错\n    if (p == NULL || j > i) return ERROR;\n\n// 把新值写进数据域 —— 只改数据，不碰任何指针\n    p->data = e;\n\n// 修改成功\n    return OK;\n}","none":"\nStatus ModifyElem(DuLinkList L, int i, ElemType e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    p->data = e;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 12 printListReverse ======== */\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 08  ModifyElem\n *  作用：定位到第 i 个结点，把它的数据域改成新值 —— 也就是\"查改\"里的\"改\"。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 ModifyElem：改完值要对，而且链的形状不能变（正反都得一样） */\nint main(void)\n{\n    DuLinkList L = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n\n    printf(\"修改前（正向）：\");\n    printList(L);\n\n    if (ModifyElem(L, 2, 99) == OK)\n    {\n        printf(\"把第 2 个元素改成 99 后（正向）：\");\n        printList(L);\n        printf(\"把第 2 个元素改成 99 后（反向）：\");\n        printListReverse(L);\n    }\n    printf(\"（改值只动 data，指针一根都不该变）\\n\");\n\n    if (ModifyElem(L, 9, 1) == ERROR) printf(\"改第 9 个元素：越界，返回 ERROR（符合预期）\\n\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"修改前（正向）：10 ⇄ 20 ⇄ 30 ⇄ NULL\n把第 2 个元素改成 99 后（正向）：10 ⇄ 99 ⇄ 30 ⇄ NULL\n把第 2 个元素改成 99 后（反向）：30 ⇄ 99 ⇄ 10 ⇄ NULL\n（改值只动 data，指针一根都不该变）\n改第 9 个元素：越界，返回 ERROR（符合预期）"},"02-02-doubly-09":{"modes":{"detail":"// ============ 双向链表插入要动四根指针 ============\n// 单链表插入动两根：\n//\n//     s->next = p->next;\n//     p->next = s;\n//\n// 双向链表要动四根，因为新结点和前后两个邻居都要建立双向关系：\n//\n//     ① s->next = p->next;\n//     ② if (p->next != NULL)  p->next->prior = s;\n//     ③ s->prior = p;\n//     ④ p->next = s;\n//\n// ①③ 是\"新结点指出去\"，②④ 是\"邻居指回来\"。四句缺一句，\n// 链表就会变成\"一个方向通、另一个方向断\"——正向遍历一切正常，\n// 反向遍历或后续删除时突然崩溃。这是最难查的一类 bug。\n//\n// ============ ②为什么要判空 ============\n// 如果 i 正好等于表长 +1（插到表尾），那么 p->next 是 NULL，\n// 此时 p 后面没有结点，也就没有人需要把 prior 指回来。\n// 不判空就直接写 p->next->prior，等于对空指针取成员，程序当场崩溃。\n//\n// ============ 四步做完的过程 ============\n// 目标：在第 2 个位置插入 15（即 [10] 之后）\n//\n//  插入前：  L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//                       p\n//\n//  ① s->next = p->next\n//          [15] ──▶ [20]            （此时 p->next 还指着 [20]）\n//\n//  ② p->next->prior = s\n//          [20] ◀── [15]            （[20] 的前驱从 [10] 改成 [15]）\n//\n//  ③ s->prior = p\n//          [10] ◀── [15]            （[15] 的前驱指向 [10]）\n//\n//  ④ p->next = s\n//          [10] ──▶ [15]            （[10] 的后继从 [20] 改成 [15]）\n//\n//  插入后：  L ─▶ [头] ⇄ [10] ⇄ [15] ⇄ [20] ⇄ [30]\n//\n// 还有一条铁律：**先判断能不能插，再造结点**。顺序颠倒会导致插入失败时\n// 白白多出一个没挂上链表的\"孤儿结点\"。\n\n// 参数 L 是头结点指针；i 是位置，e 是数据\n// 位置 i 从 1 开始计数，表示\"插完以后它是第 i 个结点\"\nStatus ListInsert(DuLinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n    DuLNode *p = L;\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n// 循环条件 j < i - 1：i = 1 时一次也不进循环，p 直接就是头结点\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n// 先检查后造结点，避免产生孤儿结点\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// ① 新结点的 next 接住后面的那一串\n    s->next = p->next;\n// ② 后面那个结点的 prior 改指新结点（表尾时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = s;\n    }\n// ③ 新结点的 prior 指向前驱（前驱一定存在，不用判空）\n// 前驱要么是头结点，要么是真实结点，绝不会是 NULL —— 这正是头结点的价值\n    s->prior = p;\n// ④ 前驱的 next 指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}","short":"\n// 参数 L 是头结点指针；i 是位置，e 是数据\nStatus ListInsert(DuLinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n    DuLNode *p = L;\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// ① 新结点的 next 接住后面的那一串\n    s->next = p->next;\n// ② 后面那个结点的 prior 改指新结点（表尾时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = s;\n    }\n// ③ 新结点的 prior 指向前驱（前驱一定存在，不用判空）\n    s->prior = p;\n// ④ 前驱的 next 指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}","none":"\nStatus ListInsert(DuLinkList L, int i, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n    int j = 0;\n\n    while (p != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i - 1) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = p->next;\n    if (p->next != NULL)\n    {\n        p->next->prior = s;\n    }\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 12 printListReverse ======== */\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 09  ListInsert\n *  作用：先找第 i-1 个结点 p，再造新结点 s，把 s 插到 p 后面并补全两个方向。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 ListInsert：中间插、头部插、尾部插（判空分支）、越界插 */\nint main(void)\n{\n    DuLinkList L = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n\n    printf(\"插入前（正向）：\");\n    printList(L);\n\n    ListInsert(L, 2, 15);\n    printf(\"在第 2 位插入 15（正向）：\");\n    printList(L);\n    printf(\"在第 2 位插入 15（反向）：\");\n    printListReverse(L);\n\n    ListInsert(L, 1, 5);\n    printf(\"在第 1 位插入 5 （正向）：\");\n    printList(L);\n\n    ListInsert(L, 5, 99);\n    printf(\"插到表尾（第 5 位）后（正向）：\");\n    printList(L);\n    printf(\"插到表尾（第 5 位）后（反向）：\");\n    printListReverse(L);\n    printf(\"（插到表尾时 p->next 是 NULL，不判空就会崩在这里）\\n\");\n\n    printf(\"在第 99 位插入：%s\\n\", ListInsert(L, 99, 1) == OK ? \"成功（不符合预期）\" : \"失败（越界，符合预期）\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"插入前（正向）：10 ⇄ 20 ⇄ NULL\n在第 2 位插入 15（正向）：10 ⇄ 15 ⇄ 20 ⇄ NULL\n在第 2 位插入 15（反向）：20 ⇄ 15 ⇄ 10 ⇄ NULL\n在第 1 位插入 5 （正向）：5 ⇄ 10 ⇄ 15 ⇄ 20 ⇄ NULL\n插到表尾（第 5 位）后（正向）：5 ⇄ 10 ⇄ 15 ⇄ 20 ⇄ 99 ⇄ NULL\n插到表尾（第 5 位）后（反向）：99 ⇄ 20 ⇄ 15 ⇄ 10 ⇄ 5 ⇄ NULL\n（插到表尾时 p->next 是 NULL，不判空就会崩在这里）\n在第 99 位插入：失败（越界，符合预期）"},"02-02-doubly-10":{"modes":{"detail":"// ============ 这里体现双向链表真正的价值 ============\n// 单链表要删除第 i 个结点，必须先找到第 i-1 个结点（前驱），\n// 因为只有前驱能改 next 把待删结点绕过去：\n//\n//     单链表：  走 i-1 步找到前驱 p  →  改 p 一根指针  →  free\n//\n// 双向链表的第 i 个结点自己就带着 prior，压根不需要那个前驱变量：\n//\n//     双向链表：走 i 步定位到待删结点 p  →  用 p->prior 直接改两根指针  →  free\n//\n// 少了一次\"找前驱\"的过程，代码也更直白：删谁就站在谁身上操作。\n//\n// ============ 为什么 p->prior 不用判空 ============\n// 因为 p 是第 i 个数据结点（i ≥ 1），它的前驱**至少是头结点**，\n// 永远不可能是 NULL。头结点在这里又一次当了哨兵。\n// 而 p->next 有可能是 NULL（删的正好是尾结点），所以那一句要判空。\n//\n// ============ 两根指针 + 一次 free ============\n// 目标：删除第 2 个结点（值为 15）\n//\n//  删除前：  L ─▶ [头] ⇄ [10] ⇄ [15] ⇄ [20] ⇄ [30]\n//                                p\n//\n//  ① p->prior->next = p->next\n//          [10] ──▶ [20]            （[10] 的后继越过 [15]）\n//\n//  ② p->next->prior = p->prior\n//          [20] ◀── [10]            （[20] 的前驱越过 [15]）\n//\n//  ③ free(p)\n//          [15] 这块内存还给系统，p 变成野指针\n//\n//  删除后：  L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//\n// 顺序要点：一定要先把 p->data 存进 *e，再 free(p)；\n// free 之后再去读 p->data 就是\"释放后使用\"，属于未定义行为。\n\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(DuLinkList L, int i, ElemType *e)\n{\n// 注意：直接从第 1 个数据结点起步，目标是\"第 i 个结点自己\"\n// 单链表这里必须从头结点起步去找前驱，双向链表不需要\n    DuLNode *p = L->next;\n// j 与 p 同步，表示 p 是第几个结点\n    int j = 1;\n\n// 走 i 步，让 p 停在待删结点上\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p 为空说明第 i 个结点不存在\n    if (p == NULL || j > i) return ERROR;\n\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = p->data;\n\n// ① 前驱的后继越过 p\n// p->prior 一定非空：它至少是头结点\n    p->prior->next = p->next;\n// ② 后继的前驱越过 p（删的是尾结点时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = p->prior;\n    }\n// ③ 释放 p 占用的内存，防止内存泄漏\n    free(p);\n\n// 删除成功\n    return OK;\n}","short":"\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(DuLinkList L, int i, ElemType *e)\n{\n// 注意：直接从第 1 个数据结点起步，目标是\"第 i 个结点自己\"\n    DuLNode *p = L->next;\n// j 与 p 同步，表示 p 是第几个结点\n    int j = 1;\n\n// 走 i 步，让 p 停在待删结点上\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p 为空说明第 i 个结点不存在\n    if (p == NULL || j > i) return ERROR;\n\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = p->data;\n\n// ① 前驱的后继越过 p\n    p->prior->next = p->next;\n// ② 后继的前驱越过 p（删的是尾结点时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = p->prior;\n    }\n// ③ 释放 p 占用的内存，防止内存泄漏\n    free(p);\n\n// 删除成功\n    return OK;\n}","none":"\nStatus ListDelete(DuLinkList L, int i, ElemType *e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    p->prior->next = p->next;\n    if (p->next != NULL)\n    {\n        p->next->prior = p->prior;\n    }\n    free(p);\n\n    return OK;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 12 printListReverse ======== */\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 10  ListDelete\n *  作用：定位到第 i 个结点，用它的 prior 摘链，再释放 —— 双向链表不必找前驱。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 ListDelete：删中间、删第一个、删尾结点（判空分支）、越界删 */\nint main(void)\n{\n    DuLinkList L = NULL;\n    ElemType e = -1;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    applist(L, 40);\n\n    printf(\"删除前（正向）：\");\n    printList(L);\n\n    if (ListDelete(L, 2, &e) == OK) printf(\"删除第 2 个结点，值是 %d\\n\", e);\n    printf(\"删除后（正向）：\");\n    printList(L);\n    printf(\"删除后（反向）：\");\n    printListReverse(L);\n\n    if (ListDelete(L, 1, &e) == OK) printf(\"删除第 1 个结点，值是 %d\\n\", e);\n    printf(\"再删除后（正向）：\");\n    printList(L);\n\n    if (ListDelete(L, 2, &e) == OK) printf(\"删除最后一个结点，值是 %d\\n\", e);\n    printf(\"删掉尾结点后（正向）：\");\n    printList(L);\n    printf(\"删掉尾结点后（反向）：\");\n    printListReverse(L);\n    printf(\"（删尾结点时 p->next 是 NULL，那一条判空分支必须测到）\\n\");\n\n    printf(\"删除第 9 个结点：%s\\n\", ListDelete(L, 9, &e) == OK ? \"成功（不符合预期）\" : \"失败（越界，符合预期）\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"删除前（正向）：10 ⇄ 20 ⇄ 30 ⇄ 40 ⇄ NULL\n删除第 2 个结点，值是 20\n删除后（正向）：10 ⇄ 30 ⇄ 40 ⇄ NULL\n删除后（反向）：40 ⇄ 30 ⇄ 10 ⇄ NULL\n删除第 1 个结点，值是 10\n再删除后（正向）：30 ⇄ 40 ⇄ NULL\n删除最后一个结点，值是 40\n删掉尾结点后（正向）：30 ⇄ NULL\n删掉尾结点后（反向）：30 ⇄ NULL\n（删尾结点时 p->next 是 NULL，那一条判空分支必须测到）\n删除第 9 个结点：失败（越界，符合预期）"},"02-02-doubly-11":{"modes":{"detail":"// ============ 正向遍历：和单链表一模一样 ============\n//\n//   p = L->next;              ← 跳过不存数据的头结点\n//   while (p != NULL) {       ← 走到 NULL 说明到尾了\n//       ... 处理 p->data ...\n//       p = p->next;          ← 千万别忘了这一句，否则死循环\n//   }\n//\n// 输出用 `⇄` 而不是 `->`，是为了提醒这条链是**双向**的：\n// 屏幕上看到的每个 `⇄`，在内存里都对应两根指针。\n//\n// 真正体现双向价值的是下一个模块 —— 反向打印。\n\n// 只读操作，参数直接传 DuLinkList（一级指针）\nvoid printList(DuLinkList L)\n{\n// 从第一个真实结点开始\n    DuLNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，`⇄` 表示这里是双向连接\n        printf(\"%d ⇄ \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}","short":"\n// 只读操作，参数直接传 DuLinkList（一级指针）\nvoid printList(DuLinkList L)\n{\n// 从第一个真实结点开始\n    DuLNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，`⇄` 表示这里是双向连接\n        printf(\"%d ⇄ \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}","none":"\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 11  printList\n *  作用：顺着 next 从头走到尾，把每个结点的数据打出来。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 printList：空表要有明确提示，非空要能完整走完 */\nint main(void)\n{\n    DuLinkList L = NULL;\n\n    InitList(&L);\n\n    printf(\"空表输出：\");\n    printList(L);\n\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    printf(\"三元素输出：\");\n    printList(L);\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"空表输出：(空表)\n三元素输出：10 ⇄ 20 ⇄ 30 ⇄ NULL"},"02-02-doubly-12":{"modes":{"detail":"// ============ 这就是双向链表多一个指针换来的能力 ============\n// 单链表想倒着打印，只有两条路：\n//   1. 递归（函数调用栈替你记住来路，但表长了会爆栈）；\n//   2. 先把数据全拷进数组，再倒着输出（额外 O(n) 空间）。\n// 双向链表不用这么麻烦 —— prior 就在结点里，直接往回走。\n//\n// ============ 为什么循环条件是 p != L 而不是 p != NULL ============\n// 这是本模块最容易写错的地方。\n//\n//   反向走的时候，如果写 while (p != NULL)：\n//     p 会一路走过头结点，而头结点的 data 是**没意义的垃圾值**，\n//     屏幕上会莫名其妙多打一个数；再往后 p 变成 NULL 才停。\n//\n//   正确写法是 while (p != L)：\n//     走到头结点就停 —— 头结点是哨兵，不该被当成数据打印。\n//\n// 顺带一提：正因为头结点的 prior 是 NULL，如果哪一步把\"停\"的条件写错，\n// 就会顺着 NULL 继续取 prior，直接崩掉。\n//\n// ============ 走法示意 ============\n//\n//  第一步：顺着 next 找到尾结点\n//    L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//                                  p\n//\n//  第二步：顺着 prior 一路往回，边走边打印，直到撞上头结点\n//    L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//            p ◀──── 返回路径\n//\n//  屏幕上得到：  30 ⇄ 20 ⇄ 10 ⇄ NULL\n\n// 只读操作，不需要修改链表\nvoid printListReverse(DuLinkList L)\n{\n// 从头结点后面开始\n    DuLNode *p = L->next;\n\n// 空表同样单独处理\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回\n        return;\n    }\n\n// 第一步：顺着 next 走到尾结点\n// 条件写 p->next != NULL，所以停下时 p 正好是最后一个结点\n    while (p->next != NULL)\n    {\n// 指针后移\n        p = p->next;\n    }\n\n// 第二步：顺着 prior 往回走，撞到头结点就停\n// 这里必须写 p != L，不能写 p != NULL —— 头结点不能被当成数据打印\n    while (p != L)\n    {\n// 打印当前数据\n        printf(\"%d ⇄ \", p->data);\n// 顺着 prior 往回走一格（单链表没有这一步可走）\n        p = p->prior;\n    }\n\n// 收尾\n    printf(\"NULL\\n\");\n}","short":"\n// 只读操作，不需要修改链表\nvoid printListReverse(DuLinkList L)\n{\n// 从头结点后面开始\n    DuLNode *p = L->next;\n\n// 空表同样单独处理\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回\n        return;\n    }\n\n// 第一步：顺着 next 走到尾结点\n    while (p->next != NULL)\n    {\n// 指针后移\n        p = p->next;\n    }\n\n// 第二步：顺着 prior 往回走，撞到头结点就停\n    while (p != L)\n    {\n// 打印当前数据\n        printf(\"%d ⇄ \", p->data);\n// 顺着 prior 往回走一格（单链表没有这一步可走）\n        p = p->prior;\n    }\n\n// 收尾\n    printf(\"NULL\\n\");\n}","none":"\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 12  printListReverse\n *  作用：先走到尾结点，再顺着 prior 一路走回头结点 —— 单链表做不到这件事。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 printListReverse：这是双向链表独有的能力，也是最容易写错的地方 */\nint main(void)\n{\n    DuLinkList L = NULL;\n\n    InitList(&L);\n\n    printf(\"空表反向输出：\");\n    printListReverse(L);\n\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    printf(\"正向输出：\");\n    printList(L);\n    printf(\"反向输出：\");\n    printListReverse(L);\n    printf(\"（反向输出必须是正向的倒序，而且是 30 20 10，不能多打一个头结点）\\n\");\n\n    freeList(&L);\n    return 0;\n}\n","expectedOutput":"空表反向输出：(空表)\n正向输出：10 ⇄ 20 ⇄ 30 ⇄ NULL\n反向输出：30 ⇄ 20 ⇄ 10 ⇄ NULL\n（反向输出必须是正向的倒序，而且是 30 20 10，不能多打一个头结点）"},"02-02-doubly-13":{"modes":{"detail":"// ============ 释放只需要 next 方向 ============\n// 虽然结点里有 prior，但释放时**根本用不到它** ——\n// 因为必须先把整条链走完才能全部释放，而\"走完\"只需要 next。\n//\n// ============ 为什么必须先保存 next ============\n//\n//  错误写法：                正确写法：\n//    free(p);                  q = p;\n//    p = p->next;   ← 危险!    p = p->next;\n//                             free(q);\n//\n// free(p) 之后那块内存已经还给系统，p->next 读到的是垃圾值，\n// 用这个垃圾值当地址去访问就是\"释放后使用\"（use after free）。\n//\n// 更危险的是：如果在 free(p) 之前先去动 p->prior->next 之类的指针\n// （双向链表很容易顺手这么写），会把已经损坏的链再改一次，\n// 崩溃点离真正的错误点非常远，极难排查。\n\n// 参数是 DuLinkList *，因为最后要把调用者的 L 改成 NULL\nvoid freeList(DuLinkList *L)\n{\n// 从第一个真实结点开始清理\n    DuLNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    DuLNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n// 这三步顺序固定：存 → 走 → 放，缺一不可\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}","short":"\n// 参数是 DuLinkList *，因为最后要把调用者的 L 改成 NULL\nvoid freeList(DuLinkList *L)\n{\n// 从第一个真实结点开始清理\n    DuLNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    DuLNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}","none":"\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ====================================================================\n *  轮到你了：模块 13  freeList\n *  作用：逐个释放每个结点，最后把头结点也释放掉，并把指针置 NULL。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 freeList：释放完 L 必须变成 NULL，否则是野指针 */\nint main(void)\n{\n    DuLinkList L = NULL;\n\n    InitList(&L);\n    applist(L, 10);\n    applist(L, 20);\n\n    printf(\"释放前 L = %s\\n\", L != NULL ? \"非 NULL\" : \"NULL\");\n    freeList(&L);\n    printf(\"释放后 L = %s\\n\", L == NULL ? \"NULL（符合预期）\" : \"非 NULL（不符合预期）\");\n    printf(\"（双向链表释放只需要走 next 方向，prior 用不上）\\n\");\n\n    return 0;\n}\n","expectedOutput":"释放前 L = 非 NULL\n释放后 L = NULL（符合预期）\n（双向链表释放只需要走 next 方向，prior 用不上）"},"02-02-doubly-14":{"modes":{"detail":"// ============ 双向链表的测试要多做一件事 ============\n// 单链表只要正向打印对，基本就对了。\n// 双向链表**每一步都要正反向打印对照** —— 因为漏改 prior 的 bug\n// 正向完全看不出来，只有反向遍历（或后续的删除）才会暴露。\n//\n// 所以本测试里每次改动链表之后，都会同时打印正向和反向，\n// 两个方向互为对方的\"校验和\"。\n//\n// 另外一定要测三种边界：\n//   1. 空表      （初始化后立刻打印）\n//   2. 越界位置  （插入/查找/修改/删除一个不存在的位置，应返回 ERROR）\n//   3. 删尾结点  （专门验证 p->next == NULL 那条判空分支）\n\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    DuLinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    DuLNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望正向：10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"追加后（正向）：\");\n    printList(L);\n// 期望反向：30 ⇄ 20 ⇄ 10 ⇄ NULL\n// 反向能打对，说明尾插时 s->prior = p 那一句没漏\n    printf(\"追加后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 5 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望正向：5 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"头插 5 后（正向）：\");\n    printList(L);\n// 反向也要对：30 ⇄ 20 ⇄ 10 ⇄ 5 ⇄ NULL\n    printf(\"头插 5 后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 5 和 10 之间\n    ListInsert(L, 2, 15);\n// 期望正向：5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"插入后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 5 ⇄ NULL\n    printf(\"插入后（反向）：\");\n    printListReverse(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n// 此时链表是 5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30，第 3 个是 10\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：10\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就同时验证前后两个方向\n    if (p != NULL)\n    {\n// 双向链表的好处：拿到结点就能同时说出它的前驱和后继\n        printf(\"找到 20：它的前驱是 %d，后继是 %s\\n\",\n               p->prior->data,\n               p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位修改 ==========\n    printf(\"\\n========== 7. 按位修改 ==========\\n\");\n// 把第 1 个元素改成 99\n    if (ModifyElem(L, 1, 99) == OK)\n    {\n// 期望正向：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n        printf(\"把第 1 个元素改成 99 后：\");\n        printList(L);\n    }\n// 越界修改测试\n    printf(\"改第 100 个元素：%s\\n\", ModifyElem(L, 100, 1) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：正反向遍历对照 ==========\n    printf(\"\\n========== 8. 正反向遍历对照 ==========\\n\");\n// 同一份链表，两个方向必须互为倒序\n    printf(\"正向：\");\n    printList(L);\n    printf(\"反向：\");\n    printListReverse(L);\n\n// ========== 第 9 步：按位删除 ==========\n    printf(\"\\n========== 9. 删除第 2 个结点 ==========\\n\");\n// 删除第 2 个结点（值为 15），被删的值通过 e 带出来\n    if (ListDelete(L, 2, &e) == OK)\n    {\n// 期望输出：被删除的是 15\n        printf(\"已删除第 2 个结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"删除后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除后（反向）：\");\n    printListReverse(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 10 步：删尾结点（专测判空分支）==========\n    printf(\"\\n========== 10. 删除尾结点 ==========\\n\");\n// 此时链表是 99 ⇄ 10 ⇄ 20 ⇄ 30，共 4 个结点\n// 删尾结点会走到 p->next == NULL 那条分支 —— 也就是不执行\"后继的 prior 改指\"。\n// 这条分支不测，删尾结点时的崩溃就永远不会被发现。\n    if (ListDelete(L, 4, &e) == OK)\n    {\n        printf(\"已删除尾结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ NULL\n    printf(\"删除尾结点后（正向）：\");\n    printList(L);\n// 反向核对：20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除尾结点后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 11 步：释放整表 ==========\n    printf(\"\\n========== 11. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}","short":"\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    DuLinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    DuLNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望正向：10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"追加后（正向）：\");\n    printList(L);\n// 期望反向：30 ⇄ 20 ⇄ 10 ⇄ NULL\n    printf(\"追加后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 5 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望正向：5 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"头插 5 后（正向）：\");\n    printList(L);\n// 反向也要对：30 ⇄ 20 ⇄ 10 ⇄ 5 ⇄ NULL\n    printf(\"头插 5 后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 5 和 10 之间\n    ListInsert(L, 2, 15);\n// 期望正向：5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"插入后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 5 ⇄ NULL\n    printf(\"插入后（反向）：\");\n    printListReverse(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：10\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就同时验证前后两个方向\n    if (p != NULL)\n    {\n// 双向链表的好处：拿到结点就能同时说出它的前驱和后继\n        printf(\"找到 20：它的前驱是 %d，后继是 %s\\n\",\n               p->prior->data,\n               p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位修改 ==========\n    printf(\"\\n========== 7. 按位修改 ==========\\n\");\n// 把第 1 个元素改成 99\n    if (ModifyElem(L, 1, 99) == OK)\n    {\n// 期望正向：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n        printf(\"把第 1 个元素改成 99 后：\");\n        printList(L);\n    }\n// 越界修改测试\n    printf(\"改第 100 个元素：%s\\n\", ModifyElem(L, 100, 1) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：正反向遍历对照 ==========\n    printf(\"\\n========== 8. 正反向遍历对照 ==========\\n\");\n// 同一份链表，两个方向必须互为倒序\n    printf(\"正向：\");\n    printList(L);\n    printf(\"反向：\");\n    printListReverse(L);\n\n// ========== 第 9 步：按位删除 ==========\n    printf(\"\\n========== 9. 删除第 2 个结点 ==========\\n\");\n// 删除第 2 个结点（值为 15），被删的值通过 e 带出来\n    if (ListDelete(L, 2, &e) == OK)\n    {\n// 期望输出：被删除的是 15\n        printf(\"已删除第 2 个结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"删除后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除后（反向）：\");\n    printListReverse(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 10 步：删尾结点（专测判空分支）==========\n    printf(\"\\n========== 10. 删除尾结点 ==========\\n\");\n// 此时链表是 99 ⇄ 10 ⇄ 20 ⇄ 30，共 4 个结点\n    if (ListDelete(L, 4, &e) == OK)\n    {\n        printf(\"已删除尾结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ NULL\n    printf(\"删除尾结点后（正向）：\");\n    printList(L);\n// 反向核对：20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除尾结点后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 11 步：释放整表 ==========\n    printf(\"\\n========== 11. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}","none":"\nint main(void)\n{\n    DuLinkList L = NULL;\n    DuLNode *p = NULL;\n    ElemType e = 0;\n\n    printf(\"========== 1. 初始化 ==========\\n\");\n    if (InitList(&L) != OK)\n    {\n        printf(\"初始化失败！\\n\");\n        return ERROR;\n    }\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    printf(\"追加后（正向）：\");\n    printList(L);\n    printf(\"追加后（反向）：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 3. 头插法插入 5 ==========\\n\");\n    HeadInsert(L, 5);\n    printf(\"头插 5 后（正向）：\");\n    printList(L);\n    printf(\"头插 5 后（反向）：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n    ListInsert(L, 2, 15);\n    printf(\"插入后（正向）：\");\n    printList(L);\n    printf(\"插入后（反向）：\");\n    printListReverse(L);\n\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n    p = LocateElem(L, 20);\n    if (p != NULL)\n    {\n        printf(\"找到 20：它的前驱是 %d，后继是 %s\\n\",\n               p->prior->data,\n               p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n    if (LocateElem(L, 99) == NULL)\n    {\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 7. 按位修改 ==========\\n\");\n    if (ModifyElem(L, 1, 99) == OK)\n    {\n        printf(\"把第 1 个元素改成 99 后：\");\n        printList(L);\n    }\n    printf(\"改第 100 个元素：%s\\n\", ModifyElem(L, 100, 1) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 8. 正反向遍历对照 ==========\\n\");\n    printf(\"正向：\");\n    printList(L);\n    printf(\"反向：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 9. 删除第 2 个结点 ==========\\n\");\n    if (ListDelete(L, 2, &e) == OK)\n    {\n        printf(\"已删除第 2 个结点，它的值是 %d\\n\", e);\n    }\n    printf(\"删除后（正向）：\");\n    printList(L);\n    printf(\"删除后（反向）：\");\n    printListReverse(L);\n\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 10. 删除尾结点 ==========\\n\");\n    if (ListDelete(L, 4, &e) == OK)\n    {\n        printf(\"已删除尾结点，它的值是 %d\\n\", e);\n    }\n    printf(\"删除尾结点后（正向）：\");\n    printList(L);\n    printf(\"删除尾结点后（反向）：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 11. 释放内存 ==========\\n\");\n    freeList(&L);\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n    return 0;\n}"},"scaffold":"/* ======== [已给出] 模块 01 头文件与 typedef ======== */\n\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n/* ======== [已给出] 模块 02 creatNode ======== */\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n/* ======== [已给出] 模块 03 InitList ======== */\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 applist ======== */\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 05 HeadInsert ======== */\n\nStatus HeadInsert(DuLinkList L, ElemType e)\n{\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = L->next;\n    if (s->next != NULL)\n    {\n        s->next->prior = s;\n    }\n    s->prior = L;\n    L->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 06 GetElem_L ======== */\n\nStatus GetElem_L(DuLinkList L, int i, ElemType *e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 07 LocateElem ======== */\n\nDuLNode *LocateElem(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L->next;\n\n    while (p != NULL && p->data != e)\n    {\n        p = p->next;\n    }\n\n    return p;\n}\n\n/* ======== [已给出] 模块 08 ModifyElem ======== */\n\nStatus ModifyElem(DuLinkList L, int i, ElemType e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    p->data = e;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 09 ListInsert ======== */\n\nStatus ListInsert(DuLinkList L, int i, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n    int j = 0;\n\n    while (p != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i - 1) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = p->next;\n    if (p->next != NULL)\n    {\n        p->next->prior = s;\n    }\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 10 ListDelete ======== */\n\nStatus ListDelete(DuLinkList L, int i, ElemType *e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    p->prior->next = p->next;\n    if (p->next != NULL)\n    {\n        p->next->prior = p->prior;\n    }\n    free(p);\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 11 printList ======== */\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 12 printListReverse ======== */\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n/* ======== [已给出] 模块 13 freeList ======== */\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n/* ====================================================================\n *  轮到你了：模块 14  main\n *  作用：把增、删、查、改四类操作各跑一遍，正反向遍历对照验证 prior 没写错。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n","expectedOutput":"========== 1. 初始化 ==========\n初始化完成，当前链表：(空表)\n\n========== 2. 尾部追加 10 20 30 ==========\n追加后（正向）：10 ⇄ 20 ⇄ 30 ⇄ NULL\n追加后（反向）：30 ⇄ 20 ⇄ 10 ⇄ NULL\n\n========== 3. 头插法插入 5 ==========\n头插 5 后（正向）：5 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n头插 5 后（反向）：30 ⇄ 20 ⇄ 10 ⇄ 5 ⇄ NULL\n\n========== 4. 在第 2 位插入 15 ==========\n插入后（正向）：5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n插入后（反向）：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 5 ⇄ NULL\n在第 99 位插入 5 ：失败（越界，符合预期）\n\n========== 5. 按位查找 ==========\n第 3 个元素的值是 10\n查第 100 个元素失败（越界，符合预期）\n\n========== 6. 按值查找 ==========\n找到 20：它的前驱是 10，后继是 一个有效结点\n查找值 99 返回 NULL（符合预期）\n\n========== 7. 按位修改 ==========\n把第 1 个元素改成 99 后：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n改第 100 个元素：失败（越界，符合预期）\n\n========== 8. 正反向遍历对照 ==========\n正向：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n反向：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 99 ⇄ NULL\n\n========== 9. 删除第 2 个结点 ==========\n已删除第 2 个结点，它的值是 15\n删除后（正向）：99 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n删除后（反向）：30 ⇄ 20 ⇄ 10 ⇄ 99 ⇄ NULL\n删除第 10 个结点：失败（越界，符合预期）\n\n========== 10. 删除尾结点 ==========\n已删除尾结点，它的值是 30\n删除尾结点后（正向）：99 ⇄ 10 ⇄ 20 ⇄ NULL\n删除尾结点后（反向）：20 ⇄ 10 ⇄ 99 ⇄ NULL\n\n========== 11. 释放内存 ==========\n释放完成，L = NULL"},"02-02-doubly-15":{"modes":{"detail":"// ============ 相比单链表，结构体里多了什么 ============\n//\n//   单链表：  typedef struct LNode  { ElemType data; struct LNode  *next;  } LNode,  *LinkList;\n//   双向链表：typedef struct DuLNode { ElemType data; struct DuLNode *prior;\n//                                                     struct DuLNode *next;  } DuLNode, *DuLinkList;\n//\n// 就多了一个 prior。命名上习惯用 Du 前缀（double）把两者区分开 ——\n// 一份代码里同时出现单链表和双向链表时，这个前缀能救命。\n//\n// ============ 内存里长什么样 ============\n//\n//   [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//    L\n//\n// 屏幕上每个 ⇄ 在内存里其实是**两根方向相反**的指针。\n// 把每个结点的两个指针摊开列出来：\n//\n//   [头]：prior = NULL、next = [10]\n//   [10]：prior = [头]、next = [20]\n//   [20]：prior = [10]、next = [30]\n//   [30]：prior = [20]、next = NULL\n//\n// 只有头结点的 prior 是 NULL（它前面没有结点了），\n// 只有尾结点的 next 是 NULL（它后面没有结点了）。\n// 这两条性质后面写插入、删除时会反复用到 —— 尤其是\"要不要判空\"。\n\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n// 约定：函数返回 Status，OK 表示成功，ERROR 表示失败\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n// 内存分配失败的专用错误码，取一个和 OK/ERROR 都不冲突的负数\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\ntypedef int Status;\ntypedef int ElemType;\n\n// 双向结点：数据域 + 前驱指针 + 后继指针\n// 两个指针的分工：\n//   prior  指向前一个结点，头结点和\"第一个数据结点\"的 prior 关系要注意\n//   next   指向后一个结点，最后一个结点的 next 是 NULL\n//\n// 有一条贯穿全书的等式，插入删除时全靠它自检：\n//\n//     p->next->prior == p        以及        p->prior->next == p\n//\n// 也就是\"我指向谁，谁就得指回我\"。写双向链表的代码时，每改一次指针\n// 都拿这句话对一遍，基本不会错。\ntypedef struct DuLNode {\n// 数据域，存具体元素\n    ElemType data;\n// 前驱指针：指向上一个结点（这是单链表没有的）\n    struct DuLNode *prior;\n// 后继指针：指向下一个结点，尾结点为 NULL\n    struct DuLNode *next;\n// 一次 typedef 出两个名字：DuLNode（结点）和 DuLinkList（指向结点的指针）\n} DuLNode, *DuLinkList;\n\n// ============ 比单链表多置一个指针 ============\n// 单链表的新结点只需要 p->next = NULL；\n// 双向链表必须**两个方向都置 NULL**，否则 prior 就是野指针，\n// 后面一旦顺着 prior 往回走（比如反向遍历）就会访问到非法内存。\n//\n// 这个函数只负责\"造出一个孤立的结点\"，它还没有被挂到任何链表上，\n// 所以两个指针都应该是干净的 NULL。\n\n// 返回新结点的地址，所以返回类型是 DuLNode *\nDuLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 DuLNode *\n// 双向结点比单链表结点大一个指针：32 位下 12 字节，64 位下 24 字节\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n// 打印中文提示，让使用者知道是内存不够，而不是程序莫名其妙退出\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 前驱置 NULL（单链表没有这一步）\n    p->prior = NULL;\n// 后继置 NULL\n// 两个方向都干净了，结点才算\"造好但还没上链\"\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}\n\n// ============ 头结点在双向链表里的特殊性 ============\n// 头结点是\"哨兵\"：不存有效数据，只为让插入/删除不用特判第一个位置。\n// 在双向链表里它还有两条铁律：\n//\n//   1. 头结点的 prior 永远是 NULL —— 它前面没有结点了；\n//   2. 头结点的 next 指向第一个数据结点（空表时是 NULL）。\n//\n// 空表的样子：\n//\n//        ┌────┬──────┬──────┐\n//        │NULL│  --  │ NULL │\n//        └────┴──────┴──────┘\n//          L\n//\n// 因为头结点的 prior 是 NULL，\"第一个数据结点的 prior 指向头结点\"这条关系\n// 才和中间结点完全一致 —— 这就是后面删除时 p->prior 不用判空的原因。\n\n// 参数是 DuLinkList *（二级指针），因为要修改调用者手里的 L 本身\nStatus InitList(DuLinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 头结点的 prior 恒为 NULL，写出来是为了明确这条不变式\n    (*L)->prior = NULL;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}\n\n// ============ 双向链表比单链表多改一根指针 ============\n// 单链表尾插只有一步：p->next = s;\n// 双向链表要做两步，而且两步都要做：\n//\n//     s->prior = p;      ← 新结点回指旧的尾结点\n//     p->next  = s;      ← 旧尾结点指向新结点\n//\n// 少了第一句，整条链表从后往前就断了：正向遍历看不出来，一旦反向遍历\n// （或者删除尾结点时用 p->prior）就会踩到野指针。\n//\n// ============ 指针推进过程 ============\n// 目标：在尾部追加 30\n//\n//  初始：  L ─▶ [头] ⇄ [10] ⇄ [20]\n//          p\n//\n//  循环：  L ─▶ [头] ⇄ [10] ⇄ [20]\n//                              p        （p->next == NULL，p 就是尾结点）\n//\n//  挂链：  L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n// 注意 [20] 和 [30] 之间是**双向**的箭头，两根指针缺一不可。\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus applist(DuLinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    DuLNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n// 循环条件是 p->next != NULL，看的永远是\"下一个\"，所以停下时 p 正是尾结点\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点（此时它的 prior 和 next 都是 NULL）\n    s = creatNode(e);\n\n// 第一步：新结点的 prior 回指旧的尾结点\n// 这一句是双向链表特有的，单链表尾插没有\n    s->prior = p;\n// 第二步：旧尾结点的 next 指向新结点\n// 两句都做完，[p] ⇄ [s] 这条双向关系才成立\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}\n\n// ============ 头插法的特点 ============\n//\n//   applist（尾插）  ：从头一路走到尾             —— 时间复杂度 O(n)\n//   HeadInsert（头插）：直接插在头结点后面        —— 时间复杂度 O(1)\n//\n// 代价是头插会**颠倒顺序**：依次头插 10、20、30，得到的是 30 ⇄ 20 ⇄ 10。\n// 这不是缺陷 —— \"头插法建表\"正是靠它做逆序的。\n//\n// 再记住一个等价关系：HeadInsert(L, e) 和 ListInsert(L, 1, e) 干的是同一件事。\n//\n// ============ 双向链表的头插为什么必须判空表 ============\n// 单链表头插只有两句，不用管表是不是空的：\n//\n//     s->next = L->next;\n//     L->next = s;\n//\n// 双向链表要处理的指针多一根，而且有一句**在空表时是非法操作**：\n//\n//     s->next = L->next;\n//     if (s->next != NULL)  s->next->prior = s;    ← 必须判空！\n//     s->prior = L;\n//     L->next = s;\n//\n// 空表时 L->next 是 NULL，如果不判空就去写 s->next->prior，\n// 等于对空指针取成员 —— 程序当场崩溃。这是双向链表头插最经典的坑。\n//\n// ============ 插入前后 ============\n// 把 5 头插进 10 ⇄ 20\n//\n//  插入前：  L ─▶ [头] ⇄ [10] ⇄ [20]\n//\n//  四步做完：\n//           L ─▶ [头] ⇄ [5] ⇄ [10] ⇄ [20]\n//\n// 其中 [5] 与 [头]、[5] 与 [10] 之间都要是双向的。\n\n// 参数 L 是头结点指针，不需要改 L 本身\n// 位置固定在第 1 位，所以不需要 i 这个参数\nStatus HeadInsert(DuLinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点的 next 接住原来头结点后面的那一串\n    s->next = L->next;\n// 第二步：如果后面确实有结点，让它的 prior 改指新结点\n// 这一步必须判空：空表时 L->next 是 NULL，s->next->prior 就是空指针取成员\n    if (s->next != NULL)\n    {\n        s->next->prior = s;\n    }\n// 第三步：新结点的 prior 指向头结点\n// 头结点一定存在，所以这一句不需要判空\n    s->prior = L;\n// 第四步：头结点指向新结点，s 正式成为第 1 个结点\n    L->next = s;\n\n// 插入成功\n    return OK;\n}\n\n// ============ 这段代码和单链表完全一样 ============\n// 按位查找只需要沿 next 往后走，\"双向\"在这里帮不上忙\n// —— 除非你想从尾部倒着数，但那需要先知道表长，通常不划算。\n// （进阶：有的实现会额外记录 length，那样 i 大于一半时可以从尾结点\n//   顺着 prior 往前找，代价是每次插入删除都要维护 length。本教材从简。）\n//\n// ============ 找第 3 个结点（i = 3）============\n//\n//   L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//          j=1     j=2     j=3\n//           p       p       p\n//\n// 两个必须同时检查的失败情况：\n//   p == NULL  → i 比表长还大，数着数着掉出去了\n//   j > i      → i 小于 1（比如 i = 0），循环一次都没进\n\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\nStatus GetElem_L(DuLinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n// p != NULL 写在前面，短路求值保证不会对空指针取 ->next\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}\n\n// ============ 按值查找返回的是\"结点\"不是\"值\" ============\n// 拿回结点地址后，调用者还能继续访问它的 prior / next，\n// 也能在它前后做插入删除 —— 这比只拿回一个数据值有用得多。\n//\n// 找不到时返回 NULL，这是 C 语言表示\"没有这个结点\"的标准做法。\n//\n// 双向链表在这里同样没有优势（按值查找本身就是逐个比对），\n// 除非链表是按别的规则组织过的。\n\n// 返回 DuLNode *，找不到就是 NULL\nDuLNode *LocateElem(DuLinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    DuLNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n// 一旦 p->data == e，循环立刻停下，p 就停在第一个匹配的结点上\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}\n\n// ============ 改和查是一对 ============\n// 把 ModifyElem 和 GetElem_L 摆在一起看，会发现它们**只差最后一句**：\n//\n//     GetElem_L ：  *e = p->data;     把结点里的值读出来\n//     ModifyElem ：  p->data = e;     把新值写进结点\n//\n// 前面\"走 i-1 步定位\"的循环、以及越界检查，两者一模一样。\n// 这也说明一件事：**定位是链表操作里最通用的部分**，\n// 插入、删除、查找、修改都要先定位，区别只在定位到第几个结点、\n// 以及定位之后动几根指针。\n//\n// ============ 修改只动数据域，不动指针 ============\n// 这一点很重要：改一个结点的值不会改变链表的形状，\n// 所以不需要碰 prior / next，也就不会有双向链表那些\"漏改一根指针\"的风险。\n//\n// 代价是修改**不能改变链表的顺序**。如果你要的是\"把某个值挪到前面\"，\n// 那得先删除再插入，属于删+插的组合操作。\n\n// 要改的目标值通过 e 传进来（按值传递就够了，不需要指针）\nStatus ModifyElem(DuLinkList L, int i, ElemType e)\n{\n// 从第 1 个\"带数据的结点\"开始\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步\n    int j = 1;\n\n// 定位到第 i 个结点（与 GetElem_L 的循环完全相同）\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：第 i 个结点不存在就报错\n    if (p == NULL || j > i) return ERROR;\n\n// 把新值写进数据域 —— 只改数据，不碰任何指针\n    p->data = e;\n\n// 修改成功\n    return OK;\n}\n\n// ============ 双向链表插入要动四根指针 ============\n// 单链表插入动两根：\n//\n//     s->next = p->next;\n//     p->next = s;\n//\n// 双向链表要动四根，因为新结点和前后两个邻居都要建立双向关系：\n//\n//     ① s->next = p->next;\n//     ② if (p->next != NULL)  p->next->prior = s;\n//     ③ s->prior = p;\n//     ④ p->next = s;\n//\n// ①③ 是\"新结点指出去\"，②④ 是\"邻居指回来\"。四句缺一句，\n// 链表就会变成\"一个方向通、另一个方向断\"——正向遍历一切正常，\n// 反向遍历或后续删除时突然崩溃。这是最难查的一类 bug。\n//\n// ============ ②为什么要判空 ============\n// 如果 i 正好等于表长 +1（插到表尾），那么 p->next 是 NULL，\n// 此时 p 后面没有结点，也就没有人需要把 prior 指回来。\n// 不判空就直接写 p->next->prior，等于对空指针取成员，程序当场崩溃。\n//\n// ============ 四步做完的过程 ============\n// 目标：在第 2 个位置插入 15（即 [10] 之后）\n//\n//  插入前：  L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//                       p\n//\n//  ① s->next = p->next\n//          [15] ──▶ [20]            （此时 p->next 还指着 [20]）\n//\n//  ② p->next->prior = s\n//          [20] ◀── [15]            （[20] 的前驱从 [10] 改成 [15]）\n//\n//  ③ s->prior = p\n//          [10] ◀── [15]            （[15] 的前驱指向 [10]）\n//\n//  ④ p->next = s\n//          [10] ──▶ [15]            （[10] 的后继从 [20] 改成 [15]）\n//\n//  插入后：  L ─▶ [头] ⇄ [10] ⇄ [15] ⇄ [20] ⇄ [30]\n//\n// 还有一条铁律：**先判断能不能插，再造结点**。顺序颠倒会导致插入失败时\n// 白白多出一个没挂上链表的\"孤儿结点\"。\n\n// 参数 L 是头结点指针；i 是位置，e 是数据\n// 位置 i 从 1 开始计数，表示\"插完以后它是第 i 个结点\"\nStatus ListInsert(DuLinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n    DuLNode *p = L;\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n// 循环条件 j < i - 1：i = 1 时一次也不进循环，p 直接就是头结点\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n// 先检查后造结点，避免产生孤儿结点\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// ① 新结点的 next 接住后面的那一串\n    s->next = p->next;\n// ② 后面那个结点的 prior 改指新结点（表尾时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = s;\n    }\n// ③ 新结点的 prior 指向前驱（前驱一定存在，不用判空）\n// 前驱要么是头结点，要么是真实结点，绝不会是 NULL —— 这正是头结点的价值\n    s->prior = p;\n// ④ 前驱的 next 指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}\n\n// ============ 这里体现双向链表真正的价值 ============\n// 单链表要删除第 i 个结点，必须先找到第 i-1 个结点（前驱），\n// 因为只有前驱能改 next 把待删结点绕过去：\n//\n//     单链表：  走 i-1 步找到前驱 p  →  改 p 一根指针  →  free\n//\n// 双向链表的第 i 个结点自己就带着 prior，压根不需要那个前驱变量：\n//\n//     双向链表：走 i 步定位到待删结点 p  →  用 p->prior 直接改两根指针  →  free\n//\n// 少了一次\"找前驱\"的过程，代码也更直白：删谁就站在谁身上操作。\n//\n// ============ 为什么 p->prior 不用判空 ============\n// 因为 p 是第 i 个数据结点（i ≥ 1），它的前驱**至少是头结点**，\n// 永远不可能是 NULL。头结点在这里又一次当了哨兵。\n// 而 p->next 有可能是 NULL（删的正好是尾结点），所以那一句要判空。\n//\n// ============ 两根指针 + 一次 free ============\n// 目标：删除第 2 个结点（值为 15）\n//\n//  删除前：  L ─▶ [头] ⇄ [10] ⇄ [15] ⇄ [20] ⇄ [30]\n//                                p\n//\n//  ① p->prior->next = p->next\n//          [10] ──▶ [20]            （[10] 的后继越过 [15]）\n//\n//  ② p->next->prior = p->prior\n//          [20] ◀── [10]            （[20] 的前驱越过 [15]）\n//\n//  ③ free(p)\n//          [15] 这块内存还给系统，p 变成野指针\n//\n//  删除后：  L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//\n// 顺序要点：一定要先把 p->data 存进 *e，再 free(p)；\n// free 之后再去读 p->data 就是\"释放后使用\"，属于未定义行为。\n\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(DuLinkList L, int i, ElemType *e)\n{\n// 注意：直接从第 1 个数据结点起步，目标是\"第 i 个结点自己\"\n// 单链表这里必须从头结点起步去找前驱，双向链表不需要\n    DuLNode *p = L->next;\n// j 与 p 同步，表示 p 是第几个结点\n    int j = 1;\n\n// 走 i 步，让 p 停在待删结点上\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p 为空说明第 i 个结点不存在\n    if (p == NULL || j > i) return ERROR;\n\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = p->data;\n\n// ① 前驱的后继越过 p\n// p->prior 一定非空：它至少是头结点\n    p->prior->next = p->next;\n// ② 后继的前驱越过 p（删的是尾结点时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = p->prior;\n    }\n// ③ 释放 p 占用的内存，防止内存泄漏\n    free(p);\n\n// 删除成功\n    return OK;\n}\n\n// ============ 正向遍历：和单链表一模一样 ============\n//\n//   p = L->next;              ← 跳过不存数据的头结点\n//   while (p != NULL) {       ← 走到 NULL 说明到尾了\n//       ... 处理 p->data ...\n//       p = p->next;          ← 千万别忘了这一句，否则死循环\n//   }\n//\n// 输出用 `⇄` 而不是 `->`，是为了提醒这条链是**双向**的：\n// 屏幕上看到的每个 `⇄`，在内存里都对应两根指针。\n//\n// 真正体现双向价值的是下一个模块 —— 反向打印。\n\n// 只读操作，参数直接传 DuLinkList（一级指针）\nvoid printList(DuLinkList L)\n{\n// 从第一个真实结点开始\n    DuLNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，`⇄` 表示这里是双向连接\n        printf(\"%d ⇄ \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}\n\n// ============ 这就是双向链表多一个指针换来的能力 ============\n// 单链表想倒着打印，只有两条路：\n//   1. 递归（函数调用栈替你记住来路，但表长了会爆栈）；\n//   2. 先把数据全拷进数组，再倒着输出（额外 O(n) 空间）。\n// 双向链表不用这么麻烦 —— prior 就在结点里，直接往回走。\n//\n// ============ 为什么循环条件是 p != L 而不是 p != NULL ============\n// 这是本模块最容易写错的地方。\n//\n//   反向走的时候，如果写 while (p != NULL)：\n//     p 会一路走过头结点，而头结点的 data 是**没意义的垃圾值**，\n//     屏幕上会莫名其妙多打一个数；再往后 p 变成 NULL 才停。\n//\n//   正确写法是 while (p != L)：\n//     走到头结点就停 —— 头结点是哨兵，不该被当成数据打印。\n//\n// 顺带一提：正因为头结点的 prior 是 NULL，如果哪一步把\"停\"的条件写错，\n// 就会顺着 NULL 继续取 prior，直接崩掉。\n//\n// ============ 走法示意 ============\n//\n//  第一步：顺着 next 找到尾结点\n//    L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//                                  p\n//\n//  第二步：顺着 prior 一路往回，边走边打印，直到撞上头结点\n//    L ─▶ [头] ⇄ [10] ⇄ [20] ⇄ [30]\n//            p ◀──── 返回路径\n//\n//  屏幕上得到：  30 ⇄ 20 ⇄ 10 ⇄ NULL\n\n// 只读操作，不需要修改链表\nvoid printListReverse(DuLinkList L)\n{\n// 从头结点后面开始\n    DuLNode *p = L->next;\n\n// 空表同样单独处理\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回\n        return;\n    }\n\n// 第一步：顺着 next 走到尾结点\n// 条件写 p->next != NULL，所以停下时 p 正好是最后一个结点\n    while (p->next != NULL)\n    {\n// 指针后移\n        p = p->next;\n    }\n\n// 第二步：顺着 prior 往回走，撞到头结点就停\n// 这里必须写 p != L，不能写 p != NULL —— 头结点不能被当成数据打印\n    while (p != L)\n    {\n// 打印当前数据\n        printf(\"%d ⇄ \", p->data);\n// 顺着 prior 往回走一格（单链表没有这一步可走）\n        p = p->prior;\n    }\n\n// 收尾\n    printf(\"NULL\\n\");\n}\n\n// ============ 释放只需要 next 方向 ============\n// 虽然结点里有 prior，但释放时**根本用不到它** ——\n// 因为必须先把整条链走完才能全部释放，而\"走完\"只需要 next。\n//\n// ============ 为什么必须先保存 next ============\n//\n//  错误写法：                正确写法：\n//    free(p);                  q = p;\n//    p = p->next;   ← 危险!    p = p->next;\n//                             free(q);\n//\n// free(p) 之后那块内存已经还给系统，p->next 读到的是垃圾值，\n// 用这个垃圾值当地址去访问就是\"释放后使用\"（use after free）。\n//\n// 更危险的是：如果在 free(p) 之前先去动 p->prior->next 之类的指针\n// （双向链表很容易顺手这么写），会把已经损坏的链再改一次，\n// 崩溃点离真正的错误点非常远，极难排查。\n\n// 参数是 DuLinkList *，因为最后要把调用者的 L 改成 NULL\nvoid freeList(DuLinkList *L)\n{\n// 从第一个真实结点开始清理\n    DuLNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    DuLNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n// 这三步顺序固定：存 → 走 → 放，缺一不可\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}\n\n// ============ 双向链表的测试要多做一件事 ============\n// 单链表只要正向打印对，基本就对了。\n// 双向链表**每一步都要正反向打印对照** —— 因为漏改 prior 的 bug\n// 正向完全看不出来，只有反向遍历（或后续的删除）才会暴露。\n//\n// 所以本测试里每次改动链表之后，都会同时打印正向和反向，\n// 两个方向互为对方的\"校验和\"。\n//\n// 另外一定要测三种边界：\n//   1. 空表      （初始化后立刻打印）\n//   2. 越界位置  （插入/查找/修改/删除一个不存在的位置，应返回 ERROR）\n//   3. 删尾结点  （专门验证 p->next == NULL 那条判空分支）\n\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    DuLinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    DuLNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望正向：10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"追加后（正向）：\");\n    printList(L);\n// 期望反向：30 ⇄ 20 ⇄ 10 ⇄ NULL\n// 反向能打对，说明尾插时 s->prior = p 那一句没漏\n    printf(\"追加后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 5 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望正向：5 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"头插 5 后（正向）：\");\n    printList(L);\n// 反向也要对：30 ⇄ 20 ⇄ 10 ⇄ 5 ⇄ NULL\n    printf(\"头插 5 后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 5 和 10 之间\n    ListInsert(L, 2, 15);\n// 期望正向：5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"插入后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 5 ⇄ NULL\n    printf(\"插入后（反向）：\");\n    printListReverse(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n// 此时链表是 5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30，第 3 个是 10\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：10\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就同时验证前后两个方向\n    if (p != NULL)\n    {\n// 双向链表的好处：拿到结点就能同时说出它的前驱和后继\n        printf(\"找到 20：它的前驱是 %d，后继是 %s\\n\",\n               p->prior->data,\n               p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位修改 ==========\n    printf(\"\\n========== 7. 按位修改 ==========\\n\");\n// 把第 1 个元素改成 99\n    if (ModifyElem(L, 1, 99) == OK)\n    {\n// 期望正向：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n        printf(\"把第 1 个元素改成 99 后：\");\n        printList(L);\n    }\n// 越界修改测试\n    printf(\"改第 100 个元素：%s\\n\", ModifyElem(L, 100, 1) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：正反向遍历对照 ==========\n    printf(\"\\n========== 8. 正反向遍历对照 ==========\\n\");\n// 同一份链表，两个方向必须互为倒序\n    printf(\"正向：\");\n    printList(L);\n    printf(\"反向：\");\n    printListReverse(L);\n\n// ========== 第 9 步：按位删除 ==========\n    printf(\"\\n========== 9. 删除第 2 个结点 ==========\\n\");\n// 删除第 2 个结点（值为 15），被删的值通过 e 带出来\n    if (ListDelete(L, 2, &e) == OK)\n    {\n// 期望输出：被删除的是 15\n        printf(\"已删除第 2 个结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"删除后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除后（反向）：\");\n    printListReverse(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 10 步：删尾结点（专测判空分支）==========\n    printf(\"\\n========== 10. 删除尾结点 ==========\\n\");\n// 此时链表是 99 ⇄ 10 ⇄ 20 ⇄ 30，共 4 个结点\n// 删尾结点会走到 p->next == NULL 那条分支 —— 也就是不执行\"后继的 prior 改指\"。\n// 这条分支不测，删尾结点时的崩溃就永远不会被发现。\n    if (ListDelete(L, 4, &e) == OK)\n    {\n        printf(\"已删除尾结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ NULL\n    printf(\"删除尾结点后（正向）：\");\n    printList(L);\n// 反向核对：20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除尾结点后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 11 步：释放整表 ==========\n    printf(\"\\n========== 11. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}\n","short":"\n// 标准输入输出，printf 需要\n#include <stdio.h>\n// 内存管理，malloc / free 需要\n#include <stdlib.h>\n\n// 用宏定义状态码，让函数返回值有意义（比裸 1 / 0 好读）\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\n// Status 是函数返回类型；ElemType 是数据域类型\ntypedef int Status;\ntypedef int ElemType;\n\n// 双向结点：数据域 + 前驱指针 + 后继指针\ntypedef struct DuLNode {\n// 数据域，存具体元素\n    ElemType data;\n// 前驱指针：指向上一个结点（这是单链表没有的）\n    struct DuLNode *prior;\n// 后继指针：指向下一个结点，尾结点为 NULL\n    struct DuLNode *next;\n// 一次 typedef 出两个名字：DuLNode（结点）和 DuLinkList（指向结点的指针）\n} DuLNode, *DuLinkList;\n\n\n// 返回新结点的地址，所以返回类型是 DuLNode *\nDuLNode *creatNode(ElemType e)\n{\n// 申请一个结点大小的内存，强制转换成 DuLNode *\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n// 申请失败必须拦住，否则后面 p->data 就是非法访问\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n// 申请不到内存属于致命错误，直接结束程序\n        exit(OVERFLOW);\n    }\n\n// 填数据域\n    p->data = e;\n// 前驱置 NULL（单链表没有这一步）\n    p->prior = NULL;\n// 后继置 NULL\n    p->next = NULL;\n\n// 把新结点的地址返回给调用者\n    return p;\n}\n\n\n// 参数是 DuLinkList *（二级指针），因为要修改调用者手里的 L 本身\nStatus InitList(DuLinkList *L)\n{\n// 只申请头结点，不存数据\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n// 头结点也可能申请失败\n    if (*L == NULL) return ERROR;\n// 头结点的 prior 恒为 NULL，写出来是为了明确这条不变式\n    (*L)->prior = NULL;\n// 空表的标志：头结点的 next 为 NULL\n    (*L)->next = NULL;\n// 返回 OK 表示初始化成功\n    return OK;\n}\n\n\n// 参数 L 是头结点指针，不需要改 L 本身，所以不用二级指针\nStatus applist(DuLinkList L, ElemType e)\n{\n// p 是\"游标\"，从头结点开始；它最终会停在尾结点上\n    DuLNode *p = L;\n// s 用来接住 creatNode 造出来的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过，直接报错\n    if (L == NULL) return ERROR;\n\n// 一路向后走，直到 p 后面没有结点了\n    while (p->next != NULL)\n    {\n// 指针后移一格\n        p = p->next;\n    }\n\n// 造新结点（此时它的 prior 和 next 都是 NULL）\n    s = creatNode(e);\n\n// 第一步：新结点的 prior 回指旧的尾结点\n    s->prior = p;\n// 第二步：旧尾结点的 next 指向新结点\n    p->next = s;\n\n// 返回 OK 表示追加成功\n    return OK;\n}\n\n\n// 参数 L 是头结点指针，不需要改 L 本身\nStatus HeadInsert(DuLinkList L, ElemType e)\n{\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n\n// 防御：L 为空说明没初始化过\n    if (L == NULL) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// 第一步：新结点的 next 接住原来头结点后面的那一串\n    s->next = L->next;\n// 第二步：如果后面确实有结点，让它的 prior 改指新结点\n    if (s->next != NULL)\n    {\n        s->next->prior = s;\n    }\n// 第三步：新结点的 prior 指向头结点\n    s->prior = L;\n// 第四步：头结点指向新结点，s 正式成为第 1 个结点\n    L->next = s;\n\n// 插入成功\n    return OK;\n}\n\n\n// 结果通过 e 带回去，所以要传 ElemType *（地址），不是 ElemType\nStatus GetElem_L(DuLinkList L, int i, ElemType *e)\n{\n// 从第 1 个\"带数据的结点\"开始，所以是 L->next 而不是 L\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步，表示\"p 现在指的是第几个结点\"\n    int j = 1;\n\n// 边走边数，直到数到 i 或者走到表尾\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步加一\n        j++;\n    }\n\n// 越界检查：p 为空说明 i 超过表长；j > i 说明 i 小于 1\n    if (p == NULL || j > i) return ERROR;\n\n// 把找到的数据写到调用者提供的地址里\n    *e = p->data;\n\n// 查找成功\n    return OK;\n}\n\n\n// 返回 DuLNode *，找不到就是 NULL\nDuLNode *LocateElem(DuLinkList L, ElemType e)\n{\n// 同样从第一个带数据的结点开始\n    DuLNode *p = L->next;\n\n// 两个条件：还没走到尾，且当前值不等于目标\n    while (p != NULL && p->data != e)\n    {\n// 不匹配就继续往后\n        p = p->next;\n    }\n\n// 找到返回地址，没找到时 p 已经是 NULL，直接返回即可\n    return p;\n}\n\n\n// 要改的目标值通过 e 传进来（按值传递就够了，不需要指针）\nStatus ModifyElem(DuLinkList L, int i, ElemType e)\n{\n// 从第 1 个\"带数据的结点\"开始\n    DuLNode *p = L->next;\n// 计数器 j 与 p 同步\n    int j = 1;\n\n// 定位到第 i 个结点（与 GetElem_L 的循环完全相同）\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：第 i 个结点不存在就报错\n    if (p == NULL || j > i) return ERROR;\n\n// 把新值写进数据域 —— 只改数据，不碰任何指针\n    p->data = e;\n\n// 修改成功\n    return OK;\n}\n\n\n// 参数 L 是头结点指针；i 是位置，e 是数据\nStatus ListInsert(DuLinkList L, int i, ElemType e)\n{\n// p 要从头结点开始：因为插到第 1 位时，前驱就是头结点\n    DuLNode *p = L;\n// s 是等待挂链的新结点\n    DuLNode *s = NULL;\n// j 表示 p 当前指向第几个结点；头结点算第 0 个\n    int j = 0;\n\n// 走 i-1 步，让 p 停在\"第 i 个结点的前驱\"上\n    while (p != NULL && j < i - 1)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 合法性检查：p 为空说明 i-1 已经超过表长；j > i-1 说明 i < 1\n    if (p == NULL || j > i - 1) return ERROR;\n\n// 造新结点\n    s = creatNode(e);\n\n// ① 新结点的 next 接住后面的那一串\n    s->next = p->next;\n// ② 后面那个结点的 prior 改指新结点（表尾时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = s;\n    }\n// ③ 新结点的 prior 指向前驱（前驱一定存在，不用判空）\n    s->prior = p;\n// ④ 前驱的 next 指向新结点，链表正式接上\n    p->next = s;\n\n// 返回 OK 表示插入成功\n    return OK;\n}\n\n\n// 删除的数据要通过 *e 带出去\nStatus ListDelete(DuLinkList L, int i, ElemType *e)\n{\n// 注意：直接从第 1 个数据结点起步，目标是\"第 i 个结点自己\"\n    DuLNode *p = L->next;\n// j 与 p 同步，表示 p 是第几个结点\n    int j = 1;\n\n// 走 i 步，让 p 停在待删结点上\n    while (p != NULL && j < i)\n    {\n// 指针后移\n        p = p->next;\n// 计数器同步\n        j++;\n    }\n\n// 越界检查：p 为空说明第 i 个结点不存在\n    if (p == NULL || j > i) return ERROR;\n\n// 先把数据取出来交给调用者（必须在 free 之前做）\n    *e = p->data;\n\n// ① 前驱的后继越过 p\n    p->prior->next = p->next;\n// ② 后继的前驱越过 p（删的是尾结点时 p->next 为空，必须判空）\n    if (p->next != NULL)\n    {\n        p->next->prior = p->prior;\n    }\n// ③ 释放 p 占用的内存，防止内存泄漏\n    free(p);\n\n// 删除成功\n    return OK;\n}\n\n\n// 只读操作，参数直接传 DuLinkList（一级指针）\nvoid printList(DuLinkList L)\n{\n// 从第一个真实结点开始\n    DuLNode *p = L->next;\n\n// 空表单独处理，提示比\"什么都不打印\"清楚得多\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回，不再进入循环\n        return;\n    }\n\n// 标准遍历\n    while (p != NULL)\n    {\n// 打印当前数据，`⇄` 表示这里是双向连接\n        printf(\"%d ⇄ \", p->data);\n// 推进到下一个结点\n        p = p->next;\n    }\n\n// 收尾打印 NULL，标明链表末端\n    printf(\"NULL\\n\");\n}\n\n\n// 只读操作，不需要修改链表\nvoid printListReverse(DuLinkList L)\n{\n// 从头结点后面开始\n    DuLNode *p = L->next;\n\n// 空表同样单独处理\n    if (p == NULL)\n    {\n// 空表提示\n        printf(\"(空表)\\n\");\n// 直接返回\n        return;\n    }\n\n// 第一步：顺着 next 走到尾结点\n    while (p->next != NULL)\n    {\n// 指针后移\n        p = p->next;\n    }\n\n// 第二步：顺着 prior 往回走，撞到头结点就停\n    while (p != L)\n    {\n// 打印当前数据\n        printf(\"%d ⇄ \", p->data);\n// 顺着 prior 往回走一格（单链表没有这一步可走）\n        p = p->prior;\n    }\n\n// 收尾\n    printf(\"NULL\\n\");\n}\n\n\n// 参数是 DuLinkList *，因为最后要把调用者的 L 改成 NULL\nvoid freeList(DuLinkList *L)\n{\n// 从第一个真实结点开始清理\n    DuLNode *p = (*L)->next;\n// q 用来暂存\"即将被释放\"的那个结点\n    DuLNode *q = NULL;\n\n// 逐个结点释放\n    while (p != NULL)\n    {\n// 先记住当前结点\n        q = p;\n// 再让 p 走到下一个（此时 p 已经安全了）\n        p = p->next;\n// 最后才释放 q\n        free(q);\n    }\n\n// 数据结点都清完了，头结点也要还回去\n    free(*L);\n// 把调用者的指针置 NULL，避免它继续指向已释放的内存\n    *L = NULL;\n}\n\n\n// 主函数不需要参数，返回 0 表示正常结束\nint main(void)\n{\n// L 是头结点指针，先置 NULL，等 InitList 去创建头结点\n    DuLinkList L = NULL;\n// p 用来接 LocateElem 返回的结点地址\n    DuLNode *p = NULL;\n// e 用来接按位查找 / 删除带回来的数据\n    ElemType e = 0;\n\n// ========== 第 1 步：初始化 ==========\n    printf(\"========== 1. 初始化 ==========\\n\");\n// 检查返回值，初始化失败就没必要继续了\n    if (InitList(&L) != OK)\n    {\n// 失败提示\n        printf(\"初始化失败！\\n\");\n// 直接返回错误码\n        return ERROR;\n    }\n// 打印验证：此时应该是空表\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n// ========== 第 2 步：尾部追加 ==========\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n// 依次追加三个数\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n// 期望正向：10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"追加后（正向）：\");\n    printList(L);\n// 期望反向：30 ⇄ 20 ⇄ 10 ⇄ NULL\n    printf(\"追加后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 3 步：头插法 ==========\n    printf(\"\\n========== 3. 头插法插入 5 ==========\\n\");\n// 头插 5：不遍历，直接变成第 1 个结点\n    HeadInsert(L, 5);\n// 期望正向：5 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"头插 5 后（正向）：\");\n    printList(L);\n// 反向也要对：30 ⇄ 20 ⇄ 10 ⇄ 5 ⇄ NULL\n    printf(\"头插 5 后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 4 步：按位插入 ==========\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n// 插到第 2 个位置，即 5 和 10 之间\n    ListInsert(L, 2, 15);\n// 期望正向：5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"插入后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 5 ⇄ NULL\n    printf(\"插入后（反向）：\");\n    printListReverse(L);\n\n// 越界测试：在第 99 位插入必须失败\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 5 步：按位查找 ==========\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n// 查第 3 个元素，用 e 带回结果\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n// 期望输出：10\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n// 越界查找测试\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n// 期望走到这里\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n// ========== 第 6 步：按值查找 ==========\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n// 找值为 20 的结点\n    p = LocateElem(L, 20);\n// 找到就同时验证前后两个方向\n    if (p != NULL)\n    {\n// 双向链表的好处：拿到结点就能同时说出它的前驱和后继\n        printf(\"找到 20：它的前驱是 %d，后继是 %s\\n\",\n               p->prior->data,\n               p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n// 找不存在的值应当返回 NULL\n    if (LocateElem(L, 99) == NULL)\n    {\n// 期望走到这里\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n// ========== 第 7 步：按位修改 ==========\n    printf(\"\\n========== 7. 按位修改 ==========\\n\");\n// 把第 1 个元素改成 99\n    if (ModifyElem(L, 1, 99) == OK)\n    {\n// 期望正向：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n        printf(\"把第 1 个元素改成 99 后：\");\n        printList(L);\n    }\n// 越界修改测试\n    printf(\"改第 100 个元素：%s\\n\", ModifyElem(L, 100, 1) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 8 步：正反向遍历对照 ==========\n    printf(\"\\n========== 8. 正反向遍历对照 ==========\\n\");\n// 同一份链表，两个方向必须互为倒序\n    printf(\"正向：\");\n    printList(L);\n    printf(\"反向：\");\n    printListReverse(L);\n\n// ========== 第 9 步：按位删除 ==========\n    printf(\"\\n========== 9. 删除第 2 个结点 ==========\\n\");\n// 删除第 2 个结点（值为 15），被删的值通过 e 带出来\n    if (ListDelete(L, 2, &e) == OK)\n    {\n// 期望输出：被删除的是 15\n        printf(\"已删除第 2 个结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n    printf(\"删除后（正向）：\");\n    printList(L);\n// 反向核对：30 ⇄ 20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除后（反向）：\");\n    printListReverse(L);\n\n// 越界删除测试\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n// ========== 第 10 步：删尾结点（专测判空分支）==========\n    printf(\"\\n========== 10. 删除尾结点 ==========\\n\");\n// 此时链表是 99 ⇄ 10 ⇄ 20 ⇄ 30，共 4 个结点\n    if (ListDelete(L, 4, &e) == OK)\n    {\n        printf(\"已删除尾结点，它的值是 %d\\n\", e);\n    }\n// 期望正向：99 ⇄ 10 ⇄ 20 ⇄ NULL\n    printf(\"删除尾结点后（正向）：\");\n    printList(L);\n// 反向核对：20 ⇄ 10 ⇄ 99 ⇄ NULL\n    printf(\"删除尾结点后（反向）：\");\n    printListReverse(L);\n\n// ========== 第 11 步：释放整表 ==========\n    printf(\"\\n========== 11. 释放内存 ==========\\n\");\n// 释放所有结点，并把 L 置为 NULL\n    freeList(&L);\n// 期望输出：NULL，证明 L 已被正确置空\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n// 正常结束\n    return 0;\n}\n","none":"\n#include <stdio.h>\n#include <stdlib.h>\n\n#define OK 1\n#define ERROR 0\n#define TRUE 1\n#define FALSE 0\n#define OVERFLOW -2\n\ntypedef int Status;\ntypedef int ElemType;\n\ntypedef struct DuLNode {\n    ElemType data;\n    struct DuLNode *prior;\n    struct DuLNode *next;\n} DuLNode, *DuLinkList;\n\n\nDuLNode *creatNode(ElemType e)\n{\n    DuLNode *p = (DuLNode *)malloc(sizeof(DuLNode));\n\n    if (p == NULL)\n    {\n        printf(\"内存分配失败：无法创建新结点。\\n\");\n        exit(OVERFLOW);\n    }\n\n    p->data = e;\n    p->prior = NULL;\n    p->next = NULL;\n\n    return p;\n}\n\n\nStatus InitList(DuLinkList *L)\n{\n    *L = (DuLinkList)malloc(sizeof(DuLNode));\n    if (*L == NULL) return ERROR;\n    (*L)->prior = NULL;\n    (*L)->next = NULL;\n    return OK;\n}\n\n\nStatus applist(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    s = creatNode(e);\n\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n\nStatus HeadInsert(DuLinkList L, ElemType e)\n{\n    DuLNode *s = NULL;\n\n    if (L == NULL) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = L->next;\n    if (s->next != NULL)\n    {\n        s->next->prior = s;\n    }\n    s->prior = L;\n    L->next = s;\n\n    return OK;\n}\n\n\nStatus GetElem_L(DuLinkList L, int i, ElemType *e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    return OK;\n}\n\n\nDuLNode *LocateElem(DuLinkList L, ElemType e)\n{\n    DuLNode *p = L->next;\n\n    while (p != NULL && p->data != e)\n    {\n        p = p->next;\n    }\n\n    return p;\n}\n\n\nStatus ModifyElem(DuLinkList L, int i, ElemType e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    p->data = e;\n\n    return OK;\n}\n\n\nStatus ListInsert(DuLinkList L, int i, ElemType e)\n{\n    DuLNode *p = L;\n    DuLNode *s = NULL;\n    int j = 0;\n\n    while (p != NULL && j < i - 1)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i - 1) return ERROR;\n\n    s = creatNode(e);\n\n    s->next = p->next;\n    if (p->next != NULL)\n    {\n        p->next->prior = s;\n    }\n    s->prior = p;\n    p->next = s;\n\n    return OK;\n}\n\n\nStatus ListDelete(DuLinkList L, int i, ElemType *e)\n{\n    DuLNode *p = L->next;\n    int j = 1;\n\n    while (p != NULL && j < i)\n    {\n        p = p->next;\n        j++;\n    }\n\n    if (p == NULL || j > i) return ERROR;\n\n    *e = p->data;\n\n    p->prior->next = p->next;\n    if (p->next != NULL)\n    {\n        p->next->prior = p->prior;\n    }\n    free(p);\n\n    return OK;\n}\n\n\nvoid printList(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p != NULL)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->next;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n\nvoid printListReverse(DuLinkList L)\n{\n    DuLNode *p = L->next;\n\n    if (p == NULL)\n    {\n        printf(\"(空表)\\n\");\n        return;\n    }\n\n    while (p->next != NULL)\n    {\n        p = p->next;\n    }\n\n    while (p != L)\n    {\n        printf(\"%d ⇄ \", p->data);\n        p = p->prior;\n    }\n\n    printf(\"NULL\\n\");\n}\n\n\nvoid freeList(DuLinkList *L)\n{\n    DuLNode *p = (*L)->next;\n    DuLNode *q = NULL;\n\n    while (p != NULL)\n    {\n        q = p;\n        p = p->next;\n        free(q);\n    }\n\n    free(*L);\n    *L = NULL;\n}\n\n\nint main(void)\n{\n    DuLinkList L = NULL;\n    DuLNode *p = NULL;\n    ElemType e = 0;\n\n    printf(\"========== 1. 初始化 ==========\\n\");\n    if (InitList(&L) != OK)\n    {\n        printf(\"初始化失败！\\n\");\n        return ERROR;\n    }\n    printf(\"初始化完成，当前链表：\");\n    printList(L);\n\n    printf(\"\\n========== 2. 尾部追加 10 20 30 ==========\\n\");\n    applist(L, 10);\n    applist(L, 20);\n    applist(L, 30);\n    printf(\"追加后（正向）：\");\n    printList(L);\n    printf(\"追加后（反向）：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 3. 头插法插入 5 ==========\\n\");\n    HeadInsert(L, 5);\n    printf(\"头插 5 后（正向）：\");\n    printList(L);\n    printf(\"头插 5 后（反向）：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 4. 在第 2 位插入 15 ==========\\n\");\n    ListInsert(L, 2, 15);\n    printf(\"插入后（正向）：\");\n    printList(L);\n    printf(\"插入后（反向）：\");\n    printListReverse(L);\n\n    printf(\"在第 99 位插入 5 ：%s\\n\", ListInsert(L, 99, 5) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 5. 按位查找 ==========\\n\");\n    if (GetElem_L(L, 3, &e) == OK)\n    {\n        printf(\"第 3 个元素的值是 %d\\n\", e);\n    }\n    if (GetElem_L(L, 100, &e) == ERROR)\n    {\n        printf(\"查第 100 个元素失败（越界，符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 6. 按值查找 ==========\\n\");\n    p = LocateElem(L, 20);\n    if (p != NULL)\n    {\n        printf(\"找到 20：它的前驱是 %d，后继是 %s\\n\",\n               p->prior->data,\n               p->next != NULL ? \"一个有效结点\" : \"NULL\");\n    }\n    if (LocateElem(L, 99) == NULL)\n    {\n        printf(\"查找值 99 返回 NULL（符合预期）\\n\");\n    }\n\n    printf(\"\\n========== 7. 按位修改 ==========\\n\");\n    if (ModifyElem(L, 1, 99) == OK)\n    {\n        printf(\"把第 1 个元素改成 99 后：\");\n        printList(L);\n    }\n    printf(\"改第 100 个元素：%s\\n\", ModifyElem(L, 100, 1) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 8. 正反向遍历对照 ==========\\n\");\n    printf(\"正向：\");\n    printList(L);\n    printf(\"反向：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 9. 删除第 2 个结点 ==========\\n\");\n    if (ListDelete(L, 2, &e) == OK)\n    {\n        printf(\"已删除第 2 个结点，它的值是 %d\\n\", e);\n    }\n    printf(\"删除后（正向）：\");\n    printList(L);\n    printf(\"删除后（反向）：\");\n    printListReverse(L);\n\n    printf(\"删除第 10 个结点：%s\\n\", ListDelete(L, 10, &e) == OK ? \"成功\" : \"失败（越界，符合预期）\");\n\n    printf(\"\\n========== 10. 删除尾结点 ==========\\n\");\n    if (ListDelete(L, 4, &e) == OK)\n    {\n        printf(\"已删除尾结点，它的值是 %d\\n\", e);\n    }\n    printf(\"删除尾结点后（正向）：\");\n    printList(L);\n    printf(\"删除尾结点后（反向）：\");\n    printListReverse(L);\n\n    printf(\"\\n========== 11. 释放内存 ==========\\n\");\n    freeList(&L);\n    printf(\"释放完成，L = %s\\n\", L == NULL ? \"NULL\" : \"非 NULL\");\n\n    return 0;\n}\n"},"scaffold":null,"expectedOutput":"========== 1. 初始化 ==========\n初始化完成，当前链表：(空表)\n\n========== 2. 尾部追加 10 20 30 ==========\n追加后（正向）：10 ⇄ 20 ⇄ 30 ⇄ NULL\n追加后（反向）：30 ⇄ 20 ⇄ 10 ⇄ NULL\n\n========== 3. 头插法插入 5 ==========\n头插 5 后（正向）：5 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n头插 5 后（反向）：30 ⇄ 20 ⇄ 10 ⇄ 5 ⇄ NULL\n\n========== 4. 在第 2 位插入 15 ==========\n插入后（正向）：5 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n插入后（反向）：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 5 ⇄ NULL\n在第 99 位插入 5 ：失败（越界，符合预期）\n\n========== 5. 按位查找 ==========\n第 3 个元素的值是 10\n查第 100 个元素失败（越界，符合预期）\n\n========== 6. 按值查找 ==========\n找到 20：它的前驱是 10，后继是 一个有效结点\n查找值 99 返回 NULL（符合预期）\n\n========== 7. 按位修改 ==========\n把第 1 个元素改成 99 后：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n改第 100 个元素：失败（越界，符合预期）\n\n========== 8. 正反向遍历对照 ==========\n正向：99 ⇄ 15 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n反向：30 ⇄ 20 ⇄ 10 ⇄ 15 ⇄ 99 ⇄ NULL\n\n========== 9. 删除第 2 个结点 ==========\n已删除第 2 个结点，它的值是 15\n删除后（正向）：99 ⇄ 10 ⇄ 20 ⇄ 30 ⇄ NULL\n删除后（反向）：30 ⇄ 20 ⇄ 10 ⇄ 99 ⇄ NULL\n删除第 10 个结点：失败（越界，符合预期）\n\n========== 10. 删除尾结点 ==========\n已删除尾结点，它的值是 30\n删除尾结点后（正向）：99 ⇄ 10 ⇄ 20 ⇄ NULL\n删除尾结点后（反向）：20 ⇄ 10 ⇄ 99 ⇄ NULL\n\n========== 11. 释放内存 ==========\n释放完成，L = NULL"}}},"modules":null}