{"section":"02-01","views":null,"modules":{"02-01-01":{"modes":{"detail":"// ============ 顺序表长什么样 ============\n//\n//   下标   0    1    2    3    4    5\n//        ┌────┬────┬────┬────┬────┐\n//   data │ 10 │ 20 │ 30 │    │    │\n//        └────┴────┴────┴────┴────┘\n//   length = 3\n//\n// 两个关键点：\n//\n//   1. 元素挨着放 —— 所以能\"按下标随机访问\"。想拿第 3 个，直接 data[2]，\n//      一步到位，不需要从头数。这是链表做不到的（链表得走 3 步）。\n//\n//   2. length 记录**实际有几个元素**，它和 MAXSIZE（**最多能放几个**）\n//      完全是两回事。表里只有 3 个元素，但数组开了 100 个格子 ——\n//      后面那些格子里的内容是垃圾值，永远不要去看它们。\n\n// 元素类型：想换成 char / double，只要改这一行\ntypedef int ElemType;\n\n// 最多能放多少个元素\n#define MAXSIZE 100\n\n// 函数返回状态：OK 表示成功，ERROR 表示失败\n// 用宏而不是 enum，是为了和教材写法保持一致\n#define OK 1\n#define ERROR 0\n\n// 返回值类型\ntypedef int Status;\n\n// 顺序表 = 定长数组 + 当前长度\n// 注意 length 不是 MAXSIZE：它随插入删除变化，是这张表的\"实际尺寸\"\ntypedef struct\n{\n// 连续内存，元素一个挨一个\n    ElemType data[MAXSIZE];\n// 当前实际元素个数\n    int length;\n} SeqList;","short":"\n// 元素类型：想换成 char / double，只要改这一行\ntypedef int ElemType;\n\n// 最多能放多少个元素\n#define MAXSIZE 100\n\n// 函数返回状态：OK 表示成功，ERROR 表示失败\n#define OK 1\n#define ERROR 0\n\n// 返回值类型\ntypedef int Status;\n\n// 顺序表 = 定长数组 + 当前长度\ntypedef struct\n{\n// 连续内存，元素一个挨一个\n    ElemType data[MAXSIZE];\n// 当前实际元素个数\n    int length;\n} SeqList;","none":"\ntypedef int ElemType;\n\n#define MAXSIZE 100\n\n#define OK 1\n#define ERROR 0\n\ntypedef int Status;\n\ntypedef struct\n{\n    ElemType data[MAXSIZE];\n    int length;\n} SeqList;"},"scaffold":"#include <stdio.h>\n\n/* ====================================================================\n *  轮到你了：模块 01  typedef\n *  作用：顺序表 = 一段连续内存 + 一个 length。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证结构定义：能装的格子上限是 MAXSIZE，实际有几个看 length */\nint main(void)\n{\n    SeqList L;\n\n    /* 这个模块只讲\"结构长什么样\"，所以不调用 InitList_Sq ——\n       那是模块 02 的事，这里直接给 length 赋值就够了，\n       也免得脚手架里出现\"用了后面的模块\"这种反向依赖 */\n    L.length = 0;\n\n    printf(\"MAXSIZE = %d\\n\", MAXSIZE);\n    printf(\"sizeof(SeqList) = %d 字节\\n\", (int)sizeof(SeqList));\n    printf(\"length = %d\\n\", L.length);\n    printf(\"（关键：MAXSIZE 是能装多少，length 是实际有几个）\\n\");\n\n    return 0;\n}\n","expectedOutput":"MAXSIZE = 100\nsizeof(SeqList) = 404 字节\nlength = 0\n（关键：MAXSIZE 是能装多少，length 是实际有几个）"},"02-01-02":{"modes":{"detail":"// ============ 为什么不需要清空数组 ============\n//\n// 新手常写一个循环把 data[0..MAXSIZE-1] 全赋 0，其实完全没必要。\n//\n// 因为\"表里有几个元素\"是 **length 说了算**，而不是数组里有没有值。\n// length = 0 就意味着\"这是张空表\"，后面格子里的垃圾值谁也看不见。\n//\n// 反过来，如果只清了数组却忘了 length = 0，那才是真的错。\n\n// 参数是指针：要改调用者手里那张表本身，所以必须传地址\n// 写成 SeqList *L 而不是 SeqList L，因为\"初始化\"要修改 L 的 length。\n// 传值的话改的是副本，调用者那边毫无变化 —— 这是个高频错误。\nStatus InitList_Sq(SeqList *L)\n{\n// 空表：一个元素都没有 —— 这一句就够了\n    L->length = 0;\n// 约定：返回 OK 表示初始化成功\n    return OK;\n}","short":"\n// 参数是指针：要改调用者手里那张表本身，所以必须传地址\nStatus InitList_Sq(SeqList *L)\n{\n// 空表：一个元素都没有 —— 这一句就够了\n    L->length = 0;\n// 约定：返回 OK 表示初始化成功\n    return OK;\n}","none":"\nStatus InitList_Sq(SeqList *L)\n{\n    L->length = 0;\n    return OK;\n}"},"scaffold":"#include <stdio.h>\n\n/* ======== [已给出] 模块 01 typedef ======== */\n\ntypedef int ElemType;\n\n#define MAXSIZE 100\n\n#define OK 1\n#define ERROR 0\n\ntypedef int Status;\n\ntypedef struct\n{\n    ElemType data[MAXSIZE];\n    int length;\n} SeqList;\n\n/* ====================================================================\n *  轮到你了：模块 02  InitList_Sq\n *  作用：只把 length 置 0 —— 一格内存都不用清。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 InitList_Sq：初始化后 length 必须是 0 */\nint main(void)\n{\n    SeqList L;\n\n    L.length = 999;                 /* 先故意弄脏 */\n    InitList_Sq(&L);\n    printf(\"初始化后 length = %d\\n\", L.length);\n    printf(\"（关键：初始化不是清空数组，而是把 length 归零）\\n\");\n\n    return 0;\n}\n","expectedOutput":"初始化后 length = 0\n（关键：初始化不是清空数组，而是把 length 归零）"},"02-01-03":{"modes":{"detail":"// ============ 插入要\"搬家\" ============\n// 在位置 i 插入 e，意味着 e 要占住下标 i-1 那个格子。\n// 但那个格子原本有元素，所以它以及它后面所有的元素都得往后挪一格：\n//\n//   插入前： 10  20  30  _   _        （length = 3，在下标 1 处插入 15）\n//             0   1   2   3   4\n//\n//   后移：   10  20  __  30  _        ← 30 从下标 2 挪到 3\n//                └── 20 从下标 1 挪到 2\n//\n//   填入：   10  15  20  30  _        ← e 放进腾出来的下标 1\n//   length = 4\n//\n// ============ 为什么必须从后往前搬 ============\n//\n//   正确（j 从 length 递减到 i）：先搬最右边的，空位一路向左让出来\n//       data[3] = data[2]   →  data[2] = data[1]   →  data[1] = e\n//\n//   错误（j 从 i 递增到 length）：先搬左边的，会把右边的值覆盖掉\n//       data[1] = data[0]   →  data[2] = data[1]   ← 这里 data[1] 已经是 10 了！\n//\n// 一句话记住：**搬家永远从\"没人占的那一头\"开始搬**。\n\n// L 是要改的表，i 是插入位置（1 基，不是下标），e 是待插入元素\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length+1 都算合法\n// 为什么是 length+1？因为\"插在最后一个元素后面\"是允许的。\n// 长度为 3 时，合法位置是 1、2、3、4；i=5 就越界了。\n// 注意这里用的是 length 而不是 MAXSIZE —— 前者是\"有几个元素\"，\n// 后者是\"能装几个\"，判断位置用前者。\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n// 表满了也插不进去\n// 这是顺序表天生的局限：容量是固定的，满了只能报错（或者扩容搬迁）。\n// 链表没有这个问题 —— 内存够就能一直加。\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n// 从最后一个元素开始，一个个往后挪一格\n// j 从 length 递减到 i：注意 data[length] 这个格子本身是\"空\"的\n// （不在 length 计数范围内），所以第一次赋值刚好把最后那个元素挪进空位。\n// 循环结束时，下标 i-1 那个格子已经被腾出来了。\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n// 把 e 填进腾出来的空位\n// 存的是下标，所以位置 i 对应 data[i-1]。这个 ±1 的转换是初学时\n// 最容易写错的地方，建议在纸上画一遍。\n    L->data[i - 1] = e;\n\n// 元素多了一个\n    L->length++;\n\n// 插入成功\n    return OK;\n}","short":"\n// L 是要改的表，i 是插入位置（1 基，不是下标），e 是待插入元素\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length+1 都算合法\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n// 表满了也插不进去\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n// 从最后一个元素开始，一个个往后挪一格\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n// 把 e 填进腾出来的空位\n    L->data[i - 1] = e;\n\n// 元素多了一个\n    L->length++;\n\n// 插入成功\n    return OK;\n}","none":"\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n    int j;\n\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n    L->data[i - 1] = e;\n\n    L->length++;\n\n    return OK;\n}"},"scaffold":"#include <stdio.h>\n\n/* ======== [已给出] 模块 01 typedef ======== */\n\ntypedef int ElemType;\n\n#define MAXSIZE 100\n\n#define OK 1\n#define ERROR 0\n\ntypedef int Status;\n\ntypedef struct\n{\n    ElemType data[MAXSIZE];\n    int length;\n} SeqList;\n\n/* ======== [已给出] 模块 02 InitList_Sq ======== */\n\nStatus InitList_Sq(SeqList *L)\n{\n    L->length = 0;\n    return OK;\n}\n\n/* ====================================================================\n *  轮到你了：模块 03  ListInsert_Sq\n *  作用：先把第 i 个及以后的元素统统后移一位，腾出空位再填进去。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 ListInsert_Sq：插入后 length 加一，且顺序正确 */\nint main(void)\n{\n    SeqList L;\n    int i;\n\n    InitList_Sq(&L);\n    for (i = 1; i <= 5; i++)\n    {\n        ListInsert_Sq(&L, i, i * 10);\n    }\n    printf(\"尾部追加 5 个后 length = %d\\n\", L.length);\n\n    /* 在下标 2（位序 3）插入 25，观察后移 */\n    ListInsert_Sq(&L, 3, 25);\n    printf(\"在位序 3 插入 25 后 length = %d\\n\", L.length);\n    printf(\"数据: \");\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n\n    /* 越界与满表 */\n    printf(\"在位序 0 插入 -> %s\\n\", ListInsert_Sq(&L, 0, 1) == ERROR ? \"被拒绝\" : \"竟然成功了\");\n    printf(\"在位序 %d 插入 -> %s\\n\", L.length + 2, ListInsert_Sq(&L, L.length + 2, 1) == ERROR ? \"被拒绝\" : \"竟然成功了\");\n\n    return 0;\n}\n","expectedOutput":"尾部追加 5 个后 length = 5\n在位序 3 插入 25 后 length = 6\n数据: 10 20 25 30 40 50 \n在位序 0 插入 -> 被拒绝\n在位序 8 插入 -> 被拒绝"},"02-01-04":{"modes":{"detail":"// ============ 删除也要\"搬家\"，方向相反 ============\n//\n//   删除前： 10  15  20  30  _        （length = 4，删除下标 1 的 15）\n//             0   1   2   3   4\n//\n//   前移：   10  20  30  __  _        ← 20 从下标 2 挪到 1，30 从 3 挪到 2\n//   length = 3\n//\n// 和插入正好相反：**删除要从前往后搬**。\n// 因为这次空位在左边，得让右边的元素一个个往左填。\n// 如果从后往前搬，就会把还没搬的元素覆盖掉。\n\n// e 是\"带回被删元素\"的出口参数 —— 调用者想知道删掉的是谁\nStatus ListDelete_Sq(SeqList *L, int i, ElemType *e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length\n// 注意和插入的区别：这里上限是 length 而不是 length+1。\n// 因为删除的是\"已经存在的元素\"，而长度为 3 的表里只存在 1、2、3 三个位置。\n    if (i < 1 || i > L->length)\n    {\n        return ERROR;\n    }\n\n// 先把被删元素取出来 —— 必须在搬家之前做\n// 因为它马上就要被后面的元素覆盖掉了，晚一步就取不到了。\n    *e = L->data[i - 1];\n\n// 从被删位置开始，后面的元素统统往前挪一格\n// j 从 i 递增到 length-1：把 data[j] 搬到 data[j-1]。\n// 循环结束后，最后一个位置 data[length-1] 的内容虽然还在，\n// 但已经不在 length 计数范围内，等于被抛弃了。\n    for (j = i; j < L->length; j++)\n    {\n        L->data[j - 1] = L->data[j];\n    }\n\n// 元素少了一个\n    L->length--;\n\n// 删除成功\n    return OK;\n}","short":"\n// e 是\"带回被删元素\"的出口参数 —— 调用者想知道删掉的是谁\nStatus ListDelete_Sq(SeqList *L, int i, ElemType *e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length\n    if (i < 1 || i > L->length)\n    {\n        return ERROR;\n    }\n\n// 先把被删元素取出来 —— 必须在搬家之前做\n    *e = L->data[i - 1];\n\n// 从被删位置开始，后面的元素统统往前挪一格\n    for (j = i; j < L->length; j++)\n    {\n        L->data[j - 1] = L->data[j];\n    }\n\n// 元素少了一个\n    L->length--;\n\n// 删除成功\n    return OK;\n}","none":"\nStatus ListDelete_Sq(SeqList *L, int i, ElemType *e)\n{\n    int j;\n\n    if (i < 1 || i > L->length)\n    {\n        return ERROR;\n    }\n\n    *e = L->data[i - 1];\n\n    for (j = i; j < L->length; j++)\n    {\n        L->data[j - 1] = L->data[j];\n    }\n\n    L->length--;\n\n    return OK;\n}"},"scaffold":"#include <stdio.h>\n\n/* ======== [已给出] 模块 01 typedef ======== */\n\ntypedef int ElemType;\n\n#define MAXSIZE 100\n\n#define OK 1\n#define ERROR 0\n\ntypedef int Status;\n\ntypedef struct\n{\n    ElemType data[MAXSIZE];\n    int length;\n} SeqList;\n\n/* ======== [已给出] 模块 02 InitList_Sq ======== */\n\nStatus InitList_Sq(SeqList *L)\n{\n    L->length = 0;\n    return OK;\n}\n\n/* ======== [已给出] 模块 03 ListInsert_Sq ======== */\n\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n    int j;\n\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n    L->data[i - 1] = e;\n\n    L->length++;\n\n    return OK;\n}\n\n/* ====================================================================\n *  轮到你了：模块 04  ListDelete_Sq\n *  作用：把第 i 个元素取走后，后面的元素统统前移一位把空位填上。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 ListDelete_Sq：删除后 length 减一，被删元素通过指针带回 */\nint main(void)\n{\n    SeqList L;\n    ElemType e;\n    int i;\n\n    InitList_Sq(&L);\n    for (i = 1; i <= 5; i++)\n    {\n        ListInsert_Sq(&L, i, i * 10);\n    }\n    printf(\"删除前 length = %d\\n\", L.length);\n\n    ListDelete_Sq(&L, 1, &e);\n    printf(\"删除位序 1 -> 拿到 %d\\n\", e);\n    ListDelete_Sq(&L, L.length, &e);\n    printf(\"删除最后一个 -> 拿到 %d\\n\", e);\n    printf(\"删除后 length = %d\\n\", L.length);\n    printf(\"数据: \");\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n\n    printf(\"删空表 -> %s\\n\", ListDelete_Sq(&L, 1, &e) == ERROR ? \"被拒绝\" : \"竟然成功了\");\n\n    return 0;\n}\n","expectedOutput":"删除前 length = 5\n删除位序 1 -> 拿到 10\n删除最后一个 -> 拿到 50\n删除后 length = 3\n数据: 20 30 40 \n删空表 -> 竟然成功了"},"02-01-05":{"modes":{"detail":"// ============ 顺序查找 ============\n//\n//   找 30： 10  20  30  40\n//           ↑   ↑   ↑\n//           比  比  中 ✓        比较 3 次\n//\n// 这叫\"顺序查找\"，平均要比较 (n+1)/2 次，时间复杂度 O(n)。\n//\n// 如果这张表是**有序的**，可以改用二分查找，降到 O(log n)。\n// 但二分要求\"能按下标跳跃访问\"—— 这恰好是顺序表的强项，\n// 链表就做不了（链表中途没法直接跳到中间）。\n\n// 返回\"位序\"（1 基），不是下标；找不到返回 0\n// 为什么返回位序而不是下标？因为调用者拿到 0 就知道\"没找到\"，\n// 而下标 0 是合法位置，用它当\"没找到\"会分不清。\nint LocateElem_Sq(SeqList L, ElemType e)\n{\n// 循环变量\n    int i;\n\n// 从下标 0 扫到 length-1 —— 只扫\"有效元素\"那一段\n// 一定用 length 做上界，不要用 MAXSIZE：\n// 后面那些格子是垃圾值，扫进去可能\"莫名奇妙找到\"一个相等的东西。\n    for (i = 0; i < L.length; i++)\n    {\n// 相等就返回它的位序（下标 +1）\n        if (L.data[i] == e)\n        {\n            return i + 1;\n        }\n    }\n\n// 扫完了还没有，就是没有\n    return 0;\n}","short":"\n// 返回\"位序\"（1 基），不是下标；找不到返回 0\nint LocateElem_Sq(SeqList L, ElemType e)\n{\n// 循环变量\n    int i;\n\n// 从下标 0 扫到 length-1 —— 只扫\"有效元素\"那一段\n    for (i = 0; i < L.length; i++)\n    {\n// 相等就返回它的位序（下标 +1）\n        if (L.data[i] == e)\n        {\n            return i + 1;\n        }\n    }\n\n// 扫完了还没有，就是没有\n    return 0;\n}","none":"\nint LocateElem_Sq(SeqList L, ElemType e)\n{\n    int i;\n\n    for (i = 0; i < L.length; i++)\n    {\n        if (L.data[i] == e)\n        {\n            return i + 1;\n        }\n    }\n\n    return 0;\n}"},"scaffold":"#include <stdio.h>\n\n/* ======== [已给出] 模块 01 typedef ======== */\n\ntypedef int ElemType;\n\n#define MAXSIZE 100\n\n#define OK 1\n#define ERROR 0\n\ntypedef int Status;\n\ntypedef struct\n{\n    ElemType data[MAXSIZE];\n    int length;\n} SeqList;\n\n/* ======== [已给出] 模块 02 InitList_Sq ======== */\n\nStatus InitList_Sq(SeqList *L)\n{\n    L->length = 0;\n    return OK;\n}\n\n/* ======== [已给出] 模块 03 ListInsert_Sq ======== */\n\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n    int j;\n\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n    L->data[i - 1] = e;\n\n    L->length++;\n\n    return OK;\n}\n\n/* ====================================================================\n *  轮到你了：模块 05  LocateElem_Sq\n *  作用：从头挨个比，返回第一个相等元素的位序；找不到返回 0。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n\n/* ======== 测试驱动（已给出，可自行修改） ======== */\n/* 验证 LocateElem_Sq：找到返回位序，找不到返回 0 */\nint main(void)\n{\n    SeqList L;\n    int r;\n    int i;\n\n    InitList_Sq(&L);\n    for (i = 1; i <= 5; i++)\n    {\n        ListInsert_Sq(&L, i, i * 10);\n    }\n\n    r = LocateElem_Sq(L, 30);\n    printf(\"找 30 -> 位序 %d\\n\", r);\n    r = LocateElem_Sq(L, 10);\n    printf(\"找 10 -> 位序 %d\\n\", r);\n    r = LocateElem_Sq(L, 50);\n    printf(\"找 50 -> 位序 %d\\n\", r);\n    r = LocateElem_Sq(L, 99);\n    printf(\"找 99 -> 位序 %d（0 表示没找到）\\n\", r);\n\n    return 0;\n}\n","expectedOutput":"找 30 -> 位序 3\n找 10 -> 位序 1\n找 50 -> 位序 5\n找 99 -> 位序 0（0 表示没找到）"},"02-01-06":{"modes":{"detail":"// ============ 为什么要\"一步一打印\" ============\n//\n// 顺序表的 bug 大多不是崩溃，而是**悄悄出错**：\n// 元素顺序错了、length 忘了加、搬家方向反了 —— 程序照跑不误，\n// 只是数据不对。所以每一步都打印出来，肉眼一比对就露馅了。\n\n// 打印辅助函数放在 main 之前\n// 它不属于要默写的核心操作，所以直接给出、不用自己写\nstatic void PrintList(const char *tag, SeqList L)\n{\n    int i;\n\n    printf(\"%s length=%d : \", tag, L.length);\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n}\n\n// 主函数\nint main(void)\n{\n// 定义一张表\n    SeqList L;\n// 用来接住被删除的元素\n    ElemType e;\n// 查找结果（位序）\n    int pos;\n\n// 先初始化 —— 别忘了，否则 length 是随机值\n    InitList_Sq(&L);\n\n// 依次在表尾追加 10、20、40\n// 插到 length+1 的位置就是\"追加到末尾\"，每次只搬家 0 次，是最省的情形。\n    ListInsert_Sq(&L, 1, 10);\n    ListInsert_Sq(&L, 2, 20);\n    ListInsert_Sq(&L, 3, 40);\n    PrintList(\"追加 10 20 40 后:\", L);\n\n// 在下标 2（位序 3）处插入 30，让表变成有序的\n// 这一次要搬家 1 个元素（40 后移），刚好演示\"插入要挪位置\"。\n    ListInsert_Sq(&L, 3, 30);\n    PrintList(\"在下标 2 处插入 30:\", L);\n\n// 查找 30 在哪\n    pos = LocateElem_Sq(L, 30);\n    printf(\"查找 30 -> 位序 %d（下标 %d）\\n\", pos, pos - 1);\n\n// 查找一个不存在的值\n    pos = LocateElem_Sq(L, 99);\n    printf(\"查找 99 -> 位序 %d（0 表示没找到）\\n\", pos);\n\n// 删除位序 2 的元素，看它是不是 20\n// 这次要搬家 2 个元素（30、40 前移），演示\"删除要往前填\"。\n    ListDelete_Sq(&L, 2, &e);\n    printf(\"删除位序 2 -> 拿到的元素是 %d\\n\", e);\n    PrintList(\"删除后:\", L);\n\n// 越界插入应该被挡住\n    if (ListInsert_Sq(&L, 99, 7) == ERROR)\n    {\n        printf(\"在位序 99 插入 -> 被拒绝（越界）\\n\");\n    }\n    PrintList(\"越界插入后:\", L);\n\n// 正常结束\n    return 0;\n}","short":"\n// 打印辅助函数放在 main 之前\nstatic void PrintList(const char *tag, SeqList L)\n{\n    int i;\n\n    printf(\"%s length=%d : \", tag, L.length);\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n}\n\n// 主函数\nint main(void)\n{\n// 定义一张表\n    SeqList L;\n// 用来接住被删除的元素\n    ElemType e;\n// 查找结果（位序）\n    int pos;\n\n// 先初始化 —— 别忘了，否则 length 是随机值\n    InitList_Sq(&L);\n\n// 依次在表尾追加 10、20、40\n    ListInsert_Sq(&L, 1, 10);\n    ListInsert_Sq(&L, 2, 20);\n    ListInsert_Sq(&L, 3, 40);\n    PrintList(\"追加 10 20 40 后:\", L);\n\n// 在下标 2（位序 3）处插入 30，让表变成有序的\n    ListInsert_Sq(&L, 3, 30);\n    PrintList(\"在下标 2 处插入 30:\", L);\n\n// 查找 30 在哪\n    pos = LocateElem_Sq(L, 30);\n    printf(\"查找 30 -> 位序 %d（下标 %d）\\n\", pos, pos - 1);\n\n// 查找一个不存在的值\n    pos = LocateElem_Sq(L, 99);\n    printf(\"查找 99 -> 位序 %d（0 表示没找到）\\n\", pos);\n\n// 删除位序 2 的元素，看它是不是 20\n    ListDelete_Sq(&L, 2, &e);\n    printf(\"删除位序 2 -> 拿到的元素是 %d\\n\", e);\n    PrintList(\"删除后:\", L);\n\n// 越界插入应该被挡住\n    if (ListInsert_Sq(&L, 99, 7) == ERROR)\n    {\n        printf(\"在位序 99 插入 -> 被拒绝（越界）\\n\");\n    }\n    PrintList(\"越界插入后:\", L);\n\n// 正常结束\n    return 0;\n}","none":"\nstatic void PrintList(const char *tag, SeqList L)\n{\n    int i;\n\n    printf(\"%s length=%d : \", tag, L.length);\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n}\n\nint main(void)\n{\n    SeqList L;\n    ElemType e;\n    int pos;\n\n    InitList_Sq(&L);\n\n    ListInsert_Sq(&L, 1, 10);\n    ListInsert_Sq(&L, 2, 20);\n    ListInsert_Sq(&L, 3, 40);\n    PrintList(\"追加 10 20 40 后:\", L);\n\n    ListInsert_Sq(&L, 3, 30);\n    PrintList(\"在下标 2 处插入 30:\", L);\n\n    pos = LocateElem_Sq(L, 30);\n    printf(\"查找 30 -> 位序 %d（下标 %d）\\n\", pos, pos - 1);\n\n    pos = LocateElem_Sq(L, 99);\n    printf(\"查找 99 -> 位序 %d（0 表示没找到）\\n\", pos);\n\n    ListDelete_Sq(&L, 2, &e);\n    printf(\"删除位序 2 -> 拿到的元素是 %d\\n\", e);\n    PrintList(\"删除后:\", L);\n\n    if (ListInsert_Sq(&L, 99, 7) == ERROR)\n    {\n        printf(\"在位序 99 插入 -> 被拒绝（越界）\\n\");\n    }\n    PrintList(\"越界插入后:\", L);\n\n    return 0;\n}"},"scaffold":"#include <stdio.h>\n\n/* ======== [已给出] 模块 01 typedef ======== */\n\ntypedef int ElemType;\n\n#define MAXSIZE 100\n\n#define OK 1\n#define ERROR 0\n\ntypedef int Status;\n\ntypedef struct\n{\n    ElemType data[MAXSIZE];\n    int length;\n} SeqList;\n\n/* ======== [已给出] 模块 02 InitList_Sq ======== */\n\nStatus InitList_Sq(SeqList *L)\n{\n    L->length = 0;\n    return OK;\n}\n\n/* ======== [已给出] 模块 03 ListInsert_Sq ======== */\n\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n    int j;\n\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n    L->data[i - 1] = e;\n\n    L->length++;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 04 ListDelete_Sq ======== */\n\nStatus ListDelete_Sq(SeqList *L, int i, ElemType *e)\n{\n    int j;\n\n    if (i < 1 || i > L->length)\n    {\n        return ERROR;\n    }\n\n    *e = L->data[i - 1];\n\n    for (j = i; j < L->length; j++)\n    {\n        L->data[j - 1] = L->data[j];\n    }\n\n    L->length--;\n\n    return OK;\n}\n\n/* ======== [已给出] 模块 05 LocateElem_Sq ======== */\n\nint LocateElem_Sq(SeqList L, ElemType e)\n{\n    int i;\n\n    for (i = 0; i < L.length; i++)\n    {\n        if (L.data[i] == e)\n        {\n            return i + 1;\n        }\n    }\n\n    return 0;\n}\n\n/* ====================================================================\n *  轮到你了：模块 06  main\n *  作用：插入、删除、查找各跑一次，每一步都打印出来对照。\n *  提示：先写函数签名，再写循环定位，最后写指针操作。\n *  写完按 Ctrl + Enter 编译运行，点\"对比\"看与参考实现的差异。\n * ==================================================================== */\n\n\n","expectedOutput":"追加 10 20 40 后: length=3 : 10 20 40 \n在下标 2 处插入 30: length=4 : 10 20 30 40 \n查找 30 -> 位序 3（下标 2）\n查找 99 -> 位序 0（0 表示没找到）\n删除位序 2 -> 拿到的元素是 20\n删除后: length=3 : 10 30 40 \n在位序 99 插入 -> 被拒绝（越界）\n越界插入后: length=3 : 10 30 40"},"02-01-07":{"modes":{"detail":"#include <stdio.h>\n\n// ============ 顺序表长什么样 ============\n//\n//   下标   0    1    2    3    4    5\n//        ┌────┬────┬────┬────┬────┐\n//   data │ 10 │ 20 │ 30 │    │    │\n//        └────┴────┴────┴────┴────┘\n//   length = 3\n//\n// 两个关键点：\n//\n//   1. 元素挨着放 —— 所以能\"按下标随机访问\"。想拿第 3 个，直接 data[2]，\n//      一步到位，不需要从头数。这是链表做不到的（链表得走 3 步）。\n//\n//   2. length 记录**实际有几个元素**，它和 MAXSIZE（**最多能放几个**）\n//      完全是两回事。表里只有 3 个元素，但数组开了 100 个格子 ——\n//      后面那些格子里的内容是垃圾值，永远不要去看它们。\n\n// 元素类型：想换成 char / double，只要改这一行\ntypedef int ElemType;\n\n// 最多能放多少个元素\n#define MAXSIZE 100\n\n// 函数返回状态：OK 表示成功，ERROR 表示失败\n// 用宏而不是 enum，是为了和教材写法保持一致\n#define OK 1\n#define ERROR 0\n\n// 返回值类型\ntypedef int Status;\n\n// 顺序表 = 定长数组 + 当前长度\n// 注意 length 不是 MAXSIZE：它随插入删除变化，是这张表的\"实际尺寸\"\ntypedef struct\n{\n// 连续内存，元素一个挨一个\n    ElemType data[MAXSIZE];\n// 当前实际元素个数\n    int length;\n} SeqList;\n\n// ============ 为什么不需要清空数组 ============\n//\n// 新手常写一个循环把 data[0..MAXSIZE-1] 全赋 0，其实完全没必要。\n//\n// 因为\"表里有几个元素\"是 **length 说了算**，而不是数组里有没有值。\n// length = 0 就意味着\"这是张空表\"，后面格子里的垃圾值谁也看不见。\n//\n// 反过来，如果只清了数组却忘了 length = 0，那才是真的错。\n\n// 参数是指针：要改调用者手里那张表本身，所以必须传地址\n// 写成 SeqList *L 而不是 SeqList L，因为\"初始化\"要修改 L 的 length。\n// 传值的话改的是副本，调用者那边毫无变化 —— 这是个高频错误。\nStatus InitList_Sq(SeqList *L)\n{\n// 空表：一个元素都没有 —— 这一句就够了\n    L->length = 0;\n// 约定：返回 OK 表示初始化成功\n    return OK;\n}\n\n// ============ 插入要\"搬家\" ============\n// 在位置 i 插入 e，意味着 e 要占住下标 i-1 那个格子。\n// 但那个格子原本有元素，所以它以及它后面所有的元素都得往后挪一格：\n//\n//   插入前： 10  20  30  _   _        （length = 3，在下标 1 处插入 15）\n//             0   1   2   3   4\n//\n//   后移：   10  20  __  30  _        ← 30 从下标 2 挪到 3\n//                └── 20 从下标 1 挪到 2\n//\n//   填入：   10  15  20  30  _        ← e 放进腾出来的下标 1\n//   length = 4\n//\n// ============ 为什么必须从后往前搬 ============\n//\n//   正确（j 从 length 递减到 i）：先搬最右边的，空位一路向左让出来\n//       data[3] = data[2]   →  data[2] = data[1]   →  data[1] = e\n//\n//   错误（j 从 i 递增到 length）：先搬左边的，会把右边的值覆盖掉\n//       data[1] = data[0]   →  data[2] = data[1]   ← 这里 data[1] 已经是 10 了！\n//\n// 一句话记住：**搬家永远从\"没人占的那一头\"开始搬**。\n\n// L 是要改的表，i 是插入位置（1 基，不是下标），e 是待插入元素\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length+1 都算合法\n// 为什么是 length+1？因为\"插在最后一个元素后面\"是允许的。\n// 长度为 3 时，合法位置是 1、2、3、4；i=5 就越界了。\n// 注意这里用的是 length 而不是 MAXSIZE —— 前者是\"有几个元素\"，\n// 后者是\"能装几个\"，判断位置用前者。\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n// 表满了也插不进去\n// 这是顺序表天生的局限：容量是固定的，满了只能报错（或者扩容搬迁）。\n// 链表没有这个问题 —— 内存够就能一直加。\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n// 从最后一个元素开始，一个个往后挪一格\n// j 从 length 递减到 i：注意 data[length] 这个格子本身是\"空\"的\n// （不在 length 计数范围内），所以第一次赋值刚好把最后那个元素挪进空位。\n// 循环结束时，下标 i-1 那个格子已经被腾出来了。\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n// 把 e 填进腾出来的空位\n// 存的是下标，所以位置 i 对应 data[i-1]。这个 ±1 的转换是初学时\n// 最容易写错的地方，建议在纸上画一遍。\n    L->data[i - 1] = e;\n\n// 元素多了一个\n    L->length++;\n\n// 插入成功\n    return OK;\n}\n\n// ============ 删除也要\"搬家\"，方向相反 ============\n//\n//   删除前： 10  15  20  30  _        （length = 4，删除下标 1 的 15）\n//             0   1   2   3   4\n//\n//   前移：   10  20  30  __  _        ← 20 从下标 2 挪到 1，30 从 3 挪到 2\n//   length = 3\n//\n// 和插入正好相反：**删除要从前往后搬**。\n// 因为这次空位在左边，得让右边的元素一个个往左填。\n// 如果从后往前搬，就会把还没搬的元素覆盖掉。\n\n// e 是\"带回被删元素\"的出口参数 —— 调用者想知道删掉的是谁\nStatus ListDelete_Sq(SeqList *L, int i, ElemType *e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length\n// 注意和插入的区别：这里上限是 length 而不是 length+1。\n// 因为删除的是\"已经存在的元素\"，而长度为 3 的表里只存在 1、2、3 三个位置。\n    if (i < 1 || i > L->length)\n    {\n        return ERROR;\n    }\n\n// 先把被删元素取出来 —— 必须在搬家之前做\n// 因为它马上就要被后面的元素覆盖掉了，晚一步就取不到了。\n    *e = L->data[i - 1];\n\n// 从被删位置开始，后面的元素统统往前挪一格\n// j 从 i 递增到 length-1：把 data[j] 搬到 data[j-1]。\n// 循环结束后，最后一个位置 data[length-1] 的内容虽然还在，\n// 但已经不在 length 计数范围内，等于被抛弃了。\n    for (j = i; j < L->length; j++)\n    {\n        L->data[j - 1] = L->data[j];\n    }\n\n// 元素少了一个\n    L->length--;\n\n// 删除成功\n    return OK;\n}\n\n// ============ 顺序查找 ============\n//\n//   找 30： 10  20  30  40\n//           ↑   ↑   ↑\n//           比  比  中 ✓        比较 3 次\n//\n// 这叫\"顺序查找\"，平均要比较 (n+1)/2 次，时间复杂度 O(n)。\n//\n// 如果这张表是**有序的**，可以改用二分查找，降到 O(log n)。\n// 但二分要求\"能按下标跳跃访问\"—— 这恰好是顺序表的强项，\n// 链表就做不了（链表中途没法直接跳到中间）。\n\n// 返回\"位序\"（1 基），不是下标；找不到返回 0\n// 为什么返回位序而不是下标？因为调用者拿到 0 就知道\"没找到\"，\n// 而下标 0 是合法位置，用它当\"没找到\"会分不清。\nint LocateElem_Sq(SeqList L, ElemType e)\n{\n// 循环变量\n    int i;\n\n// 从下标 0 扫到 length-1 —— 只扫\"有效元素\"那一段\n// 一定用 length 做上界，不要用 MAXSIZE：\n// 后面那些格子是垃圾值，扫进去可能\"莫名奇妙找到\"一个相等的东西。\n    for (i = 0; i < L.length; i++)\n    {\n// 相等就返回它的位序（下标 +1）\n        if (L.data[i] == e)\n        {\n            return i + 1;\n        }\n    }\n\n// 扫完了还没有，就是没有\n    return 0;\n}\n\n// ============ 为什么要\"一步一打印\" ============\n//\n// 顺序表的 bug 大多不是崩溃，而是**悄悄出错**：\n// 元素顺序错了、length 忘了加、搬家方向反了 —— 程序照跑不误，\n// 只是数据不对。所以每一步都打印出来，肉眼一比对就露馅了。\n\n// 打印辅助函数放在 main 之前\n// 它不属于要默写的核心操作，所以直接给出、不用自己写\nstatic void PrintList(const char *tag, SeqList L)\n{\n    int i;\n\n    printf(\"%s length=%d : \", tag, L.length);\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n}\n\n// 主函数\nint main(void)\n{\n// 定义一张表\n    SeqList L;\n// 用来接住被删除的元素\n    ElemType e;\n// 查找结果（位序）\n    int pos;\n\n// 先初始化 —— 别忘了，否则 length 是随机值\n    InitList_Sq(&L);\n\n// 依次在表尾追加 10、20、40\n// 插到 length+1 的位置就是\"追加到末尾\"，每次只搬家 0 次，是最省的情形。\n    ListInsert_Sq(&L, 1, 10);\n    ListInsert_Sq(&L, 2, 20);\n    ListInsert_Sq(&L, 3, 40);\n    PrintList(\"追加 10 20 40 后:\", L);\n\n// 在下标 2（位序 3）处插入 30，让表变成有序的\n// 这一次要搬家 1 个元素（40 后移），刚好演示\"插入要挪位置\"。\n    ListInsert_Sq(&L, 3, 30);\n    PrintList(\"在下标 2 处插入 30:\", L);\n\n// 查找 30 在哪\n    pos = LocateElem_Sq(L, 30);\n    printf(\"查找 30 -> 位序 %d（下标 %d）\\n\", pos, pos - 1);\n\n// 查找一个不存在的值\n    pos = LocateElem_Sq(L, 99);\n    printf(\"查找 99 -> 位序 %d（0 表示没找到）\\n\", pos);\n\n// 删除位序 2 的元素，看它是不是 20\n// 这次要搬家 2 个元素（30、40 前移），演示\"删除要往前填\"。\n    ListDelete_Sq(&L, 2, &e);\n    printf(\"删除位序 2 -> 拿到的元素是 %d\\n\", e);\n    PrintList(\"删除后:\", L);\n\n// 越界插入应该被挡住\n    if (ListInsert_Sq(&L, 99, 7) == ERROR)\n    {\n        printf(\"在位序 99 插入 -> 被拒绝（越界）\\n\");\n    }\n    PrintList(\"越界插入后:\", L);\n\n// 正常结束\n    return 0;\n}\n","short":"#include <stdio.h>\n\n\n// 元素类型：想换成 char / double，只要改这一行\ntypedef int ElemType;\n\n// 最多能放多少个元素\n#define MAXSIZE 100\n\n// 函数返回状态：OK 表示成功，ERROR 表示失败\n#define OK 1\n#define ERROR 0\n\n// 返回值类型\ntypedef int Status;\n\n// 顺序表 = 定长数组 + 当前长度\ntypedef struct\n{\n// 连续内存，元素一个挨一个\n    ElemType data[MAXSIZE];\n// 当前实际元素个数\n    int length;\n} SeqList;\n\n\n// 参数是指针：要改调用者手里那张表本身，所以必须传地址\nStatus InitList_Sq(SeqList *L)\n{\n// 空表：一个元素都没有 —— 这一句就够了\n    L->length = 0;\n// 约定：返回 OK 表示初始化成功\n    return OK;\n}\n\n\n// L 是要改的表，i 是插入位置（1 基，不是下标），e 是待插入元素\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length+1 都算合法\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n// 表满了也插不进去\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n// 从最后一个元素开始，一个个往后挪一格\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n// 把 e 填进腾出来的空位\n    L->data[i - 1] = e;\n\n// 元素多了一个\n    L->length++;\n\n// 插入成功\n    return OK;\n}\n\n\n// e 是\"带回被删元素\"的出口参数 —— 调用者想知道删掉的是谁\nStatus ListDelete_Sq(SeqList *L, int i, ElemType *e)\n{\n// 循环变量\n    int j;\n\n// 位置合法性：1 到 length\n    if (i < 1 || i > L->length)\n    {\n        return ERROR;\n    }\n\n// 先把被删元素取出来 —— 必须在搬家之前做\n    *e = L->data[i - 1];\n\n// 从被删位置开始，后面的元素统统往前挪一格\n    for (j = i; j < L->length; j++)\n    {\n        L->data[j - 1] = L->data[j];\n    }\n\n// 元素少了一个\n    L->length--;\n\n// 删除成功\n    return OK;\n}\n\n\n// 返回\"位序\"（1 基），不是下标；找不到返回 0\nint LocateElem_Sq(SeqList L, ElemType e)\n{\n// 循环变量\n    int i;\n\n// 从下标 0 扫到 length-1 —— 只扫\"有效元素\"那一段\n    for (i = 0; i < L.length; i++)\n    {\n// 相等就返回它的位序（下标 +1）\n        if (L.data[i] == e)\n        {\n            return i + 1;\n        }\n    }\n\n// 扫完了还没有，就是没有\n    return 0;\n}\n\n\n// 打印辅助函数放在 main 之前\nstatic void PrintList(const char *tag, SeqList L)\n{\n    int i;\n\n    printf(\"%s length=%d : \", tag, L.length);\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n}\n\n// 主函数\nint main(void)\n{\n// 定义一张表\n    SeqList L;\n// 用来接住被删除的元素\n    ElemType e;\n// 查找结果（位序）\n    int pos;\n\n// 先初始化 —— 别忘了，否则 length 是随机值\n    InitList_Sq(&L);\n\n// 依次在表尾追加 10、20、40\n    ListInsert_Sq(&L, 1, 10);\n    ListInsert_Sq(&L, 2, 20);\n    ListInsert_Sq(&L, 3, 40);\n    PrintList(\"追加 10 20 40 后:\", L);\n\n// 在下标 2（位序 3）处插入 30，让表变成有序的\n    ListInsert_Sq(&L, 3, 30);\n    PrintList(\"在下标 2 处插入 30:\", L);\n\n// 查找 30 在哪\n    pos = LocateElem_Sq(L, 30);\n    printf(\"查找 30 -> 位序 %d（下标 %d）\\n\", pos, pos - 1);\n\n// 查找一个不存在的值\n    pos = LocateElem_Sq(L, 99);\n    printf(\"查找 99 -> 位序 %d（0 表示没找到）\\n\", pos);\n\n// 删除位序 2 的元素，看它是不是 20\n    ListDelete_Sq(&L, 2, &e);\n    printf(\"删除位序 2 -> 拿到的元素是 %d\\n\", e);\n    PrintList(\"删除后:\", L);\n\n// 越界插入应该被挡住\n    if (ListInsert_Sq(&L, 99, 7) == ERROR)\n    {\n        printf(\"在位序 99 插入 -> 被拒绝（越界）\\n\");\n    }\n    PrintList(\"越界插入后:\", L);\n\n// 正常结束\n    return 0;\n}\n","none":"#include <stdio.h>\n\n\ntypedef int ElemType;\n\n#define MAXSIZE 100\n\n#define OK 1\n#define ERROR 0\n\ntypedef int Status;\n\ntypedef struct\n{\n    ElemType data[MAXSIZE];\n    int length;\n} SeqList;\n\n\nStatus InitList_Sq(SeqList *L)\n{\n    L->length = 0;\n    return OK;\n}\n\n\nStatus ListInsert_Sq(SeqList *L, int i, ElemType e)\n{\n    int j;\n\n    if (i < 1 || i > L->length + 1)\n    {\n        return ERROR;\n    }\n\n    if (L->length >= MAXSIZE)\n    {\n        return ERROR;\n    }\n\n    for (j = L->length; j >= i; j--)\n    {\n        L->data[j] = L->data[j - 1];\n    }\n\n    L->data[i - 1] = e;\n\n    L->length++;\n\n    return OK;\n}\n\n\nStatus ListDelete_Sq(SeqList *L, int i, ElemType *e)\n{\n    int j;\n\n    if (i < 1 || i > L->length)\n    {\n        return ERROR;\n    }\n\n    *e = L->data[i - 1];\n\n    for (j = i; j < L->length; j++)\n    {\n        L->data[j - 1] = L->data[j];\n    }\n\n    L->length--;\n\n    return OK;\n}\n\n\nint LocateElem_Sq(SeqList L, ElemType e)\n{\n    int i;\n\n    for (i = 0; i < L.length; i++)\n    {\n        if (L.data[i] == e)\n        {\n            return i + 1;\n        }\n    }\n\n    return 0;\n}\n\n\nstatic void PrintList(const char *tag, SeqList L)\n{\n    int i;\n\n    printf(\"%s length=%d : \", tag, L.length);\n    for (i = 0; i < L.length; i++)\n    {\n        printf(\"%d \", L.data[i]);\n    }\n    printf(\"\\n\");\n}\n\nint main(void)\n{\n    SeqList L;\n    ElemType e;\n    int pos;\n\n    InitList_Sq(&L);\n\n    ListInsert_Sq(&L, 1, 10);\n    ListInsert_Sq(&L, 2, 20);\n    ListInsert_Sq(&L, 3, 40);\n    PrintList(\"追加 10 20 40 后:\", L);\n\n    ListInsert_Sq(&L, 3, 30);\n    PrintList(\"在下标 2 处插入 30:\", L);\n\n    pos = LocateElem_Sq(L, 30);\n    printf(\"查找 30 -> 位序 %d（下标 %d）\\n\", pos, pos - 1);\n\n    pos = LocateElem_Sq(L, 99);\n    printf(\"查找 99 -> 位序 %d（0 表示没找到）\\n\", pos);\n\n    ListDelete_Sq(&L, 2, &e);\n    printf(\"删除位序 2 -> 拿到的元素是 %d\\n\", e);\n    PrintList(\"删除后:\", L);\n\n    if (ListInsert_Sq(&L, 99, 7) == ERROR)\n    {\n        printf(\"在位序 99 插入 -> 被拒绝（越界）\\n\");\n    }\n    PrintList(\"越界插入后:\", L);\n\n    return 0;\n}\n"},"scaffold":null,"expectedOutput":"追加 10 20 40 后: length=3 : 10 20 40 \n在下标 2 处插入 30: length=4 : 10 20 30 40 \n查找 30 -> 位序 3（下标 2）\n查找 99 -> 位序 0（0 表示没找到）\n删除位序 2 -> 拿到的元素是 20\n删除后: length=3 : 10 30 40 \n在位序 99 插入 -> 被拒绝（越界）\n越界插入后: length=3 : 10 30 40"}}}